# Weight sensitivity — the empirical α-Pareto frontier

The α-weight sensitivity analysis for the FAMAIL objective
(`L = α_sp·F_spatial + α_ca·F_causal + α_fi·F_fidelity`): five sweep points (α_fidelity fixed at 0.1)
plus the shipped headline as anchor, every point a full k=10,000 trim+lift edit run on Shenzhen
PRIMARY with the infeasible-trim filter — the identical pipeline as the headline.

| file | contents |
|---|---|
| [`alpha_sweep_summary.md`](alpha_sweep_summary.md) | the 6-point frontier table + Pareto flags + criterion line |
| [`alpha_pareto.png`](alpha_pareto.png) | the (ΔF_spatial, ΔF_causal) scatter with frontier |
| [`alpha_sweep_summary.json`](alpha_sweep_summary.json) | machine-readable rows + provenance dirs |
| [`DECISION.md`](DECISION.md) | the Q0 weight-decision memo (keep-vs-re-anchor) |

**Headline findings:** ΔF_causal is flat (+0.0217..+0.0227) across α_spatial ∈ [0, 0.55] and drops only
at 0.8; ΔF_spatial rises monotonically (+0.0057 → +0.0117). The shipped (0.2, 0.7, 0.1) is weakly
dominated by (0.55, 0.35, 0.1) — noise-scale on the causal axis, material on the spatial axis; see
`DECISION.md` for the disposition.

**Provenance:** generated by `python -m famail_temporal.analysis.alpha_sweep_summary` (tests in
`famail_temporal/analysis/tests/test_alpha_sweep_summary.py`) over
`famail_temporal/results/*_alpha_sweep_*_filtered/metrics.json` + the anchor
`2026-07-08T14-03-03_supply_lift_v1_shz_primary_filtered/metrics.json`; sweep driver
`famail_temporal/results/alpha_sweep/driver.sh`; ledger row Q0 in
`famail_temporal/results/EXPERIMENTS_RUN_LEDGER.md`.
