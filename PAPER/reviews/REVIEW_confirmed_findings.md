# Adversarial review — 29 confirmed findings (0 refuted)

## [A] IMPORTANT — README cites bc F_causal as 0.722 (seed-0 value) instead of the 5-seed mean 0.725
**Verdict:** confirmed  
Verified directly against PAPER/data/L1v2_4feat_multiseed.json: per_source.bc.f_causal.mean = 0.7252180576324463 (rounds to 0.725), with per-seed values [0.72232711, 0.72766876, 0.72674727, 0.72630644, 0.72304070]. The value 0.722 in PAPER/README.md line 19 equals exactly seed-0 (0.72232711 -> 0.722), not the mean. In the same sentence edited (0.741) and raw (0.725) ARE the multiseed means (edited.mean=0.74093, raw.mean=0.72529 -> 0.725), so bc is inconsistently transcribed as a single-seed value. The raw-bc mean gap is only 6.77e-5, so bc and raw are statistically indistinguishable; printing bc as 0.722 spuriously makes bc look ~0.003 below raw. All claimed numbers and the evidence reproduce to the digit. Severity is important (it misrepresents a headline source number and an inter-source comparison) but not critical: the load-bearing claim that 'edited is the fairest faithful source' at 0.741 is unaffected.  
**Fix:** In PAPER/README.md line 19, change 'bc 0.722' to 'bc 0.725' to use the 5-seed mean (per_source.bc.f_causal.mean = 0.7252) consistent with edited/raw in the same clause. Optionally note bc is indistinguishable from raw (Delta = 0.00007, n.s.) so the sentence does not imply bc underperforms raw.

## [A] IMPORTANT — comparison_3v4.md line 18 uses seed-0 values for bc (0.7223) and gan (0.7385) instead of 5-seed means
**Verdict:** confirmed  
Verified directly against PAPER/data/L1v2_4feat_multiseed.json. The 4-feature L1 ordering on line 18 of PAPER/tables/comparison_3v4.md reads "edited 0.7409 > raw 0.7253 > bc 0.7223 (gan 0.7385 disqualified)". The JSON gives, for per_source.f_causal: bc mean=0.72522 with values=[0.72233,0.72767,0.72675,0.72631,0.72304] (seed-0=0.72233→0.7223), and gan mean=0.73692 with values=[0.73847,0.73164,0.73760,0.74490,0.73198] (seed-0=0.73847→0.7385). So the table's bc 0.7223 and gan 0.7385 are exactly the seed-0 values, not the 5-seed means (0.7252 and 0.7369). edited (mean 0.74093, std=0.0) and raw (mean 0.72529, std=0.0) are deterministic across seeds, so their values coincide with the means and are correctly reported. The ordering therefore mixes seed-0 values for bc/gan with means for edited/raw — internally inconsistent and misstates two of four numbers, as the finding claims. Impact is bounded: magnitudes are small (bc off by 0.0029, gan by 0.0016), the ordering edited>gan>raw>bc is unchanged, gan remains fidelity-disqualified, and bc<raw either way, so no qualitative conclusion is affected. Hence important, not critical. (The 3-feature column on the same line was not part of this finding and is out of scope, though it likely warrants the same check.)  
**Fix:** In PAPER/tables/comparison_3v4.md line 18, 4-feature column, replace the seed-0 values with the 5-seed means to match edited/raw: "edited **0.7409** > raw 0.7253 > bc 0.7252 (gan 0.7369 disqualified)". Optionally audit the 3-feature column on the same line (edited 0.8193 > raw 0.8069 > bc 0.8045, gan 0.8152 disqualified) against its source multiseed JSON for the same seed-0-vs-mean inconsistency.

## [A] IMPORTANT — Every "p = 0.031" is the n=6 Wilcoxon floor (2/2^6) — significance certifies all-same-sign only, not effect magnitude
**Verdict:** confirmed  
All factual claims verified against PAPER/data/weighted_bc_4feat_paired_stats.json and the paper text.

(1) The n=6 two-sided Wilcoxon signed-rank floor is exactly 2/2^6 = 0.03125 (recomputed). Every reported p=0.03125 in the file equals this floor to machine precision.

(2) I verified ALL 13 arms reporting p=0.03125 (not just the 5 named in the finding): every one has all 6 nonzero paired diffs sharing the same sign (f_causal edited_w10/w20/w30, most_fair_w10/w30; f_spatial edited_w10/w20/w30, most_fair_w30; fidelity_b edited_w10/w20/w30, random_w30). With n=6 and all-same-sign diffs the signed-rank statistic saturates at its extreme, so the test floors at 0.03125 regardless of how large or small the diffs are. The claim that p=0.03125 carries no magnitude information at this n is mathematically correct.

(3) The headline magnitude-vs-significance collision is real: most_fair_w30 mean = +0.0021972 and edited_w30 mean = +0.0274495 (ratio 12.49x, matching the paper's own "~12x") both receive the identical p=0.03125. f_causal edited_w10 (+0.0190) likewise shares the same p. So a 12x effect-size gap maps to one p-value.

Where the finding slightly overreaches: the paper does NOT actually use the p-value to rank arms. comparison_3v4.md line 21 explicitly separates magnitude (calls most_fair "~12x smaller than editing", using means/CIs) from significance, and the random/most_fair null arms are distinguished by their means and n.s. p-values (e.g. random_w30 also floors at 0.03125 yet is treated as placebo by mean). So "the paper treats p=0.031 as if the test discriminated between arms" is not literally how the discrimination is done — magnitude is carried by the means and t-CIs, which ARE reported. Hence partial-on-interpretation, but the core statistical defect (a bare, uncaveated, floored p reported as the stamp of significance) is confirmed, so net verdict is confirmed at important (not critical): no number is wrong and the substantive ranking rests on means/CIs that survive, but the significance reporting is genuinely misleading as written (README line 23: "all Wilcoxon p = 0.031" with no floor disclosure, no exact-test note, and no multiple-comparison adjustment across ~36 arms tested — at the 0.03125 floor, family-wise error is not controlled).  
**Fix:** Three concrete edits, all presentation-level (no re-run needed; the means/CIs already carry the real evidence):

1. PAPER/README.md line 23 and comparison_3v4.md/experiment_cleanup_delta.md: replace bare "Wilcoxon p = 0.031" with a footnote stating that at n=6 the two-sided signed-rank test floors at 2/2^6 = 0.03125, so p=0.03125 means "all 6 paired seeds agreed in sign" and conveys no effect-magnitude information; effect size is read from the mean Δ and its t-CI, not from p.

2. Lead the significance argument with the t-CIs already in the JSON (e.g. edited_w30 f_causal CI [0.0243, 0.0306] excludes 0 and excludes the most_fair CI [0.00098, 0.00338]; the CIs do NOT overlap, which IS the magnitude-discriminating evidence the p-value cannot provide). Cite the non-overlapping CIs as the formal basis for "edit > select", replacing reliance on the shared p.

3. Note the multiple-comparison exposure: ~36 arm×metric tests are run, several floored at 0.03125; either apply a correction (e.g. Holm) or state that p is used descriptively as a sign-agreement check, with the inferential weight on the CIs. Optionally increase seeds (n>=8 lowers the floor below 0.01) if a sub-0.03125 p is wanted for the headline.

## [A] IMPORTANT — most_fair (SELECT) significance is metric-dependent sign-luck, not a robust effect
**Verdict:** confirmed  
Every number in the finding checks out against the actual source JSONs.

3-feature (famail_temporal/results/weighted_bc_sweep/cleaned_6seed/paired_stats.json, lines 147-164): most_fair_w30 diffs = [-0.0026, 0.0020, -0.0011, 0.0053, 0.0009, 0.0024] (mixed sign: 2 neg, 4 pos), mean +0.001157, wilcoxon_p = 0.5625.

4-feature (PAPER/data/weighted_bc_4feat_paired_stats.json, lines 147-164): most_fair_w30 diffs = [0.0011, 0.0018, 0.0033, 0.0029, 0.0007, 0.0033] (all positive), mean +0.002197, wilcoxon_p = 0.03125.

Same intervention (upweight the already-fairest trajectories at weight 30), same 6 seeds, opposite verdict. The mechanism the finding identifies is correct and decisive: with n=6 the Wilcoxon signed-rank minimum p-value is exactly 1/2^5 = 0.03125, attained only when all 6 differences share a sign. So the 4-feature 'significance' is literally just the all-same-sign condition. The per-seed magnitudes (0.0007–0.0033) are the same order as the +0.0022 mean and the same order as the noise that flips two seeds negative in the 3-feature run — i.e., the effect is within seed noise and its 'significance' is sign-luck under one metric.

The framing claims are also verified verbatim: README.md lines 24-27 sell 'EDIT ≫ SELECT > RANDOM ... upweighting the already-fairest ones (+0.002, ~12x smaller)'; comparison_3v4.md line 21 reports most_fair_w30 +0.0022 (p=0.031) vs +0.0012 (n.s.), and lines 33-36 elevate it to a 'gradient' that is 'arguably a stronger story than selection does nothing.' That promotes the single least-reproducible, metric-flippable cell in the suite to a load-bearing tier.

Why important rather than critical: the finding slightly over-broadens by implicating the whole 'EDIT ≫ SELECT > RANDOM' headline. The EDIT and RANDOM endpoints are in fact robust across both metrics — edited_w30 is p=0.03125 in both (3-feat diffs all positive [0.0266,0.0255,0.0235,0.0277,0.0273,0.0253]; 4-feat all positive), and random_w30 is n.s. in both (p=0.6875 / p=0.3125). So the core two-pillar / dose-response story does not collapse. What is unsupported is specifically (a) labeling the SELECT/most_fair tier 'significant (p=0.031)' as if robust, and (b) the comparison_3v4 'shows the gradient / stronger story' narrative. That is a contained but real overclaim on a near-zero, metric-dependent result, hence important, not critical.  
**Fix:** Demote the most_fair/SELECT result from a significant tier to an honest near-null. Concretely: (1) In PAPER/README.md lines 24-27, stop attaching 'p=0.031' to SELECT and stop the 'EDIT ≫ SELECT > RANDOM' three-tier gradient framing; instead state that upweighting the already-fairest trajectories yields at most a marginal, metric-dependent effect (3-feat +0.0012 n.s., p=0.56; 4-feat +0.0022 with all-same-sign p=0.031 driven entirely by the n=6 sign pattern), i.e. SELECT is effectively null/weak and NOT robust, whereas EDIT (+0.027, p=0.031 in both metrics) and RANDOM (null in both) are robust. (2) In PAPER/tables/comparison_3v4.md, in the line-21 table row mark most_fair_w30 as 'metric-dependent / not robust (n.s. under 3-feature)' rather than implying significance, and delete or heavily qualify lines 33-36 ('arguably a stronger story... shows the gradient') — note explicitly that n=6 Wilcoxon p=0.031 requires only all-6-same-sign, which fails to replicate under the 3-feature metric, so no gradient can be claimed. (3) If a gradient/SELECT claim is wanted, back it with a sign-robust or pooled test (e.g., more seeds, or a paired effect-size CI that excludes 0 under both feature sets) before reinstating any significance label.

## [A] MINOR — n=5 two-sided Wilcoxon cannot reach p<0.05; nulls cite p as evidence of absence (but pillar is at n=6, and nulls are noise-band-supported)
**Verdict:** partial  
The arithmetic is correct and reproduces exactly against the source files. n=5 two-sided exact Wilcoxon floor = 2/2^5 = 0.0625 > 0.05 (verified via scipy). PAPER/data/L2_4feat_metrics.json paired.f_causal.raw is n=5 with wilcoxon_p=0.3125 (verbatim). PAPER/data/variance_4feat_aggregate.json paired_delta is n=5; reconstructing the famail-b0 f_causal diffs from per_seed_values yields Wilcoxon p=0.3125 and paired t-test p=0.260, both matching the finding exactly. PAPER/README.md:21 and PAPER/tables/comparison_3v4.md:19,23 do present these as 'n.s.' nulls. The methodological point — that a two-sided n=5 Wilcoxon is structurally incapable of p<0.05, so quoting p=0.31/0.44 as positive confirmation of absence-of-transfer is a p-value misuse (absence of evidence vs evidence of absence) — is legitimate.

However, the severity ('critical', 'pillar not actually tested') is overstated, for three evidenced reasons. (1) The load-bearing AFFIRMATIVE claim is the weighted-BC recovery, which is at n=6, not n=5: PAPER/data/weighted_bc_4feat_sweep.json has seeds [0,1,2,3,4,5] and reports edited_w10/w20/w30 wilcoxon_p=0.03125. The n=6 two-sided exact floor is exactly 0.03125, so that test CAN and DOES reach significance — the 'design predetermines non-significance' critique does not apply to the pillar carrying the paper's positive result. The negative-transfer 'L2 vanilla averages it away' line is the foil, not the standalone load-bearing claim. (2) The nulls are mid-range, not floor-pinned: observed two-sided p=0.3125 is ~5x the 0.0625 floor; L2 diffs are 3 neg/2 pos (mixed sign) and variance diffs are 4 pos/1 neg. A genuine large effect at n=5 would be all-same-sign and land at the 0.0625 floor; these land far above it, so the non-significance reflects genuinely small/mixed effects, not solely the sample-size ceiling — it is not 'circular.' (3) The effect sizes (L2 deltaF_causal -0.0010, variance +0.0006, both within the ~+/-0.003 cross-seed std) and the equivalence-to-noise argument are what actually support the nulls; the Wilcoxon p is decorative. So the finding identifies a real framing flaw but mischaracterizes its impact: the conclusion does not collapse.  
**Fix:** Reframe, do not re-run. (a) For the L2 and variance nulls, stop quoting the two-sided Wilcoxon p as confirmation of absence; instead state the effect size against the seed-noise band, e.g. 'L2 edited-raw deltaF_causal = -0.0010, within the +/-0.003 cross-seed std (5 seeds); no detectable transfer.' Add an explicit caveat that with n=5 a two-sided Wilcoxon cannot reach p<0.05 (floor 0.0625), so non-significance is reported as absence of a detectable effect, not a powered test of zero effect. Optionally report a TOST-style equivalence bound or just the per-seed signs (L2: 3-/2+; variance: 4+/1-) to show the effect is genuinely mixed/small rather than power-limited. (b) Note explicitly that the affirmative weighted-BC pillar is at n=6 (floor 0.03125), so its p=0.031 is a real, attainable rejection — i.e. the significant claim is not subject to this ceiling. (c) For consistency, run the weighted-BC seeds and the L2/variance seeds at the same n (ideally add the 6th seed to L2/variance) so the foil and the result share a denominator; this is cosmetic for the conclusion but removes the asymmetry a reviewer would flag.

## [A] IMPORTANT — No multiple-comparison correction: all 13 "significant" results sit at the n=6 Wilcoxon floor (0.03125) and none survive Bonferroni
**Verdict:** confirmed  
Verified directly against source files. PAPER/data/weighted_bc_4feat_paired_stats.json contains exactly 36 paired-Wilcoxon cells (4 metrics {f_causal, f_spatial, fidelity_a, fidelity_b} x 9 arms {edited, edited_w10/20/30, random_w10/30, most_fair_w10/20/30}). Exactly 13 are p<=0.05, and all 13 are at p=0.03125. Confirmed via scipy that wilcoxon on 6 same-signed diffs returns exactly 0.03125 = 2/2^6, and every test has n=6 — so 0.03125 is the minimum achievable two-sided p at this sample size. Bonferroni threshold 0.05/36 = 0.00139 < 0.03125, so NO result can clear family-wise correction; zero significant findings survive. Grep across all of PAPER/ for bonferroni|multiple|correction|FDR|holm|benjamini|familywise|multiplicity returns nothing. PAPER/README.md line 23 states the headline "all Wilcoxon p = 0.031, dose-responsive" with no multiplicity caveat. One correction to the finding's wording: weighted_bc_4feat_sweep.json's paired_vs_raw block holds the SAME 36 tests, not additional ones, so the 4-feat universe is 36 (the 3-feat artifacts, in separate result dirs, would add a parallel family and only worsen the concern, but were not present under PAPER/data/ to count). Severity is important rather than critical because: the inference is paired, dose-responsive, sign-consistent (6/6 seeds) and placebo-controlled (random arm null) — i.e., the evidence is structurally stronger than 36 isolated p-values, and the floor issue is fundamentally a low-n problem (n=6 cannot produce a Bonferroni-surviving Wilcoxon p regardless of effect size), not a fabrication. But the paper presents an uncorrected "p=0.031" as the significance story without acknowledging multiplicity or the floor, which is a real, defensible reviewer objection at a venue like KDD.  
**Fix:** Add an explicit multiplicity/statistics note to PAPER/README.md (and any figure caption citing p=0.031). Three concrete steps: (1) State the multiple-comparison structure plainly: 36 paired tests in the 4-feat family (plus the parallel 3-feat family), and note that with n=6 seeds the two-sided Wilcoxon p is floored at 0.03125, so no individual test can survive Bonferroni — i.e., do not lean on per-test family-wise-corrected significance at this n. (2) Reframe the headline evidence away from a single p-value toward what actually carries it: sign-consistency (6/6 seeds positive), monotonic dose-response across w10/20/30, the placebo (random) and select (most-fair) arms being null/12x smaller, and the t-CIs excluding zero. These are pre-registered-style structural checks that do not depend on uncorrected significance. (3) If a corrected-significance claim is wanted, either increase the number of seeds (n>=8 lowers the Wilcoxon floor below typical corrected thresholds) or report a permutation/sign-test combined across the dose-arms as a single pre-specified test, and present FDR (Benjamini-Hochberg) alongside Bonferroni so the reader sees the multiplicity was considered.

## [A] MINOR — Random arm's significant Fidelity-B shift (+0.0028, p=0.031, 6/6) is undisclosed, but "null"/"placebo" labels are F_causal-scoped, not uniform
**Verdict:** partial  
NUMBERS CONFIRMED. PAPER/data/weighted_bc_4feat_paired_stats.json fidelity_b.random_w30 = mean +0.0028019..., wilcoxon_p = 0.03125, diffs [0.0022, 0.003, 0.0048, 0.003, 0.001, 0.0028] — all 6 positive (lines 585-601). The paper does treat p=0.03125 (all-6-same-sign Wilcoxon, the floor p for n=6) as "significant": comparison_3v4.md line 21 labels most_fair_w30 F_causal (+0.0022, p=0.031) a real effect ("weakly positive"), distinguishing it from the "(n.s.)" arms. So by the paper's own rule, random_w30's Fidelity-B effect (+0.0028, p=0.031, larger than edited_w10's Fidelity-B +0.0025 p=0.031) IS "significant." That part of the finding holds.

CHARACTERIZATION OVERSTATED. The finding claims the random arm is called "uniformly null" / "'null'/'placebo null' without qualification." This is not accurate: every "null"/"placebo" usage in the paper is scoped to F_causal, the metric of the PI-requested ablation. README lines 24-26 ("EDIT ≫ SELECT > RANDOM ... random placebo (null)") are explicitly about ΔF_causal (+0.027/+0.002 are F_causal). comparison_3v4.md line 22's "placebo null" sits in a table column of F_causal values (−0.0004/+0.0013 = random_w30 F_causal); line 34 "random placebo stays null" is inside a paragraph wholly about F_causal. The paper never asserts the random arm is null on Fidelity-B. And on F_causal the random arm genuinely IS null (mean +0.0013, p=0.3125, 4/6 positive) — the load-bearing placebo claim is correctly reported. The placebo logic is intentionally a fairness (F_causal) placebo: "does oversampling alone move fairness?" Answer (no) is sound.

REAL KERNEL. There is a genuine, narrower gap: upweighting a random subset DOES significantly shift Fidelity-B (+0.0028, p=0.031, 6/6), and this is never disclosed — while most_fair's tiny F_causal effect gets a dedicated "honest nuance" paragraph (lines 32-36). That is asymmetric which-cell-to-discuss reporting, and the README's parenthetical "(null)" in a headline bullet could mislead a casual reader into thinking the random arm does nothing anywhere. The deeper methodological point — that floor p=0.03125 flags any consistent-sign micro-effect as "significant," so these Fidelity-B "significances" are weak evidence on both the active and placebo arms — is correct and worth a caveat.

NET: numeric claim confirmed; "uniformly null / without qualification" is refuted (labels are F_causal-scoped and F_causal-null is real). Hence partial. Severity minor: no headline conclusion is wrong, but an honest-completeness gap exists.  
**Fix:** Two small disclosures, no re-run needed. (1) In comparison_3v4.md/README, qualify the placebo claim as metric-specific, e.g. "random placebo is null on F_causal (the fairness target: +0.0013, p=0.31)" rather than an unscoped "(null)". (2) Add one honest-nuance sentence acknowledging that upweighting ANY subset (random included) produces a small significant Fidelity-B shift (random_w30 +0.0028, p=0.031, 6/6; cf. edited_w10 +0.0025) — i.e., the Fidelity-B movement is partly a generic oversampling/distribution-shift effect, not edit-specific — so the EDIT-specificity claim rests on F_causal, where the random arm is genuinely null. (3) Optionally note that p=0.03125 is the floor for n=6 Wilcoxon, so these sub-0.003 Fidelity-B "significances" reflect sign-consistency of tiny effects, not magnitude; this keeps the active-arm Fidelity-B claims honest too.

## [A] IMPORTANT — L1 'fairest faithful' gap is deterministic (no CI possible) and README/comparison tables understate bc by quoting its seed-minimum (0.722) instead of its mean (0.725, tied with raw)
**Verdict:** confirmed  
Both prongs verified against the actual source files.

PRONG 1 — zero variance / deterministic gap. In /home/robert/FAMAIL/PAPER/data/L1v2_4feat_multiseed.json: per_source.raw.f_causal has values = [0.7252857685]x5, std 0.0, t_ci [0.7252857685, 0.7252857685]; per_source.edited.f_causal has values = [0.7409303784]x5, std 0.0, t_ci collapsed to a point. These are deterministic data-level metrics (the edit and the raw corpus do not depend on the BC training seed), so the headline 'edited > raw' gap (+0.0156446) has NO sampling variance and NO paired test or CI is computable on it. It is a single-point comparison, yet it is presented in the same per-source table next to bc (std 0.00213) and gan (std 0.00545), which DO vary by seed — so the apparatus implies cross-source comparability that does not exist for the edited-vs-raw contrast. Confirmed.

PRONG 2 — bc misstated as 0.722. Recomputed from the JSON's bc.f_causal values [0.7223271,0.7276688,0.7267473,0.7263064,0.7230407]: mean = 0.7252181 (rounds to 0.725), min = 0.7223271 (rounds to 0.722). raw mean = 0.7252858. So bc's MEAN is statistically tied with raw (difference −0.0000677; raw is fractionally higher). README.md line 19 states 'edited 0.741 vs raw 0.725, bc 0.722' — the 0.741 and 0.725 are means but 'bc 0.722' is the seed MINIMUM, not the mean. tables/comparison_3v4.md line 18 reports 'bc 0.7223' (also the min) in a row whose stated conclusion is 'edited 0.7409 > raw 0.7253 > bc 0.7223'. Using the min makes bc appear ~0.003 worse than raw when, by the mean, bc and raw are tied. tables/fcausal_feature_selection.md also contains a 0.722* string and should be checked for the same substitution. The figure-generating code (paper_figures.py) reads the JSON means, so figures are unaffected; the error is confined to prose/tables.

Net: the qualitative ordering 'edited is the fairest faithful source' survives (edited 0.7409 exceeds raw, bc, and the disqualified gan on the point estimates), so this is not fatal to the headline — hence important, not critical. But the statistical-comparability framing and the specific bc=0.722 number are both wrong as written.  
**Fix:** 1) README.md line 19: change 'bc 0.722' to 'bc 0.725 (mean; tied with raw)' — or quote it consistently as a mean. Optionally note bc range [0.722, 0.728]. 2) tables/comparison_3v4.md line 18: change 'bc 0.7223' to the bc mean 0.7252 and soften 'raw 0.7253 > bc 0.7223' to 'raw ≈ bc (0.7253 vs 0.7252, tied)'; the 3-feature cell ('bc 0.8045') should be re-checked the same way (mean vs min). 3) Check tables/fcausal_feature_selection.md for the same min-vs-mean substitution. 4) For the deterministic gap: explicitly label raw and edited f_causal as deterministic data-level metrics (std=0.0 by construction, single-point t_ci) so readers do not mistake the collapsed CI for a measured zero-width interval, and state that the edited−raw gap is a point comparison (no paired test applies) while only bc/gan carry seed variance. This is a labeling/framing fix, not a missing test — a deterministic quantity genuinely has no sampling distribution.

## [A] IMPORTANT — fig_dose_response: significant 'Most-fair selection' markers at w=10 and w=30 are visually occluded by the 'Random placebo' n.s. labels, making SELECT read as null
**Verdict:** confirmed  
I verified every claimed number against the real source and reproduced the figure from PAPER/data.

DATA INTEGRITY: PAPER/data/weighted_bc_4feat_sweep.json is byte-for-byte identical (sorted-JSON diff) to the code's read path famail_temporal/results/weighted_bc_sweep/cleaned_4feat_6seed/sweep.json. So the figure committed at PAPER/figures/fig_dose_response.png is driven by the cited data.

NUMBERS (from PAPER/data/weighted_bc_4feat_sweep.json, paired_vs_raw.f_causal), recomputed through the exact code path (_p_stars at lines 145-153, _ci_halfwidth, annotation y = mean + ci_half + 0.0012 at lines 226-230):
- most_fair_w10: mean=+0.00201, wilcoxon_p=0.03125 -> star '*' ; annot_y=0.00477
- most_fair_w30: mean=+0.00220, wilcoxon_p=0.03125 -> star '*' ; annot_y=0.00460
- most_fair_w20: mean=+0.00026, p=1.0 -> 'n.s.' (correctly shown), annot_y=0.00313
- random_w10:  p=0.84375 -> 'n.s.' ; annot_y=0.00533
- random_w30:  p=0.3125  -> 'n.s.' ; annot_y=0.00508
All match the EVIDENCE block exactly (claimed +0.00201/p0.03125, +0.00220/p0.03125, +0.00026/p1.0, etc.).

OCCLUSION MECHANISM CONFIRMED: At x=10 the orange most_fair '*' (annot_y 0.00477) sits 0.00056 below the grey random 'n.s.' (annot_y 0.00533) at the SAME x. At x=30 the gap is 0.00048, same x. Both labels are fontsize=9 and ha='center'/va='bottom'. The series loop (lines 217-230) draws in order edited -> most_fair -> random with NO collision avoidance, so the LARGER, LATER grey 'n.s.' string is painted directly over the small orange '*'. I rendered the figure from the committed PAPER data and zoomed the low-point band: at x=10 and x=30 a prominent grey 'n.s.' dominates, with only a tiny orange asterisk peeking out from under the 'n.s.' text. At x=20 (where random has no point) the orange 'n.s.' is clean and correctly readable. A reader naturally reads the orange selection markers at w=10/w=30 as non-significant -- the opposite of the truth (they are p=0.03125, significant).

The finding's hypothesis ('selection points look n.s.') is confirmed in EFFECT. I note the precise mechanism is rendering occlusion / z-order + font size, NOT a logic error in _p_stars (which correctly returns '*'). The prompt acknowledges this same distinction.

IMPACT: README.md line 24-27 explicitly claims 'EDIT >> SELECT > RANDOM', with SELECT (+0.002) significant and beating the random null. The headline figure visually contradicts its own paper's claim by making SELECT appear null. This is a real honesty/clarity defect on the headline figure, not a fabricated or numerically-wrong result. Severity downgraded from 'critical' to 'important': the underlying numbers, p-values, and code logic are all correct; the data file and figure are consistent; the defect is a fixable annotation-placement/legibility problem on one figure, and the correct values are reported in text/tables. It does not invalidate any result, but it can mislead a reader of the headline figure, so it should be fixed before submission.  
**Fix:** In famail_temporal/analysis/paper_figures.py fig_dose_response (lines ~217-230), add collision avoidance so co-located significance labels at the same x don't overlap. Concrete options (any one fixes it):

1) Vertical separation by series: give each series a distinct vertical sign-marker offset, e.g. add a per-series base offset to `off` (edited above, most_fair above with a larger gap, random BELOW its point with va='top'). Since random is always n.s. and its point sits between/around the orange point, placing random's 'n.s.' below the marker (xytext negative, va='top') removes the overlap entirely at x=10 and x=30.

2) Horizontal nudge: offset annotations by series, e.g. ha based on series ('right' for most_fair, 'left' for random) or a small dx in points via textcoords='offset points' (e.g. most_fair xytext=(-6,2), random xytext=(+6,2)).

3) Draw order / prominence: draw the significant markers LAST (after random) and/or render '*'/'**' larger (fontsize 12, bold) than the 'n.s.' (fontsize 8), in the series color, so a real effect is never hidden under a null label.

Recommended: combine (1) for random ('n.s.' below its point, va='top') with (3) larger bold colored stars. After the change, re-run `python -m famail_temporal.analysis.paper_figures` and re-zoom x=10/x=30 to confirm the orange '*' is fully visible and not under any grey 'n.s.'. Then regenerate PAPER/figures/fig_dose_response.png from the (identical) PAPER/data source.

## [A] IMPORTANT — fig_feature_robustness presents a null, sign-flipping ΔF_causal row under an "every conclusion holds" banner with no error bars
**Verdict:** confirmed  
Every claimed number checks out against the source JSONs and the rendered PNG.

NUMBERS (verified):
- /home/robert/FAMAIL/PAPER/data/variance_4feat_aggregate.json, paired_delta.f_causal = {mean +0.00059958, std 0.00102256, min -0.00074178, max +0.00168979, n 5} — matches the finding exactly. CI95 straddles zero (t=1.31, SE 0.00046, CI [-0.00030, +0.00150]).
- 3-feature source /home/robert/FAMAIL/famail_temporal/results/variance_suite/cleaned_5seed/aggregate.json, paired_delta.f_causal = {mean -0.00037047, std 0.00143544, min -0.00234604, max +0.00160569, n 5} — null (t=-0.58, CI [-0.00163, +0.00089]).
- So the row's mean SIGN FLIPS between feature sets (-0.0004 -> +0.0006) and BOTH values are statistically null (mean < std; both 95% CIs straddle zero, n=5). Consistent with the project's own documented "model-level/editor-level F_causal NULL" finding.

FIGURE: /home/robert/FAMAIL/PAPER/figures/fig_feature_robustness.png renders the top row "Editor ΔF_causal (variance suite)" as -0.0004 (3feat) / +0.0006 (4feat) with a connecting dumbbell, bare scatter points, NO error bars/CIs/significance markers, under the title "Demographic robustness: scale shifts, every conclusion holds." Confirmed in code at /home/robert/FAMAIL/famail_temporal/analysis/paper_figures.py: fig_feature_robustness (lines 478-524) uses ax.scatter (504-507) with no errorbar/yerr/xerr and annotates "+.4f" point values (508-511); title text at 516-519. So a reader cannot see this row is null and sign-unstable. Finding confirmed.

COMPOUNDING ISSUE (makes it worse, not better): the plotted quantity is bundle.var["paired_delta"]["f_causal"]["mean"] (line 463), which is the variance-suite MODEL-LEVEL delta (b0 vs FAMAIL model). The companion table /home/robert/FAMAIL/PAPER/tables/comparison_3v4.md labels this exact quantity "variance model-level (b0 vs FAMAIL) ... Δ -0.0004, null / Δ +0.0006, null -> model-level within noise" and elsewhere "the model-level null." So (a) the figure MISLABELS a model-level null as "Editor ΔF_causal," and (b) it strips the null disclosure the table makes twice. The actual editor before->after delta is +0.0124/+0.0156 (a different, non-null quantity shown on the figure's SECOND row, "L1 edited-raw gap"). Net: the figure's headline row is both mislabeled and presented as a confirmed conclusion when the source artifacts call it null.

MITIGATION (why important, not critical): the underlying table comparison_3v4.md is HONEST — it explicitly tags this row "null" and "within noise" in two places, and no quantitative claim in the figure is fabricated (the +0.0124/+0.0156 editor gap, L1, weighted-BC w30, L2 rows are all real and correctly signed). The defect is confined to figure presentation/labeling: a null row dressed as a conclusion plus a label that overstates it (Editor vs model-level). It does not invalidate the paper's substantive conclusions, but it is a genuine figure-honesty overclaim a reviewer would flag.  
**Fix:** Three concrete fixes in /home/robert/FAMAIL/famail_temporal/analysis/paper_figures.py:

1) Add error bars / CIs. Each aggregate JSON carries std and n=5; plot ax.errorbar(..., xerr=1.96*std/sqrt(n)) (or per-seed scatter) for at least the editor/variance and L2 rows so the reader sees the CI straddling zero. Pull std from bundle.var["paired_delta"]["f_causal"]["std"] (and analogously for L2).

2) Fix the mislabel. The top row plots the variance-suite MODEL-LEVEL paired delta (b0 vs FAMAIL), not the editor before->after delta. Rename the row from "Editor ΔF_causal (variance suite)" to "Model-level ΔF_causal (variance suite, b0 vs FAMAIL)" to match comparison_3v4.md, which calls it the "model-level null." (The real editor delta is +0.0124/+0.0156.)

3) Soften the banner. Mark null rows explicitly (e.g., shade the |Δ|<2·SE band, or annotate "n.s." on the model-level and L2 rows) and change the title from "every conclusion holds" to something like "directional conclusions hold; model-level and vanilla-BC rows are null (within noise)." This matches the honest framing already present in PAPER/tables/comparison_3v4.md and avoids overclaiming a null as a confirmed result. The README rows (PAPER/README.md lines 41, 52: "every conclusion holds") should be reworded the same way.

## [A] IMPORTANT — "Preserves shape" framing in fig_fidb_components is an overclaim; edited coverage/RoG are mid-range and net_disp exceeds GAN
**Verdict:** confirmed  
Every claimed number checks out against PAPER/data/L1v2_4feat_multiseed.json (per_source.edited.fidelity_b_per_component means): length 0.01128, mean_displacement 0.04267, coverage 0.09270, radius_of_gyration 0.10163, net_displacement 0.12706, terminal_cell 0.54825 (overall Fid-B 0.15393). GAN-gen means: length 0.24738, mean_displacement 0.04522, coverage 0.29856, radius_of_gyration 0.22037, net_displacement 0.11922, terminal_cell 0.10452.

The two contested factual sub-claims hold: (1) edited net_displacement 0.12706 EXCEEDS GAN net_displacement 0.11922 (+0.0078). (2) edited RoG 0.10163 is ~46% of GAN RoG 0.22037 ("~half"). The docstring (paper_figures.py lines 401-404) asserts editing "preserves trajectory shape (low length/coverage/RoG)" — but only length (0.011) is genuinely low; coverage (0.093) and RoG (0.102) are mid-range, both larger than mean_displacement (0.043), and RoG is the third-largest of the six edited components. So "low coverage/RoG" is contradicted by the file, and the README line 40 "preserves shape" parenthetical is an overclaim on those components.

Two caveats that bound the severity to important rather than critical: (a) The figure's own TITLE already states the honest, defensible message — "Editing concentrates its distributional shift in terminal-cell (relocated pickups)" — and terminal_cell (0.548) genuinely dwarfs every other component (next largest is net_disp at 0.127, ~4.3x smaller), so the core narrative (shift concentrated in terminal_cell) is sound; only the secondary "preserves shape" prose overreaches. (b) The GAN coverage/RoG/length means are inflated by high cross-seed variance (std 0.25/0.17/0.22, driven by ~2 collapsed seeds), so the edited-vs-GAN visual parity on net_disp/RoG is on noisy GAN bars — but the figure plots exactly these means, so the comparison the reader sees is the one the finding describes. Net: the "preserves shape" claim is an overclaim; the narrower "concentrated in terminal_cell, not shape-preserving on other stats" is the accurate statement.  
**Fix:** Align the prose with the figure title (which is already honest). (1) In famail_temporal/analysis/paper_figures.py docstring lines 401-404, replace "preserves trajectory shape (low length/coverage/RoG)" with something like: "concentrates its distributional shift in terminal_cell (0.548); the other components are markedly smaller (next-largest net_disp 0.127, ~4x smaller), though only length (0.011) is truly low — coverage (0.093) and RoG (0.102) are mid-range and comparable to GAN's net_disp/RoG." (2) In PAPER/README.md line 40, change the figures-table description from "relocates pickups, preserves shape" to "relocates pickups; distributional shift concentrated in terminal_cell" (drop the "preserves shape" claim). Optionally add a one-line caption note that on net_displacement the edited shift slightly exceeds the GAN mean, so the claim is concentration-in-terminal_cell, not shape preservation per se.

## [A] IMPORTANT — fig_l1_data_quality right panel: truncated Fidelity-A y-axis [0.80,0.86] visually exaggerates a 0.0056 gap, making edited look least faithful
**Verdict:** confirmed  
Every claimed fact verifies against the real sources. paper_figures.py:314 is literally axR.set_ylim(0.80, 0.86). The Fidelity-A means in PAPER/data/L1v2_4feat_multiseed.json are raw 0.8485, edited 0.8429, bc 0.8479, gan 0.8482 — a total spread of 0.0056 on a 0–1 metric. The rendered PAPER/figures/fig_l1_data_quality.png shows the edited (blue) bar visibly stopping short of the other three, reading as the least identity-faithful source.

Quantified distortion: on the [0.80,0.86] axis (range 0.06), raw fills 80.8% of the cell while edited fills 71.5%, a visible-height ratio of 0.885 (edited appears ~11.5% shorter). The true ratio is 0.9934 (only 0.66% shorter). The truncation amplifies the perceived difference by roughly 17x. No truncation is disclosed: no broken-axis marker, and the README caption (line 38) says nothing about the zoomed axis.

This is a genuine, classic truncated-bar distortion and it works directly against the figure's own thesis (suptitle: 'edited data is the fairest identity-faithful source'), since edited is the very bar made to look worst. Aggravating: the left panel intentionally uses a zero baseline (set_ylim(0, ...)), so the two panels are inconsistent and a reader scanning both is primed to read the right-panel heights as comparable to the zero-based left panel.

I mark severity 'important' rather than 'critical': the underlying numbers and per-bar values are not falsified, and Fidelity-B annotations plus error bars are present, but an uninformed reader of the figure alone is genuinely misled about the identity-faithfulness ordering, which is a load-bearing claim of the paper.  
**Fix:** In famail_temporal/analysis/paper_figures.py, fix the right-panel axis so the visual matches the 0.0056 spread honestly. Options, in order of preference: (1) Use a zero baseline like the left panel: replace line 314 `axR.set_ylim(0.80, 0.86)` with `axR.set_ylim(0, 0.95)` (or 0,1) so all four Fidelity-A bars correctly read as near-identical (~0.85), truthfully supporting 'edited is identity-faithful'. Move the Fid-B annotations' y offset (line 317, currently 0.806) accordingly. (2) If a zoom is genuinely needed to show the tiny differences, keep a tighter window but (a) add an explicit broken-axis marker, and (b) state the truncation in the README caption and figure, e.g. 'y-axis zoomed to [0.80,0.86]; all sources within 0.006 of each other.' Given the thesis is that edited is faithful (not that the sub-0.006 differences matter), option (1) is strongly recommended. Regenerate fig_l1_data_quality.png after the change.

## [A] IMPORTANT — F_causal reduction is entirely LogPopDensity; MigrantRatio is a decorative passenger
**Verdict:** confirmed  
All load-bearing numbers in the finding reproduce exactly from PAPER/data/fcausal_feature_selection.json (recomputed, not just read).

(1) Identity of metrics with vs without migrant: set h-g-c-logpop (housing, GDP, comp, logpop — NO migrant) F_causal = 0.725318; shipped set h-c-migrant-logpop (housing, comp, migrant, logpop) F_causal = 0.725286. |diff| = 3.25e-5. The shipped config is confirmed as the one in PAPER/README.md line 73 (feat-housing-comp-migrantratio-logpopdensity). Swapping GDP out for MigrantRatio moves F_causal by ~0; the metric is unchanged.

(2) Attribution of the drop: base3 F_causal = 0.806928; shipped before-edit = 0.725286; total reduction = 0.081642. The single-feature marginal of +LogPopDensity on base3 is dF = -0.081610 — i.e. ~99.96% of the entire reduction is LogPopDensity. MigrantRatio's single-feature marginal is only -0.034672.

(3) MigrantRatio rank: sorting all 12 candidate single-feature marginals, MigrantRatio is 10th of 12 (dF -0.0347), beaten only by SexRatio100 (-0.0156) and the degenerate LogHousingPrice (-0.0257, VIF 14084). Exactly as the finding states ('worst non-degenerate single feature except SexRatio/LogHousing').

(4) 'Buys nothing on top of {h,g,c,logpop}': h-g-c-logpop F_causal 0.725318 (maxVIF 2.87, ROBUST-AND-BETTER) vs h-g-c-migrant-logpop F_causal 0.725116 (delta -0.0002) but maxVIF explodes to 24.25 (HIGH-VIF/UNSTABLE, MigrantRatio VIF 24.25). Adding migrant adds no fairness signal and breaks the VIF policy — confirming 'a near-zero passenger swapped in at a VIF cost.'

(5) Overclaim located in the prose: PAPER/tables/comparison_3v4.md line 8 sells four 'low-co-linearity axes (SES / income / population-structure / density)'; line 27 says the 4-feature metric 'captures demographic-driven inequity (density + population structure) that the 3-feature set missed.' The data show the entire gain is the density axis (LogPopDensity); the population-structure axis contributes ~0 incrementally. The 'population structure' clause is decorative.

Two precisions where the finding slightly overstates: (a) In the SHIPPED set h-c-migrant-logpop the max VIF is 4.51 (acceptable, < the <10 policy) because GDP was dropped — migrant only incurs the high-VIF cost when added on top of the full {h,g,c,logpop} set. So 'at a VIF cost' is true for the +migrant-on-top counterfactual, not for the shipped config itself. (b) The selection was a defensible 4-axis design choice (one feature per axis with acceptable per-set VIF), not a numerical error — the editor-targeting Jaccard 0.93 and all downstream conclusions are genuinely robust to the choice, as comparison_3v4 claims. So this is a framing/interpretation defect, not a result-invalidating bug. Hence 'important', not 'critical': it weakens a defensibility claim a reviewer will probe ('why migrant? it does nothing') but does not change any L1/L2/weighted-BC conclusion.  
**Fix:** Soften the four-axis framing in PAPER/tables/comparison_3v4.md (and any paper text mirroring it) to attribute the F_causal drop to the density axis, not population structure. Concretely: (1) Line 27: change 'demographic-driven inequity (density + population structure) that the 3-feature set missed' to 'demographic-driven inequity (chiefly population density) that the 3-feature set missed; the population-structure feature (MigrantRatio) is retained for axis coverage but contributes negligible incremental F_causal signal (single-feature dF -0.035; on top of {housing,GDP,comp,logpop} dF -0.0002).' (2) Either (a) keep migrant but state honestly in the appendix that the dominance over base-3 is driven by LogPopDensity, not migrant, and that migrant is included to span a population-structure axis at acceptable per-set VIF (4.51); OR (b) consider shipping h-g-c-logpop instead (identical F_causal 0.725318, maxVIF 2.87 vs 4.51, retains GDP income signal, ROBUST-AND-BETTER) and dropping the migrant axis claim entirely, which removes the weakest reviewer target. Option (b) gives a cleaner, lower-VIF set with the same metric. (3) In the appendix sensitivity table, add the marginal-contribution ranking so the reader can see LogPopDensity (dF -0.0816) is the sole driver and MigrantRatio (dF -0.0347, rank 10/12) is not.

## [A] IMPORTANT — Shipped feature set h-c-migrant-logpop is co-dominant with (not strictly dominated by) h-g-c-logpop, which is preferable on the paper's own stated VIF/interpretability criteria
**Verdict:** partial  
Every cited number reproduces from the real source `PAPER/data/fcausal_feature_selection.json` (`set_search`):
- h-g-c-logpop: F_causal=0.725318193, max_vif=2.8668356, topk_jaccard=0.9228512, spearman_alpha=0.8352877, features {housing, GDP, comp, logpop}, dominates_base3=True, verdict ROBUST-AND-BETTER.
- h-c-migrant-logpop (SHIPPED, = `DEMOGRAPHIC_FEATURES` in `famail_temporal/config.py:54-59`): F_causal=0.725285709, max_vif=4.5056975, topk_jaccard=0.9317607, spearman_alpha=0.8741662, features {housing, comp, migrant, logpop}, dominates_base3=True, verdict ROBUST-AND-BETTER.

Confirmed sub-claims:
- F_causal is an effective tie: difference = 3.25e-5, ~300x below the code's own MATERIAL_DELTA of 0.01 (`fcausal_feature_sensitivity.py:861`).
- Shipped set has 57% higher max VIF (4.506/2.867 = 1.57x).
- Shipped set drops GDPperCapita (a standard interpretable income axis) for MigrantRatio, which `config.py:51` itself calls "an inverse-income proxy" and notes is r=-0.90 collinear with GDP.
- Both Spearman values (0.835, 0.874) are below the sensitivity module's ROBUST threshold SPEARMAN_ROBUST=0.90 (`fcausal_feature_sensitivity.py:467`). The shipped set's only win is Spearman 0.874 vs 0.835, both sub-0.90.
- The paper's defensibility narrative (`PAPER/tables/comparison_3v4.md`, `PAPER/README.md`) makes a "dominance" argument against base-3 but NEVER mentions h-g-c-logpop as the rejected runner-up, nor justifies preferring migrant over GDP+logpop. The implicit driver is `_migrant_axis_winner` (`fcausal_feature_sensitivity.py:931-949`), an axis-diversity heuristic that auto-prefers any dominating set containing a `pop_structure` axis — not the VIF/interpretability criteria the paper foregrounds.

Why PARTIAL, not CONFIRMED: the finding overstates the relationship as "strictly-better dominated." On the project's own Pareto test (`_pareto_and_verdicts`, lines 901-918), which uses exactly two objectives (lower F_causal, lower max_vif), h-g-c-logpop does NOT strictly dominate the shipped set, because the shipped set has marginally LOWER F_causal (0.7252857 < 0.7253182). That 3.25e-5 edge is why BOTH appear on `pareto_frontier_sets: ['baseline_h-g-c','h-g-c-logpop','h-c-migrant-logpop']`. So no formal Pareto domination exists in either direction; they are co-dominant (mutually non-dominated) on the literal coded objectives. The finding's word "dominated" is therefore technically wrong, and its phrase "F_causal tie (3e-5)" actually understates that the tie tilts (negligibly) toward the shipped set, not the alternative.

But the SUBSTANTIVE thrust holds and is important: at an effective F_causal tie, the chosen set is strictly worse on the paper's own foregrounded tie-breakers (max VIF, and interpretability/non-redundancy of the income axis), and the paper's "dominance" framing against base-3 does boomerang — a reader applying the same lower-VIF + interpretability logic the paper uses to reject base-3 would pick h-g-c-logpop over the shipped set. The only defense (axis diversity: a population-structure axis is desirable in its own right) is real and is what the code actually used, but it is never stated as the selection rationale in the paper, so the choice currently reads as under-justified and vulnerable to exactly this reviewer objection.  
**Fix:** Either (a) switch the shipped set to h-g-c-logpop (lower VIF 2.87, retains GDP, identical F_causal, all four-axis conclusions in comparison_3v4.md reproduce because Jaccard 0.92 ≈ 0.93 — targeting is the same), or (b) keep h-c-migrant-logpop but add one explicit sentence to PAPER/tables/comparison_3v4.md and the config comment justifying the choice on axis-diversity grounds: state that h-g-c-logpop ties on F_causal at lower VIF, that the two are co-dominant on the (F_causal, VIF) frontier, and that h-c-migrant-logpop was preferred specifically to include an independent population-structure axis (MigrantRatio) rather than two correlated income features — making the tradeoff (interpretability/VIF vs. axis coverage) explicit rather than implicit in _migrant_axis_winner. Also stop using the unqualified word "dominated" for a frontier that contains three mutually non-dominated sets; say "Pareto-improves on / co-dominant with base-3." Do not claim h-g-c-logpop strictly dominates the shipped set in any rebuttal — it does not (shipped F_causal is 3e-5 lower); the correct claim is co-dominance plus a VIF/interpretability preference.

## [A] IMPORTANT — Feature set partly selected by minimizing before-edit F_causal (the scored metric), but axis/VIF constraints — not pure F_causal-minimization — drove the final pick
**Verdict:** partial  
I verified every claimed number against source.

CONFIRMED parts of the finding:
1. The selection criterion is explicitly before-edit F_causal minimization. fcausal_feature_sensitivity.py line 770 ("negative = lowers F_causal = captures more unfairness"), line 793 (delta_f_causal = f_causal - base3), line 798 ("Rank by how much it LOWERS F_causal"), and the verdict policy lines 850-859/877/881 (`captures_more = delta < -MATERIAL_DELTA` is the gate for "ROBUST-AND-BETTER"). The markdown header (line 1041) and comparison_3v4.md line 8/26 assert "lower F_causal = more demographic-driven unfairness captured" as the selection rationale. So the editor optimizes F_causal and the analyst selected the feature lens partly by minimizing the same before-edit F_causal — the circularity the reviewer describes is structurally present.
2. The mechanism the reviewer fears is real and measurable: the lower-before-edit-F_causal set produces the LARGER reported editor delta (4-feat 0.7253->0.7409 = +0.0156 vs 3-feat 0.8069->0.8193 = +0.0124, both from PAPER/tables/comparison_3v4.md line 17). Choosing on low before-edit F_causal does co-select a bigger headline improvement.
3. CONFIRMED: there is no out-of-sample or external/ground-truth validation that the chosen demographic axes are the true drivers of demand inequity. Justification is internal only (VIF<10, four low-collinearity axes, top-K targeting Jaccard/Spearman stability).

REFUTED parts (why this is partial, not confirmed):
1. F_causal was NOT the sole or decisive criterion, and the adopted set is NOT the F_causal-minimizing one. Config (config.DEMOGRAPHIC_FEATURES) = {AvgHousingPricePerSqM, CompPerCapita, MigrantRatio, LogPopDensity} = set 'h-c-migrant-logpop', F_causal=0.7253, maxVIF=4.51. The selection JSON (fcausal_feature_selection.json) shows multiple sets with LOWER or EQUAL before-edit F_causal that were rejected: h-g-c-migrant-logpop F=0.7251 (rejected, VIF 24.25), and crucially h-g-c-logpop F=0.7253 — a TIE on F_causal at strictly LOWER VIF (2.87 vs 4.51) and equal targeting. The marginal table also shows EmployeeCompensation (F=0.7167) and AvgEmployedPersons (F=0.7223) beating the adopted LogPopDensity (F=0.7253) on raw F_causal. If the analyst were pure-p-hacking, they would have taken the lowest-F_causal feasible set; instead they swapped GDP->Migrant relative to the F_causal-tied h-g-c-logpop, ACCEPTING no F_causal gain and worse VIF, purely to add the population-structure axis (adopted axes = income/density/housing/pop_structure vs h-g-c-logpop = income/density/housing). That is axis-coverage reasoning, the opposite of headline-delta-maximization.
2. The reviewer's strongest line ("you chose the metric that makes your baseline worst") overstates: among the feasible (VIF<10, <=5-feat, stable-targeting) sets, the adopted set is tied-best, not uniquely-worst-baseline, and a strictly-lower-VIF tie was passed over.

So: the circular-selection structure and lack of external demographic validation are real and worth a reviewer rebuttal, but the specific accusation that the analyst maximized apparent baseline unfairness / the editor's headline delta is not what the numbers show — they left F_causal improvement on the table for a defensibility (axis-coverage/VIF) reason. Severity is important (a hostile reviewer can still raise the circularity and the missing ground-truth validation), not critical (the conclusion is robust to the feature choice: targeting Jaccard 0.93, Spearman 0.87, and all qualitative conclusions reproduce across both feature sets per comparison_3v4.md lines 17-23; the editor delta sign and significance hold under the original 3-feature set too).  
**Fix:** Reframe the appendix selection narrative away from "lower before-edit F_causal = better" toward the defensibility criteria that actually drove the pick: (a) four orthogonal demographic axes (SES/income/density/population-structure), (b) VIF<10 / DOF policy, (c) editor-targeting stability (Jaccard 0.93, Spearman 0.87). State explicitly that F_causal magnitude is a feature-set-specific descriptive scale, NOT the optimization target for selection — and back this with the fact already in the data: the adopted set was NOT the F_causal-minimizing feasible set (h-g-c-logpop ties it at lower VIF; EmployeeCompensation/AvgEmployedPersons score lower), so it was chosen for axis coverage, not to minimize the baseline. Pre-empt the circularity charge by (1) reporting that the editor delta is positive and significant under the ORIGINAL 3-feature set too (+0.0124), so the headline does not depend on a hand-picked low-baseline lens, and (2) emphasizing targeting robustness (the editor flags essentially the same trajectories under either set). To close the 'true drivers of demand inequity' gap, either cite an external/literature justification for the four axes or add a held-out check (e.g. that the axis ranking is stable on a data split), rather than letting F_causal-rank stand as the sole justification.

## [A] IMPORTANT — LogPopDensity is a demand-geography variable, not a protected/demographic attribute, yet it single-handedly drives the F_causal metric change
**Verdict:** confirmed  
Both legs of the finding check out against the real sources.

(1) Label + magnitude (CONFIRMED exactly). FEATURE_AXIS labels LogPopDensity "density" at fcausal_feature_sensitivity.py:681. The adopted headline set is {AvgHousingPricePerSqM, CompPerCapita, MigrantRatio, LogPopDensity} (config.py:54-59, committed 7fb1fb2; PAPER/README.md:12). From PAPER/data/fcausal_feature_selection.json: base3 F_causal=0.8069; the adopted set h-c-migrant-logpop=0.7253 (ΔF=-0.0816). Decomposing within the adopted set: h-c-migrant (housing,comp,migrant, NO density)=0.7988 → ΔF only -0.0081; adding LogPopDensity drops it to 0.7253, i.e. density alone supplies -0.0735, ~90% of the move. Independently, h-g-c-logpop (housing,income,density, NO migrant)=0.7253 reproduces the SAME 0.7253 — so the entire headline metric change is carried by the density axis, and the genuine population-structure axis (MigrantRatio) is nearly inert. The finding's "LogPopDensity carries the whole dF (-0.0816)" is literally correct.

(2) Construct validity (CONFIRMED). Y=S/D is confirmed at preprocess.py:194 (supply already normalized by demand). Adding a density regressor to H_demo therefore residualizes service against where people simply live — a demand-scale/geography variable, not a protected/SES attribute. The selection routine (_marginal_contribution_table, _set_search_table) ranks features purely by ΔF_causal magnitude, VIF<10, and top-K Jaccard targeting stability — there is NO equity/protected-status criterion anywhere. Yet the narrative sells the four axes as "SES / income / population-structure / density" demographic fairness axes (comparison_3v4.md:8; config.py:44-48; PAPER/README.md:12-13) with no argument that density is equity-relevant. Worse, comparison_3v4.md:27 attributes the metric gain to "density + population structure," but the decomposition above shows population-structure (migrant) adds almost nothing — the gain is ~all density, so that framing overstates the demographic content.

Severity is "important," not "critical," because (a) the paper does flag F_causal as feature-set-specific, and (b) per comparison_3v4.md the qualitative conclusions (L1 edited fairest, L2 vanilla null, weighted-BC recovery, model-level null) hold under BOTH the 3-feature (density-free) and 4-feature metrics, with editor targeting Jaccard 0.93. So density does not flip any conclusion — it inflates the absolute F_causal scale and underpins the "we capture more demographic-driven inequity" claim, which is the least demographically-defensible part of the headline. A reviewer can plausibly argue the editor is partly chasing low-density (sparse-demand) cells rather than poor/underserved demographic groups.  
**Fix:** Do one of: (a) Drop density from the headline F_causal feature set and lead with a defensibly-demographic set. The cleanest in-data option is h-g-c-nonreg {housing, GDP, comp, NonRegisteredPermanentPop10k}: it is ROBUST-AND-BETTER (ΔF=-0.0505, max VIF 5.84) and its added axis is genuine population structure, not demand geography. Or retain the 3-feature SES/income set as primary and demote the 4-feature set to a robustness appendix. (b) If density is kept, stop calling it a demographic/fairness axis: relabel it a demand-controlling covariate and explicitly state F_causal conditions on density to isolate residual demographic inequity, with an argument (citation) that density itself is equity-relevant in this context — otherwise drop the 'captures more demographic inequity' claim. (c) Add an honest decomposition to the paper showing density supplies ~90% of the ΔF (-0.0735 of -0.0816) while MigrantRatio supplies ~-0.0081, and correct comparison_3v4.md:27/line 8 which currently credit 'density + population structure' jointly. Regardless of choice, the load-bearing point that conclusions are invariant to the feature set (Jaccard 0.93, both metrics agree qualitatively) should be foregrounded so the headline does not rest on the density axis.

## [A] IMPORTANT — F_causal demographics have only 10 district-level DOF; VIF/correlation/projection computed on N-broadcast inflate effective sample size (ecological-fallacy exposure)
**Verdict:** confirmed  
Every cited fact checks out against the actual sources, and the core mechanism is worse/more concrete than the narrative admits.

CITATIONS VERIFIED:
- config.py line 53 (not 52): "Capped at 4 features given only 10 district-level demographic profiles (DOF limit)." Confirmed verbatim.
- fcausal_feature_sensitivity.py line 643: "LOW co-linearity (VIF < 10), subject to ≤5 features (only 10 districts of DOF)." Line 654: "MAX_FEATURES: int = 5  # only 10 districts → keep DOF sane." Confirmed.
- The correlation matrix in PAPER/tables/fcausal_feature_selection.md §2 reports MigrantRatio×GDPperCapita r=-0.90 and PopDensityPerKm2×LogPopDensity r=+0.91, plus a 15×15 pairwise table — all presented with no caveat that they derive from 10 distinct points. Confirmed.

GROUND TRUTH (I read the pkl directly): famail_temporal/source_data/cell_demographics.pkl has demographics_grid shape (48,90,13), 2605 finite cells, district_names = exactly 10 Shenzhen districts (Bao'an, Dapeng, Futian, Guangming, Longgang, Longhua, Luohu, Nanshan, Pingshan, Yantian). np.unique over the finite cell rows returns exactly 10 distinct profiles. cell_to_demographics maps 2605 cells onto these 10 vectors.

MECHANISM CONFIRMED: _build_inputs() (lines 159-176) constructs unit_demo_all as (N, n_feats) by unit_demo_all[i] = demo_flat[flat_cell] — i.e. each of the ~34,524 active units inherits one of 10 district vectors. _vifs (line 199), _pairwise_corr (line 722), and _max_abs_offdiag_corr (line 733) are all called on this broadcast matrix (n=N rows), and _compute_subset passes demographic_features=demo_subset into the F_causal hat-projection. So VIF, every Pearson r, AND the per-cell F_causal attribution rest on a design whose true row-rank is 10.

The 'inflated N' claim is demonstrable: I recomputed Housing×PopDensity Pearson r two ways — broadcast over the 2605 finite cells = -0.751, but over the 10 unique district rows = -0.796. They differ. The broadcast version is a cell-count-weighted correlation masquerading as an N-sample correlation; the reported matrix is the broadcast one, not the 10-row one. With only 10 points, an r of ±0.90 has a 95% CI roughly (−0.98, −0.63) — wide enough that the 'r=-0.90 ⇒ drop GDPperCapita' decision (config line 51) is statistically fragile. The VIF<10 ceiling, max-VIF table, and ROBUST/UNSTABLE verdicts inherit the same 10-DOF fragility.

WHY NOT CRITICAL: the finding does not invalidate any result; it is a defensibility/methodology exposure. The team is partially aware — the 4-feature cap is explicitly DOF-driven (config line 53). The substantive editor outputs (Jaccard 0.93 targeting overlap, F_causal direction) are robust to feature-set choice per the table. The unsurfaced deeper point is (a) covariate RESOLUTION is 10, not just the feature COUNT, and (b) cell-level F_causal attribution using district covariates is a textbook ecological-fallacy exposure a reviewer can name. Hence 'important', not 'critical'. Not 'minor' because a fairness paper attributing cell-level unfairness to demographics is squarely in ecological-fallacy territory and a KDD reviewer will flag it.

PARTIAL caveats on the finding's wording: (1) 'r estimated from 10 points but presented as if over N units' is exactly right, but note the broadcast r is also implicitly cell-weighted, so it is not even the clean 10-point r — a second subtlety. (2) the finding says 'n_active=34,524'; the attribution is over n_units active units, consistent. These refine rather than refute. Overall: confirmed.  
**Fix:** 1) In the paper narrative and in PAPER/tables/fcausal_feature_selection.md, state explicitly that demographics have only 10 distinct district-level profiles (effective covariate resolution = 10), and that VIF/correlation/F_causal projection are therefore limited to 10 effective DOF — not the ~34,524 active units. Add this as a stated limitation. 2) Recompute the §2 correlation matrix and §3 VIFs on the 10 unique district rows (optionally area- or cell-count-weighted, but disclosed as such) rather than the N-broadcast matrix, and report the difference (e.g. Housing×PopDensity r moves -0.751→-0.796); report confidence intervals or a caveat given n=10. 3) Reframe F_causal explicitly as 'demographic-driven unfairness attributed via district-level covariates,' acknowledging the ecological-fallacy exposure and noting that finer (cell-level) demographics would be required to attribute at cell resolution — this matches the existing 'limitation is data resolution, not the editor' fallback already in project memory (granularity exploration). 4) Soften the 'drop GDPperCapita because r=-0.90' justification (config.py line 51) to note the correlation is estimated from 10 district points and is therefore uncertain.

## [A] MINOR — Chosen 4-feature set's targeting-stability Spearman (0.835/0.874) is below the module's 0.90 ROBUST bar, and prose headlines cite only Jaccard 0.93
**Verdict:** partial  
All raw numbers check out against the actual sources. In famail_temporal/analysis/fcausal_feature_sensitivity.py: SPEARMAN_ROBUST=0.90 (line 467, used in _verdict) and SPEARMAN_OK=0.80 (line 863, used in _pareto_and_verdicts) both exist. From the real JSON (results/analysis/fcausal_feature_sensitivity/fcausal_feature_selection.json): chosen set h-c-migrant-logpop has spearman_alpha=0.874166 (verdict ROBUST-AND-BETTER, dominates_base3=True); twin h-g-c-logpop=0.835288 (also ROBUST-AND-BETTER, dominator). Both are below 0.90 and both clear the 0.80 gate. The disclosure asymmetry is real: comparison_3v4.md (lines 9-10, 31, 43), config.py (line 50), and PAPER/README.md (line 29) all headline 'editor targeting preserved (Jaccard 0.93)' and never mention that per-cell-unfairness rank correlation fell to 0.835/0.874.\n\nHowever, the finding's central framing — 'two thresholds for the SAME thing; the chosen set fails the module's own ROBUST bar' — materially overstates the problem, so this is partial, not confirmed:\n\n(1) The two thresholds judge DIFFERENT comparisons. SPEARMAN_ROBUST=0.90 lives in _verdict, which run_sensitivity() applies to CANDIDATE_SUBSETS that are leave-one-out DROPS of base-3 (drop_gdp/drop_comp/drop_housing, lines 100-102), with Spearman computed vs base-3's own alpha (line 431) — i.e. 'is base-3 robust to removing one of its own features.' SPEARMAN_OK=0.80 lives in _pareto_and_verdicts, which scores candidate sets including 4-feature SUPERSETS against base-3. A set that ADDS two new demographic axes (migrant, logpop) is expected by design to re-rank cells more than a single-feature drop, so the 0.90 bar is not the natural standard for it. Crucially, the 0.90 module never evaluates the 4-feature sets at all; the chosen set was never 'tested against and failed' the 0.90 bar.\n\n(2) The number is NOT hidden in the primary appendix. PAPER/tables/fcausal_feature_selection.md has a 'Spearman alpha' column showing 0.8353 and 0.8742 explicitly (lines 63-64, 84), with a header note that Spearman = per-cell targeting stability vs base-3. Only the prose summaries omit it.\n\nSo: numbers correct, disclosure asymmetry in prose/README genuine and worth fixing, but the 'internal contradiction / failed its own threshold' characterization is not supported. Net severity: minor — a transparency/wording gap, not a methodological error or an inflated claim, since the editor still flags 92-93% of the same top cells and the Spearman drop is the expected consequence of intentionally adding two demand-inequity axes.  
**Fix:** In comparison_3v4.md (lines 9-10, 31, 43), config.py (line 50), and PAPER/README.md (line 29), report BOTH targeting metrics, e.g. 'editor targeting preserved (top-2293 Jaccard 0.93; per-cell unfairness Spearman 0.87)', and add one sentence noting the Spearman drop below the leave-one-out 0.90 bar is expected: the 4-feature set intentionally adds two demand-inequity axes (migrant, log-density) that re-rank cells, which is a different operation than a leave-one-out feature drop. Optionally rename/comment SPEARMAN_ROBUST (0.90, leave-one-out drop sensitivity) vs SPEARMAN_OK (0.80, cross-set superset comparison) in fcausal_feature_sensitivity.py so the two bars are not mistaken for the same standard, and state the 0.80 cross-set rationale in the docstring of _pareto_and_verdicts.

## [A] MINOR — Cross-set F_causal 0.81→0.73 drop is narrated as "more inequity" without controlling for the mechanical R2-inflation component (but it is NOT pure overfitting)
**Verdict:** partial  
I reproduced the metric in-memory from cache/ and confirmed every cited number, then tested the mechanism the finding alleges.

VERIFIED FACTS (finding's premises hold):
- Both shipped values reproduce exactly: 3-feat before-edit F_causal=0.8069 (metrics_before.f_causal in results/2026-06-26T12-32-59_..._cleaned/metrics.json), 4-feat=0.7253 (results/2026-06-28T11-46-12_..._cleaned_4feat/metrics.json). My from-cache recompute: R2_3=0.1931→F=0.8069, R2_4=0.2747→F=0.7253.
- F_causal=1−R2_demo IS feature-set-specific and R2 is non-decreasing in regressors (hat_matrices.py / fairness/causal.py).
- The finding's "10 effective demographic rows" is LITERALLY TRUE: across the N=34,524 active units there are exactly 10 unique demographic rows (the demographic grid, though nominally 48×90, resolves to only 10 distinct profiles among active units). So the demographic design has resolution 10, not 34,524, and a real overfitting component exists.
- The paper DOES narrate the drop magnitude as substantive: comparison_3v4.md line 8 "captures more demographic-driven demand inequity (lower before-edit F_causal)" and lines 26-28 "scale dropped ... by design." It never benchmarks the drop against a mechanical/null-feature baseline. That omission is real.

WHY ONLY PARTIAL (the strong claim "can be pure overfitting" is REFUTED):
- The overfitting ceiling is BOUNDED. A saturated model (9 dummies perfectly identifying all 10 demographic groups — the max R2 any feature set can reach) gives R2=0.2967, i.e. F_causal floors at 0.7033. So F cannot be driven arbitrarily low; the whole capturable between-group variance is bounded.
- The 4-feature set already explains 0.2747/0.2967 = 93% of ALL between-group variance vs the 3-feature set's 65%. The extra drop is mostly genuine between-group signal, not residual absorption.
- Null test: adding ONE random per-group junk feature raises R2 by mean 0.0073 (max 0.0359 / 20 seeds); a per-unit junk feature does nothing (dR2=1e-5, since N>>p). For the TWO features actually added (MigrantRatio+LogPopDensity) the mechanical null is ~0.015. The observed drop is 0.0816 — ~5x the 2-feature mean null and >2x even the worst-case single-feature null. So the drop is NOT explained by mechanical inflation.

Net: the critique correctly identifies an uncontrolled confound and an unstated assumption in the narration, but overstates it ("pure overfitting"). The drop is real and bounded; the paper just never shows it's distinguishable from the junk-feature null. Severity is minor (not important) because (a) the qualitative conclusions don't depend on the absolute scale — they depend on the within-set before→after delta and Jaccard-0.93 targeting stability, both of which I see preserved, and (b) the paper already flags F_causal as feature-set-specific.  
**Fix:** In comparison_3v4.md, soften line 8 and the lines 26-28 narration: do not present the cross-set 0.81→0.73 magnitude as direct evidence of "more real inequity." Instead add the one-line control I computed: a saturated 10-group dummy model caps demographic R2 at 0.297 (F_causal floor 0.703); the 3-feature set captures 65% of that ceiling and the 4-feature set 93%, while a random per-group null feature inflates R2 by only ~0.007 (so the +2-feature null is ~0.015 vs the observed 0.082 drop). State the claim as "the 4-feature set captures a larger fraction of the bounded between-group variance (93% vs 65% of the saturated ceiling), beyond what junk-feature inflation would produce" rather than "more inequity exists." Report F_causal strictly as feature-set-specific and rest the adoption argument on within-set before→after deltas + Jaccard-0.93 targeting stability (already in the table), not on the absolute magnitude of the cross-set drop.

## [A] IMPORTANT — README headline frames most-fair SELECT as null, but it is Wilcoxon-significant (p=0.031, 6/6 seeds) — same nominal p as the edited effect
**Verdict:** confirmed  
Verified every cited number directly against PAPER/data/weighted_bc_4feat_paired_stats.json (f_causal block):

- most_fair_w10: mean +0.002014, wilcoxon_p 0.03125; diffs [0.0038, 0.0007, 0.0028, 0.0033, 0.0003, 0.0011] = 6/6 positive.
- most_fair_w30: mean +0.002197, wilcoxon_p 0.03125; diffs [0.0011, 0.0018, 0.0033, 0.0029, 0.0007, 0.0033] = 6/6 positive.
- most_fair_w20: mean +0.000257, wilcoxon_p 1.0 (genuinely n.s. — the only weight where SELECT is null).
- random_w10: wilcoxon_p 0.84375, 3/6 positive. random_w30: wilcoxon_p 0.3125, 4/6 positive — genuinely null.
- edited_w10/w20/w30: all wilcoxon_p 0.03125.

So most_fair_w10 and most_fair_w30 carry the SAME nominal Wilcoxon p (0.03125) as the edited effect and are 6/6-seed monotone positive. The ONLY genuinely null arm is the random placebo. The claim that SELECT is significant is correct; the claim that only RANDOM is the null placebo is correct.

README framing (PAPER/README.md lines 24-27): 'EDIT ≫ SELECT > RANDOM — editing ... beats upweighting the already-fairest ones (+0.002, ~12× smaller) which beats the random placebo (null). ... So FAMAIL's gain is specific to editing, not reproducible by selecting or removing data.' The headline does correctly state SELECT's +0.002 magnitude and the '~12×' ordering, and it attaches '(null)' only to RANDOM, not to SELECT. That part is defensible. The genuine overclaim is the concluding sentence 'not reproducible by selecting ... data' — selection DOES reproduce a small, statistically significant (p=0.031, 6/6) fraction of the effect, so 'not reproducible by selecting' overstates it; the honest claim is a ~12× magnitude ordering / gradient, not a binary edit-only specificity.

The MEMORY note ('most-fair & placebo don't [reproduce it]') is the inaccurate elision — it lumps the significant SELECT arm with the null RANDOM arm.

Importantly, tables/comparison_3v4.md is already fully honest about this: line 21 reports most_fair_w30 +0.0022 (p=0.031) as 'weakly positive under richer metric, still ~12× smaller than editing,' and lines 32-36 add 'One honest nuance (most-fair) ... no longer exactly zero ... weakly but consistently positive ... The clean ordering is therefore EDIT (+0.027) ≫ SELECT (+0.002) > RANDOM (null) ... arguably a stronger story than "selection does nothing," because it shows the gradient.' So the correct framing already exists in the bundle; only the README headline and MEMORY summary need to inherit it.

Severity: important rather than critical. The finding's core complaint (the 'edit-SPECIFIC, not reproducible by selecting' phrasing overclaims) is correct and should be fixed before submission. But it is NOT a result error: the headline number ordering (EDIT ≫ SELECT > RANDOM, ~12×) is already in the README and is the substantively correct and defensible claim; the canonical table is honest; the edited effect's own significance is unaffected. The fix is a one-sentence wording change, and the corrected story (a dose/gradient) is actually slightly stronger, not weaker. So it does not undermine a headline conclusion — it tightens a single sentence.  
**Fix:** Edit PAPER/README.md lines 26-27. Replace the concluding sentence so it claims a magnitude gradient, not a binary edit-only specificity, and stop implying SELECT is null. Suggested rewrite: 'editing the unfair trajectories (+0.027) beats upweighting the already-fairest ones (+0.002, p=0.031 but ~12× smaller), which in turn beats the random placebo (genuinely null). Selecting fair data reproduces only a small fraction of the gain; filtering out unfair trajectories does not help (Pareto). So FAMAIL's gain is driven primarily by editing — selection captures ~1/12 of it and removal none.' This mirrors the already-honest tables/comparison_3v4.md lines 32-36 ('shows the gradient'). Also update the MEMORY note project_placebo_pickup / weighted-bc-transfer wording 'most-fair & placebo don't' to 'random placebo is null; most-fair (SELECT) is weakly significant (p=0.031, ~12× smaller than edit)' so the index stops eliding it. No data, figure, or numeric recomputation is required — only the headline prose.

## [A] IMPORTANT — "Significant" recovery p-values are pinned at the n=6 Wilcoxon floor (0.03125) with no multiple-comparisons control
**Verdict:** confirmed  
VERIFIED IN SOURCE. In PAPER/data/weighted_bc_4feat_paired_stats.json every arm called "significant" reports wilcoxon_p EXACTLY 0.03125: f_causal edited_w10/w20/w30 (lines 33/51/69), most_fair_w10 (123), most_fair_w30 (159); plus f_spatial and fidelity_b counterparts. dose_response.json lists all three edited weights at 0.03125. I confirmed via scipy that 0.03125 = 1/32 is the MINIMUM two-sided Wilcoxon signed-rank p achievable at n=6 (wilcoxon([0.0209,0.0191,0.0192,0.0191,0.0175,0.0184]).pvalue == 0.03125): the test is saturated, so "p=0.031" certifies only "all 6 paired seeds share the sign," nothing about rank/magnitude. grep across PAPER/ and famail_temporal/analysis/ for bonferroni|holm|fdr|benjamini|multiplicity|family-wise returns NOTHING — there is no correction. The family is at least 8 arms tested against raw (edited/most_fair/random x w10/w20/w30, minus a couple); a Bonferroni threshold 0.05/8 ≈ 0.0063 sits BELOW the 0.03125 floor, so NO n=6 Wilcoxon test in this family can survive correction. This directly undermines the most_fair "SELECT helps" sub-claim (comparison_3v4.md line 21: most_fair_w30 "+0.0022 (p=0.031)" treated as positive while random is n.s.) — and that framing is further contradicted by most_fair_w20 = p=1.0 (line 141) breaking monotonicity. TWO CAVEATS that keep this at 'important' not 'critical': (1) the HEADLINE edited_wN recovery does NOT rest on the saturated p alone — it has independent t_ci that exclude zero by a wide margin (edited_w30 CI [0.0243,0.0306], mean 0.0274, std 0.0030, ~9σ) plus a 3-point dose-response, and reproduces across the 3-feature set — so the finding's blanket "carries no information beyond sign" is slightly overstated for the main result. (2) The placebo claim (random null, edited significant) is qualitatively safe because the edited CIs dwarf random's. The real, confirmed defect is the most_fair gradient story and the across-the-board labeling of floor p-values as "significant" with zero multiplicity discipline.  
**Fix:** Stop leaning on Wilcoxon p for these n=6 paired comparisons — at n=6 it can only ever read 0.03125 (all-same-sign) or worse, so it cannot survive any multiple-comparisons correction and conveys only the sign-unanimity. Concretely: (1) Replace/augment the reported statistic with the paired t-CI already in the JSON (t_ci) or a bootstrap CI as the primary evidence, and state effect sizes with CIs rather than a saturated p. (2) For the edited_wN headline, foreground the CI + 3-point dose-response + 3-vs-4-feature reproduction (these are the genuine support) and footnote that the Wilcoxon p is at its n=6 floor. (3) For the most_fair "SELECT > RANDOM" gradient, either drop the significance language entirely (most_fair_w20 is p=1.0, breaking the monotone story) and present it explicitly as a descriptive CI-overlap observation, or run the family with an FDR/Holm correction and report adjusted intervals — noting that at n=6 no arm clears Bonferroni, so the claim must rest on effect-size/CI separation, not significance. (4) Add one sentence to README/tables disclosing the family size and that p=0.031 == n=6 floor == sign-unanimity, so no reader over-reads it.

## [A] IMPORTANT — "Causal" in F_causal is unjustified — it is the partial R² of a cross-sectional OLS with no identification
**Verdict:** confirmed  
The technical core of the finding is fully verified against source.

1. F_causal IS literally 1 − R² of a contemporaneous OLS. `famail_temporal/fairness/causal.py:4` defines `F_causal = R'(I-H_demo)R / R'MR, R = Y - g_0(D)`. `docs/F_CAUSAL_METHODOLOGY_NOTES.md:58-64` confirms `F_causal = R'(I−H)R/R'MR = 1 − r²_demo` where r²_demo is "the coefficient of determination from regressing R on [1, X̃]." `docs/FAIRNESS_DECOMPOSITION_FORMULATION.md:93` independently states `1 − F_causal = r²_demo`. The per-cell decomposition (causal.py:110, lines 150-158) is `αᵢ = 1/N − ((MR)ᵢ² − ((I−H)R)ᵢ²)/R'MR`, exactly the partial-R² FWL form the finding quotes, and the docstring (causal.py:105-106) does say negative cells mean "demographics explain MORE than baseline of the cell's residual." All claimed numbers/quotes check out.

2. No identification machinery exists anywhere. grep for exogen/instrument/unconfounded/counterfactual/confound across all .py: the only `instrument` hit is `modifier.py:304` "instrumentation" (logging). No IV, no exogeneity test, no unconfoundedness/ignorability assumption, no temporal precedence, no counterfactual is stated or tested. The metric IS a cross-sectional, contemporaneous, district-level (only ~10 districts of demographic DOF) linear partialling-out.

3. The codebase itself admits the gap. F_CAUSAL_METHODOLOGY_NOTES.md:354-358 (Limitation #2): "Endogenous demand is controlled but not modeled... using it as an instrument would be a more sophisticated extension" — an explicit acknowledgment that demand is endogenous and that no instrument is present. Yet the same doc titles itself "causal-fairness term" (line 3) and the paper-ready methods text (line 301) calls it "a double-regression causal-fairness metric."

4. The causal-interpretation language is load-bearing and pervasive, not a one-off. README.md:11 "F_causal (causal fairness, 1 = fairest)"; config.py:48 "captures more demographic-driven demand inequity"; RESEARCHER_HANDOFF.md:33 "demographics, not demand, are the right CAUSAL variable to partial out when measuring service inequity"; comparison_3v4.md, RESULTS_INDEX.md repeat "demographic-driven demand inequity." Reading edits as removing demographic-driven inequity asserts causation from a partial correlation.

Two calibration corrections to the finding as stated:
(a) The README line references are slightly off — "causal fairness, 1=fairest" is at README.md:11 (not 12), and the "demographic-driven demand inequity" phrasing is at config.py:48 / comparison_3v4.md (README line 27 is actually the edit/select/random ablation). The substance is present, the line numbers are imprecise.
(b) The finding calls it "R² of a contemporaneous linear regression of the residual on demographics" — accurate, but worth noting it is a PARTIAL R² (demand D is first removed via the power-basis stage g_0). This first-stage adjustment is a regression control, not an identification strategy, so the associational critique stands; demographics remain collinear with unobserved geography/transit/land-use and the partialled coefficient is not causal.

Severity: I set this to important rather than critical. It is a genuine, defensible overclaim that a KDD reviewer can and likely will raise, and the fix is a naming/framing change, not a numerical or experimental error. None of the empirical results (ΔF_causal +0.019/+0.026/+0.027, p=0.031, L1/L2 conclusions) change — they are valid statements about an associational fairness metric. The exposure is reputational/framing, fully remediable by renaming and a one-paragraph caveat, which is why it is serious but not result-invalidating.  
**Fix:** Rename and reframe to drop the unsupported causal claim while keeping the metric and all results intact:

1. Rename the construct in paper-facing text from "causal fairness / F_causal" to an associational descriptor, e.g. "demographic-conditional service-equity" or "demand-adjusted demographic-independence (F_demo)". At minimum, stop calling it "causal." Touch: PAPER/README.md:11, F_CAUSAL_METHODOLOGY_NOTES.md:3,301 ("double-regression causal-fairness metric"), RESEARCHER_HANDOFF.md:13,33.

2. Replace every "demographic-driven (demand) inequity" with associational wording ("service rate is statistically aligned with / predicted by demographic composition after demand adjustment"). Touch: config.py:48, comparison_3v4.md:8-9,27, RESULTS_INDEX.md:19, fcausal_feature_selection.md:3.

3. Add one explicit caveat paragraph to the methods section stating that F_causal is the partial R² of a cross-sectional OLS of the demand-adjusted residual on observational district-level demographics; it has no instrument, no temporal precedence, no counterfactual, and no unconfoundedness guarantee, so it measures demographic *association/predictability* of service, not a causal effect — district demographics are collinear with unobserved geography/transit and the partialled coefficient cannot separate causation from confounding. This can be promoted directly from the already-existing Limitation #2 (F_CAUSAL_METHODOLOGY_NOTES.md:354-358) rather than buried in a notes file.

4. Keep RESEARCHER_HANDOFF.md:33's wording defensible by changing "the right causal variable to partial out" to "the right variable to partial out."

No code, numbers, or experiments need to change — only the interpretive labels. All empirical claims remain valid as statements about an associational metric.

## [A] MINOR — L1 "edited is fairest faithful" — circularity is real but the README already flags non-transfer; overclaim charge does not hold
**Verdict:** partial  
Every number in the finding checks out against the actual JSONs. (1) Editor optimizes F_causal at alpha_causal=0.7 (PAPER/data/editor_4feat_metrics.json line 183). (2) metrics_before f_causal 0.7252857685 -> metrics_after 0.7409303784, delta +0.015644609928 (lines 194/200/206). (3) L1v2_4feat_multiseed.json edited f_causal mean 0.7409303784, std 0.0, all 5 seed values identical (lines 181-193) — and L1 raw (0.7252857685, std 0.0) equals the editor's metrics_before EXACTLY, proving L1 is a deterministic static rescore of the very grid the editor produced, not a learned/policy quantity. So the "+0.0156 edited-beats-raw" reported by L1 is literally the editor's own optimization delta re-displayed. The circularity / partly-definitional observation is therefore VALID and is genuinely not flagged anywhere in the README. (4) L2_4feat_metrics.json: edited f_causal mean 0.7263561725 vs raw 0.7273709654 — edited IS marginally lower; paired edited-raw mean -0.0010147929, Wilcoxon p=0.3125 (n.s.) (lines 26/79/247/250).

WHERE THE FINDING OVERREACHES: the central charge — "the README presents 0.741 as a headline win without flagging it is the optimized-and-rescored static value [and that it doesn't propagate]" — is largely refuted by the README's own text. PAPER/README.md line 18-19 explicitly scopes 0.741 to "Pillar 1 (L1 — data quality)" (a data-quality claim, not a policy claim), and lines 20-21 immediately state: "L2 (vanilla transfer): driver-conditioned BC trained on edited data does not transfer the fairness (edited-raw ΔF_causal -0.0010, n.s.) — it averages it away." The README reports the correct sign and the n.s. flag, and the entire two-pillar structure exists precisely to separate the static data-quality result from the (failed) vanilla policy transfer. So the claim that non-propagation is unflagged is false; only the narrower circularity caveat (that 0.741 is the editor's own objective re-scored) is missing. Hence partial, and severity drops from "important" to "minor": the math is right and the circularity note is a fair methods caveat, but the headline-overclaim narrative misreads the deliverable.  
**Fix:** Add one clause to PAPER/README.md line 18-19 making the circularity explicit, e.g.: "the edited dataset is the fairest faithful source (static rescore F_causal 0.741 vs raw 0.725; note F_causal is also the editor's optimization target at alpha_causal=0.7, so the static gain is expected by construction — its value is that it is achieved while remaining identity-faithful, Fidelity-A unchanged)." Do NOT restate the non-transfer caveat — the README already carries it correctly (lines 20-21, edited-raw -0.0010 n.s.). No numbers need changing.

## [A] IMPORTANT — README "robust to the demographic-feature choice" overclaims vs. the project's own FRAGILE sensitivity verdict
**Verdict:** confirmed  
Every number the finding cites verifies against source. fcausal_feature_sensitivity.md line 41 = "FRAGILE"; line 43 spread 0.1779 (thr 0.05); line 44 min top-K Jaccard 0.5636 worst=drop_housing (thr 0.6); line 45 min Spearman 0.7987 (thr 0.9). Confirmed at JSON source (PAPER/data/fcausal_feature_sensitivity.json lines 163-175: verdict "FRAGILE", f_causal_spread 0.1779136, min_topk_jaccard 0.5635868, worst_jaccard_subset "drop_housing"). fcausal_feature_selection.md line 24: drop_housing F_causal 0.9030, Jaccard 0.5636 — confirmed. F_causal swings 0.7251 (broad5) to 0.9030 (drop_housing) across the sensitivity subsets — confirmed.

The finding slightly mislocates the claim: it is in PAPER/README.md lines 28-29 (the "Robustness" headline bullet), not a numbered "Claim 4," and the cited Jaccard 0.93 lives in comparison_3v4.md lines 9-10/31 (the adopted-4feat-vs-prior-3feat overlap). This does not change the substance.

The core charge holds. README line 28-29 states, unqualified, "the argument is robust to the demographic-feature choice (editor targeting Jaccard 0.93)." That generalization is contradicted by the project's own dedicated sensitivity analysis, which returns FRAGILE on all three of its own criteria, driven entirely by drop_housing (Jaccard 0.5636 < 0.6, F_causal jumping to 0.9030). The Jaccard 0.93 supports only the 4feat-vs-3feat comparison — and both of those sets retain housing — so the evidence cited establishes robustness only within the housing-retaining family the authors pre-selected, not robustness to "the demographic-feature choice" in general.

Two points of fairness that bound severity to important rather than critical: (1) all numbers are real, not fabricated — this is a scope/qualifier omission, not a numeric error; (2) the underlying analysis doc comparison_3v4.md line 43 already states the correct, qualified version ("robust ... Jaccard >= 0.92 across all housing-retaining sets"). The README simply dropped the "housing-retaining" qualifier when promoting it to a headline, producing a claim that contradicts the FRAGILE verdict sitting two files over in the same bundle (tables/fcausal_feature_sensitivity.md).  
**Fix:** Edit PAPER/README.md lines 28-29 to qualify the robustness claim and reconcile it with the FRAGILE verdict. Replace "the argument is robust to the demographic-feature choice (editor targeting Jaccard 0.93)" with something like: "the argument is robust across the housing-retaining feature family (editor targeting Jaccard >= 0.92, including the prior 3-feature set). A full feature-sensitivity sweep is formally FRAGILE (F_causal spread 0.178, min Jaccard 0.564) because dropping the housing axis collapses editor targeting (drop_housing Jaccard 0.56, F_causal 0.90) and rescales F_causal; housing is therefore a load-bearing axis we retain by design. See tables/comparison_3v4.md and tables/fcausal_feature_sensitivity.md." This (a) restores the "housing-retaining" qualifier already present in comparison_3v4.md line 43, (b) discloses the FRAGILE verdict so the README does not contradict its own bundle, and (c) frames housing-retention as an explicit, defended design choice rather than an unstated precondition.

## [B] IMPORTANT — Cleanup-delta tables are silently 3-feature inside a 4-feature-labeled paper deliverable
**Verdict:** confirmed  
Every load-bearing number in the finding reproduces against the real source files. The cleanup-delta artifacts (cleanup_delta_editor.csv, experiment_cleanup_delta.md, and by extension the cleanup narrative) report 3-FEATURE F_causal: cleanup_delta_editor.csv clean_before f_causal=0.8069279193878174 and clean edited 0.8193410634994507, which match exactly famail_temporal/results/2026-06-26T12-32-59_k-10000_causal_emphasis_no-dedup_cleaned/metrics.json (metrics_before.f_causal=0.8069279193878174, metrics_after.f_causal=0.8193410634994507) whose config_snapshot.DEMOGRAPHIC_FEATURES=['AvgHousingPricePerSqM','GDPperCapita','CompPerCapita'] — the 3-feature set. The headline PAPER deliverable is 4-FEATURE: PAPER/data/editor_4feat_metrics.json has DEMOGRAPHIC_FEATURES=['AvgHousingPricePerSqM','CompPerCapita','MigrantRatio','LogPopDensity'] with metrics_before.f_causal=0.7252857685089111, and PAPER/data/L1v2_4feat_multiseed.json per_source.raw.f_causal.mean=0.7252857685089111. So the SAME 'clean before' editor run is reported as F_causal 0.8069 in the cleanup table and 0.7253 in the 4-feat headline — a 0.0816 gap, exactly as claimed. experiment_cleanup_delta.md's L1-v2 stage likewise prints raw 0.8069 / edited 0.8193 with no feature-set label. The three cleanup files carry NO feature-set annotation in their headers. README.md:50 does mention 'the 3-feat experiment dirs vs the dirty baselines', confirming the author knew, and README.md:28-30 + tables/comparison_3v4.md document the 3-vs-4 robustness story — so the deliverable as a whole is not concealing the dual feature sets, which is why this is 'important' rather than 'critical': a careful reader has the breadcrumb. But the cleanup tables in isolation are unlabeled and will produce an unexplained 0.08 discrepancy for anyone cross-referencing the cleanup 'clean edited 0.8193' against the primary 4-feat L1 numbers (raw 0.7253, edited 0.7409). The 'changed no conclusion' claim itself is methodologically fine (it is an internal dirty-vs-clean comparison held at constant 3-feature formulation, apples-to-apples) — but it is embedded in a directory whose README banner says all numbers are '4-feature F_causal', so the mismatch is real and reader-facing.  
**Fix:** Add an explicit feature-set label to each of the three cleanup-delta artifacts so they are self-describing: (1) prepend a header note to PAPER/tables/dataset_summary.md and PAPER/tables/experiment_cleanup_delta.md stating 'F_causal here uses the 3-feature set {AvgHousingPricePerSqM, GDPperCapita, CompPerCapita}; the cleanup was validated under the prior 3-feature formulation, and the dirty-vs-clean conclusions are feature-set-invariant (see comparison_3v4.md). Absolute F_causal values are NOT comparable to the 4-feature headline tables (e.g. clean edited 0.8193 here vs 0.7409 4-feat).'; (2) add a feature-set provenance column or trailing comment line to cleanup_delta_editor.csv naming the 3-feature set and its source run 2026-06-26T12-32-59_..._cleaned/metrics.json; (3) optionally, the cleaner fix is to regenerate all three cleanup-delta tables from the 4-feature dirty/clean editor runs so the entire PAPER/ deliverable is uniformly 4-feature, eliminating the cross-table 0.08 gap entirely.

## [B] MINOR — Stale hardcoded cleanup header in experiment_delta.py mischaracterizes the on-disk filter (6 drivers/cell (28,52) vs actual 9 drivers/10 cells, (28,52) absent)
**Verdict:** confirmed  
Every claimed fact checks out against the real files.

1. The hardcoded literal exists exactly as quoted. famail_temporal/analysis/experiment_delta.py:231 contains: "Data cleanup = stuck-GPS sink filter (6 drivers, cell (28,52) removed)." It is a static string in render_report(), not derived from any metadata.

2. The on-disk filter does NOT match. famail_temporal/source_data/processing_metadata.json -> stuck_gps_sinks.flagged_cells = [[17,39],[17,40],[18,29],[18,39],[19,10],[21,29],[23,47],[25,6],[29,29],[29,53]] = 10 cells, and [28,52] is NOT among them (verified programmatically: "(28,52) in flagged? False"). The actual cleanup removed 11 sink entries across 9 distinct driver plates and 10 flagged cells (n_pickups_removed=106677), not "6 drivers / cell (28,52)".

3. The contradiction with the other artifact holds. PAPER/tables/dataset_summary.md:8 correctly reports n_sink_cells = 10, directly contradicting the header's single-cell "(28,52)" claim. MEMORY also records "10 calibrated sink cells."

4. The stale text propagates into the paper deliverable: the identical line appears at PAPER/tables/experiment_cleanup_delta.md:3 AND the source results copy famail_temporal/results/analysis/experiment_cleanup_delta/experiment_cleanup_delta.md:3, since both are rendered from the hardcoded string.

Provenance note: "6 drivers, cell (28,52)" matches the pre-decision Meeting-40 finding in MEMORY (project_pickup_gps_sinks.md: "6 per-driver stuck-GPS pickup sinks ... Only cell (28,52) materially distorts F_spatial"). So the header is leftover descriptive text from the earlier exploratory analysis that was never updated when the PI decided to filter all sinks (10 calibrated cells). It is purely a documentation/labeling defect.

Why severity = minor, not important: The defect is confined to a human-readable descriptive subtitle. It does NOT touch any number in the delta tables, any verdict computation (the four-stage deltas and PRESERVED/CHANGED logic are computed from result JSONs, independent of this string), or the dataset_summary table that downstream readers would cite for the cleanup magnitude. The numbers the table actually documents are unaffected; only the one-line caption mislabels what was cleaned. The "changed no conclusion" claim is unrelated to this string and is not impugned by it. Real but cosmetic; it would mislead a reader about the cleanup scope and contradicts the adjacent dataset_summary table, so it must be fixed before submission, but it is not a numerical or correctness error.  
**Fix:** In famail_temporal/analysis/experiment_delta.py:231, replace the hardcoded literal with text matching the actual filter, ideally derived from processing_metadata.json rather than hardcoded. Concretely, change:
  "Data cleanup = stuck-GPS sink filter (6 drivers, cell (28,52) removed)."
to something like:
  "Data cleanup = stuck-GPS sink filter (9 drivers, 10 flagged cells; 106,677 pickups removed)."
Best: load famail_temporal/source_data/processing_metadata.json -> stuck_gps_sinks and interpolate len(flagged_cells), len(set of plate_ids), and n_pickups_removed so the caption can never drift again. Then re-render to refresh both PAPER/tables/experiment_cleanup_delta.md:3 and famail_temporal/results/analysis/experiment_cleanup_delta/experiment_cleanup_delta.md:3 so they agree with PAPER/tables/dataset_summary.md (n_sink_cells=10).

## [B] IMPORTANT — removal_rate denominator (seeking+driving) is shown only beside total_seeking_extracted, inviting a "~39% of seeking removed" misread
**Verdict:** confirmed  
Every claimed number checks out against the real source JSONs and the rendered table.

DIRTY (famail_temporal/source_data_dirty/processing_metadata.json, removal_summary): total_seeking_extracted=214286, total_driving_extracted=179384, total_extracted=393670, n_removed=195840, removal_rate=0.49747250234968377. Verified 195840/393670 = 0.49747250234968377 exactly. n_removed/total_seeking = 0.9139 (91.4%).

CLEAN (famail_temporal/source_data/processing_metadata.json, removal_summary): total_seeking_extracted=133091, total_driving_extracted=173178, total_extracted=306269, n_removed=119290, removal_rate=0.38949420280864205. Verified 119290/306269 = 0.38949420280864205 exactly. n_removed/total_seeking = 0.8963 (89.6%).

So removal_rate denominates over total_extracted (seeking + driving), NOT seeking alone — confirmed.

The table (PAPER/tables/dataset_summary.md, generated by famail_temporal/analysis/dataset_summary.py write_dataset_summary, lines 67-76) lists rows: n_removed, removal_rate, total_seeking_extracted, n_sink_cells, phantom_pickups_removed. dataset_summary.py lines 24-42 deliberately keep only total_seeking_extracted and drop total_driving_extracted and total_extracted from both the dirty and clean blocks. So the ONLY extraction-count denominator visible in the table is the seeking count, while removal_rate is actually over seeking+driving. Reading the two adjacent rows literally (0.3895 next to total_seeking_extracted=133,091) does invite the misread "~39% of seeking removed"; the true seeking-only removal fraction is 89.6% (clean) / 91.4% (dirty), and seeking extraction itself fell 214,286->133,091.

One refinement that strengthens rather than weakens the finding: counts_by_category sums exactly to n_removed (195,540 action_space_violation + 300 implausibly_long = 195,840), and these removals span both seeking and driving trajectories, so even "91.4% of seeking removed" slightly over-attributes — the removals are not all seeking. This makes the missing driving/total denominators in the table even more material. The 38.95% figure is correct AS an overall removal rate; it is just unlabeled and adjacency-misleading.

Severity: important (not critical) — the underlying number is arithmetically correct and defensible; the problem is presentation/labeling that can produce a materially wrong claim ("39% of seeking trajectories removed" vs the true ~90%) in the paper narrative. No numeric result is wrong, but a reader-facing misinterpretation is near-certain as laid out.  
**Fix:** In famail_temporal/analysis/dataset_summary.py (dirty_block/clean_block at lines 24-35 and the markdown rows at lines 71-76), either (a) add total_driving_extracted and total_extracted rows so the 0.4975/0.3895 rate has its true denominator visible, or (b) rename the rate to "overall_removal_rate" and add an explicit note "= n_removed / total_extracted (seeking+driving)". Additionally, add a derived "seeking_removal_fraction" only if removals are first partitioned by kind — do NOT compute n_removed/total_seeking_extracted as "seeking removed", because n_removed includes driving-trajectory removals (counts_by_category spans both kinds), so that ratio (0.914/0.896) is itself an over-attribution. Safest: present total_extracted alongside removal_rate, drop the misleading seeking-only adjacency, and state in the caption that removal_rate is over all extracted trajectories.

## [B] MINOR — Unweighted 'edited' WBC arm flips sig→n.s. (p=0.03125→0.4375) and the cleanup-delta verdict prose silently omits it
**Verdict:** partial  
All factual and code claims are verified against the real source JSONs. In famail_temporal/results/weighted_bc_sweep/sig_6seed_w10_w20_w30/sweep.json the unweighted 'edited' f_causal arm is mean=-0.0018919 (rounds to -0.0019), wilcoxon_p=0.03125 (significant, <0.05); in famail_temporal/results/weighted_bc_sweep/cleaned_6seed/sweep.json it is mean=-0.0007560 (rounds to -0.0008), wilcoxon_p=0.4375 (n.s.). PAPER/tables/experiment_cleanup_delta.md line 30 reports exactly these numbers. The verdict logic in famail_temporal/analysis/experiment_delta.py:275 (key_arms=[a for a in wbc if a.startswith('edited_w') ...]) does exclude the unweighted 'edited' arm (since 'edited'.startswith('edited_w') is False), and the prose verdict at line 282/287 'PRESERVED — weighted arms stay significant in both dirty & clean' is computed only over edited_w10/w20/w30 (all p=0.03125 in both), so the unweighted arm's flip is never flagged in prose even though the table row shows it.\n\nWhere the finding overstates: it calls this 'a real conclusion change' for the L1/L2 narrative. The paper's actual L2 conclusion (famail_temporal/baselines/LEVEL2_RESULTS.md) does NOT rest on the unweighted-edited arm being statistically significant — it explicitly stands behind the qualitative claim ('no positive transfer; a small, directionally-unanimous negative'), and even labels its own p=0.0625 the 'resolution floor', declining to lean on significance. Under clean data the unweighted-edited mean is still negative (-0.0008), so 'no positive transfer / edited slightly lower' is preserved. The dirty p=0.03125 is itself an artifact of n=6 (the floor at n=6); the same arm in the dedicated L2 5-seed run is p=0.0625 (n.s.). So the direction and the paper-level conclusion are preserved; what flips is only an n=6 significance label on a non-load-bearing arm. Hence the verdict-prose omission and code-filter claim are confirmed, but the 'changed conclusion masked' characterization is partially overstated — making this a presentation/transparency gap rather than a substantive conclusion reversal. Severity adjusted to minor (the finding claimed important): the table already discloses both p-values; only the auto-generated prose over-generalizes 'weighted arms' to imply all arms preserved.  
**Fix:** In famail_temporal/analysis/experiment_delta.py, make the WBC verdict acknowledge the unweighted 'edited' arm explicitly rather than silently filtering it out. Two concrete options: (1) Compute and report the unweighted arm's status separately, e.g. after building key_arms, check the unweighted 'edited' arm and append a note when its significance flips: ua = wbc.get('edited'); if ua and ua['status']=='compared' and ua['p_dirty'] is not None and ua['p_clean'] is not None and (ua['p_dirty']<0.05) != (ua['p_clean']<0.05): verdict_wbc += f\"; NOTE unweighted 'edited' arm significance flipped (p {ua['p_dirty']}->{ua['p_clean']}), direction preserved (Δ {ua['dirty_delta_vs_raw']:.4f}->{ua['clean_delta_vs_raw']:.4f}, both negative)\". (2) Tighten the prose wording from 'weighted arms stay significant' to 'weighted (wN) arms stay significant; unweighted edited arm stays directionally negative but is n.s. under clean (n=6 floor)'. Regenerate PAPER/tables/experiment_cleanup_delta.md from the corrected renderer so the prose verdict on line 40 no longer over-generalizes. No data re-run is needed — the numbers in the table are already correct.

## [B] MINOR — README headline juxtaposes per-cell +0.0885 against net +0.0213 without disclosing the -0.0783 redistribution residual
**Verdict:** confirmed  
Every checkable number holds against the source JSON (PAPER/data/sink_f_spatial_decomposition.json). README.md:16-17 claims sink (29,53) "alone accounted for +0.0885 of the per-cell F_spatial recovery (net global F_spatial +0.0213)". JSON confirms: per_cell delta for (29,53)=+0.08852594 and global_shift=+0.02127084, so the cell's local gain is 416.18% of the net global recovery (share_of_global_shift=4.1618; table renders 416.2%). The reconciliation is sinks_delta_sum(+0.09957793) + redistribution_residual(-0.07830709) = +0.02127084 (exact). The residual is -88.5% of the single sink's gain (-78.6% of the 10-sink sum) — a large, real cancellation spread over non-sink cells.

The misread risk is genuine: "accounted for X of the recovery" idiomatically implies X is a fraction of the recovery, yet X is 4.16x it. The lone disambiguator is the word "per-cell," which is subtle; the README never mentions redistribution/residual anywhere (grep for redistribut|residual|offset|cancel|non-sink|856 returns zero hits in README.md), so the +0.0885-vs-+0.0213 pairing reads as an unexplained paradox.

Mitigating (why minor, not important): the underlying numbers are CORRECT, not fabricated or wrong. The full decomposition IS disclosed and self-consistent in the linked table tables/sink_f_spatial_decomposition.md:3-4 (states the +0.09958 sink sum and the -0.07831 residual explicitly). So this is a presentation/clarity defect in a summary line, not a numerical or methodological error. The finding's "856 cells" sub-detail and the exact "76%" cancellation figure (actual: 78.6% of sink-sum / 88.5% of single sink) are slightly imprecise but not load-bearing; the residual's sign, magnitude, and the headline omission are all confirmed.  
**Fix:** Reword README.md:16-17 to disclose the redistribution mechanism, e.g.: "the headline stuck-GPS sink at grid (29,53) recovered +0.0885 locally (its per-cell F_spatial contribution rose from -0.0987 to -0.0101); ~88% of that local gain was offset by a -0.0783 redistribution drop spread across non-sink cells, leaving a net global F_spatial recovery of +0.0213. See tables/sink_f_spatial_decomposition.md." This keeps the headline number, removes the apparent 416% paradox, and points to the full decomposition.
