# Cut Plan v2 — re-costed on settled geometry (2026-07-20)

Supersedes the execution ordering of `2026-07-18-ranked-cut-list.md` (kept for history).
Evidence: `2026-07-20-cut-recost.md` (full re-costed table, verified quotes + line
numbers against the current build). Companion detail: `2026-07-18-cut-inventory.md`.

## Measured geometry and target

- Current build: 12 rendered pages; REFERENCES begins mid-right-column of p11 →
  **content ≈ 10.6 pp**. Target **8.0 content pp, self-contained**.
- **Deficit ≈ 290 rendered single-column lines (~2.7 pp)**, plus 2 lines of slack
  reserved for a possible Figure-1 (b)+(c) swap → effective ≈ 292.
- KDD rules: 8 content pages + references + appendix (no limit at submission;
  **camera-ready 12 pp TOTAL** → with ~1.3 pp of references the appendix budget is
  ≈ 2.5 pp). Reviewers are not required to read the appendix → every relocated item
  leaves a stub in the body carrying the claim + headline number.

## The honest arithmetic (read this first)

Conservative attainable yield across ALL live candidates ≈ **248 lines**:
~169 appendix-relocation · ~60 deletion/merge · ~19 compression. That is **~44 lines
short** of the deficit on strict counting. Three consequences:

1. **The appendix is load-bearing, not optional.** The two largest candidates
   (α-Pareto figure ~42, feature-set table ~32) are appendix-only moves.
2. **Nearly everything on the list must fire.** There is no luxury of skipping
   medium-risk items; the reserve wave will likely be entered.
3. **The last ~40 lines come from rewrite-compaction** — widow/orphan reclaim and
   float re-packing have historically beaten conservative estimates. The wave
   doctrine (land wave → rebuild → count → stop at 8.0) is what makes this safe:
   if compaction over-delivers, later waves shrink.

**Style rule for every rewrite (Robert, 2026-07-20): explicit and to the point; plain,
concise phrasing over clever language. Compression is allowed to change words freely but
never to drop a claim, a number, or a disclosure.**

## Wave 1 — body-only cuts: duplication, compression, table merges (~60 lines ≈ 0.55 pp)

No appendix dependency; safest; all LOW risk unless noted.

| # | id | lines | note |
|---|---|---:|---|
| 1 | `exp-tables-merge-A` | 12 | genuine restructure (divergent columns), LOW-MED |
| 2 | `exp-tables-merge-B` | 10 | shared mean(Y|disadv) row; one caption saved |
| 3 | `meth-weight-dup-compress` | 8 | §3.2 sweep text → forward pointer (couples w/ W2 #1) |
| 4 | `concl-restatement-shorten` | 4 | bounds paragraph untouched |
| 5 | `comp-supply-grad-exposition` | 4 | ΔS mechanism said once, not twice |
| 6 | `intro-tier-breakdown` | 3 | tier numbers duplicated at §4.2 |
| 7 | `new-intro-extmetric-numbers` | 3 | NEW dup: DI/DP/Theil verbatim from tab:external-sz |
| 8 | `rw-recourse-compress` | 3 | keep every \cite; the single admissible §2 cut |
| 9 | `comp-intro-hook` | 3 | plainer opening (drops "as competence / doomed") |
| 10 | `comp-fidelity-guardrail` | 3 | drops "We state its role honestly" framing |
| 11 | `new-intro-categories-compress` | 3 | MEDIUM: motivation para; compress, don't gut |
| 12 | `concl-future-shorten` | 2 | |
| 13 | `meth-fspatial-gini` (a) | 2 | drop O(N log N) aside only |

**Checkpoint A: rebuild + count. Expected ≈ 10.05 pp.**

## Wave 2 — appendix skeleton + the four big LOW-risk relocations (~105 lines ≈ 1.0 pp)

Build `\appendix` + `appendix.tex` first; every stub names what moved and where.

| # | id | lines | body keeps (stub) |
|---|---|---:|---|
| 1 | `exp-figure2-relocate` (α-Pareto fig + prose) | 42 | flatness ≤0.001; monotone lift-up decline (tier-1-sig only α_sp≤0.2); adopted (0.1,0.8,0.1) = criterion's best point |
| 2 | `exp-table6-relocate` (feature-set grid) | 32 | tier-2-sig on all three (+0.0411/+0.0211/+0.0771); most-fair leak +0.0054/+0.0072 stays inline; edited arm ≥3× |
| 3 | `meth-fcausal-derivation-relocate` | 18 | Eq.(1); RSS/TSS gloss; idempotence half-sentence (§3.3 dependency); r²_demo interpretation; boundary sentence |
| 4 | `meth-editor-impl-relocate` | 13 | ε=2 identity budget; "supply is endogenous"; **~5% (118/2,455) revert disclosure** |

**Checkpoint B: rebuild + count. Expected ≈ 9.1 pp.**

## Wave 3 — remaining appendix relocations, LOW→MED (~67 lines ≈ 0.6 pp)

Take in order until the count reads 8.0.

| # | id | lines | body keeps |
|---|---|---:|---|
| 1 | `exp-fairness-penalty-shorten` (λ-grid → App.) | 8 | reweigh −0.0227 wrong-way; inert/destructive verdict; formulation-independence sentence |
| 2 | `meth-attribution-eq-relocate` (Eq. 4 + check) | 7 | "closed form + autograd check in App." |
| 3 | `exp-baselines-perturbation-note-shorten` | 7 | iFGSM/FGSM naming + δ=0-no-op sentence |
| 4 | `meth-screen-detail-shorten` | 6 | 80k/95k; "screen nominates, editor derives" |
| 5 | `exp-sf-downstream-shorten` | 6 | n=12 flagship + SF F_spatial city-difference |
| 6 | `meth-attribution-eq-relocate` (Eq. 3) | 6 | MEDIUM: backs a contribution bullet; "exact per-unit partition (App.)" |
| 7 | `exp-provenance-shorten` (oracle arithmetic) | 4 | ~5% disclosure; "far below realism-free ceiling (App.)" |
| 8 | `exp-fourseource-gan-shorten` | 4 | 1-sentence GAN-collapse honesty beat |
| 9 | `exp-filtering-shorten` | 4 | 1-sentence rebuttal (0.7935 vs 0.8214) |
| 10 | `exp-setup-stats-shorten` (floor arithmetic) | 3 | sign-unanimity; n=12 survives correction |
| 11 | `exp-setup-instruments-shorten` | 3 | DP≡gap disclosure + strict-count sentence |
| 12 | `exp-dose-saturation-shorten` | 3 | "saturation; w30 at the knee" |
| 13 | `exp-variance-shorten` | 3 | +0.0030±0.0022, n=10, p=.0039 |
| 14 | `new-baselines-sf-perturbation-compress` | 3 | "arms replicate on SF, all below the editor" |

**Checkpoint C: rebuild + count. Expected ≈ 8.5 pp on strict arithmetic; compaction
typically lands lower. MEASURE.**

## Wave 4 — reserve (enter only if Checkpoint C > 8.0)

| id | lines | why held back |
|---|---:|---|
| `meth-figure1-resize` (= fig:overview, today's **Figure 2**) | 12 | best method-teacher; single-column reflow risks 3-panel legibility |
| `intro-contributions-compress` | 4 | the itemize is the reviewer's claim map |
| `meth-fspatial-gini` (b) instead of (a) | +5 net | relocates a scored metric's definition |
| Aggressive second compression pass (§3.4/§4.2 prose) | ~10–15 | needs fresh candidate hunt; same style rule |

If Wave 4 still leaves > 8.0: that is a Robert/Zhang conversation (e.g., the Figure-1
(b)+(c) question resolves the slack the other way; further §4 evidence consolidation).

## Appendix recommendations (the "safe to move" list, Robert-facing)

Everything below is canonical KDD appendix content (derivations, pseudo-code-level
implementation detail, protocol arithmetic, extended results), and the body stub keeps
the 8 pages self-contained. Proposed appendix structure, ≈ **1.7–1.9 pp** total
(≤ 2.5 pp camera-ready budget):

- **App. A — Derivations** (~0.45 pp): F_demo closed-form/FWL/O(N) identity;
  attribution closed forms Eq.(3)/(4) + autograd verification; (optionally Gini Eq.(2)).
- **App. B — Editor implementation** (~0.35 pp): Gumbel/STE bridging, taper constants,
  king-move repair procedure; screen scoring recipe.
- **App. C — Extended results** (~0.85 pp): α-Pareto figure + sweep prose; feature-set
  robustness table; penalty λ-grid; saturation doses; model-level variance detail;
  Shenzhen GAN per-seed spread; SF per-arm perturbation percentages; oracle-ceiling
  arithmetic.
- **App. D — Statistical protocol** (~0.1 pp): exact-Wilcoxon floor arithmetic;
  instrument definitions beyond the DP≡gap disclosure.

Explicitly NOT appendix-eligible (stays in body, verbatim or shortened-in-place): both
pillars' numbers; all disclosures (allocation drain, most-fair leak, leveling-down
scoping, SF caveats, §5 bounds, DP≡gap, tier-2/Reading-B block, ~5% revert, oversampling
fabrication/placebo); §3.4 structural-diagnosis numbers; two-phase-as-control paragraph;
associational caveat; n=12 flagship results.

## Execution rules (binding)

1. Wave order as ranked; **rebuild + count after every wave; stop at 8.0.**
2. Gates after every wave: latexmk chain + lint + render-QA of touched pages.
3. Every stub names its appendix target ("App. A"), every appendix block opens by naming
   what section it came from.
4. Style: plain, explicit, concise; no coinages; claims/numbers/disclosures never dropped.
5. Keep ≥2 lines of final slack for the Figure-1 (b)+(c) swap.
6. After the cut lands: tighten lint Overfull gate 8pt→5pt; then the T18 §4 audit.
