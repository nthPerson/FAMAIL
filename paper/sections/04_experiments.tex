\section{Experiments}\label{sec:experiments}
% Remaining subsections land per plan Tasks 5-10 (5.2-5.7 in plan numbering).
% Disclosure ledger for this section: rollout-allocation drain attenuated ~40% NOT
% reversed; leveling-down; skip-on-infeasible provenance; tier-1 vs tier-2 supply
% accounting; SF mean(Y|D) tension (open PI framing).

\subsection{Experimental Setup}\label{sec:exp-setup}

\textbf{Datasets.} The primary city is Shenzhen: a 50-driver sample of real taxi
GPS trajectories on a $48{\times}90$ grid of $0.01^{\circ}$ cells, with
district-level demographics. Raw data contained per-driver ``stuck-GPS'' pickup
sinks (single plates emitting thousands of phantom pickups); a signature rule
removed 106{,}677 phantom pickups before any experiment.
% src: PAPER/argument/02_datasets.md (grid, cleanup counts)
The external-validity city is San Francisco (Cabspotting): a 12-driver,
fleet-density-matched subsample (\texttt{sf12}, $32{\times}30$ grid) with the
same demographic feature names filled from ACS tract values. Density matching
is required --- the full 536-taxi fleet saturates the supply measure and leaves
no editable fairness gradient.
% src: PAPER/argument/02_datasets.md (sf12 selection, density matching)

\textbf{Editor configuration.} All edited corpora use the trim+lift editor of
\S\ref{sec:editor} with $\alpha = (0.1, 0.8, 0.1)$ --- the configuration the
three-ring selection criterion picks on the measured frontier
(\S\ref{sec:exp-robustness}) --- $\varepsilon = 2$, and the
skip-on-infeasible rule. Shenzhen: $k = 10{,}000$ (2{,}337 trim + 7{,}545
lift after filtering); San Francisco: $k = 2{,}000$ (1{,}330 trim + 629 lift).
% src: famail_temporal/results/2026-07-10T02-06-37_alpha_sweep_s10_c80_f10_filtered/metrics.json (SZ split)
% src: famail_temporal/results/2026-07-11T11-31-55_supply_lift_a10_sf12_filtered/metrics.json (SF split)
% src: PAPER/objective-motivation/weight-sensitivity/DECISION.md + EXTENDED_FRONTIER.md (decision 2026-07-11)

\textbf{Protocol and statistics.} Every model-based comparison uses paired
seeds (identical seeding before each arm), so per-seed differences cancel
shared seed noise. With $n = 6$ paired seeds the two-sided Wilcoxon signed-rank
$p$ floors at $0.03125$, attained exactly when all six seeds share a sign; we
therefore read effects from the mean $\Delta$, its $t$-interval, and
dose-response monotonicity, and treat $p = 0.031$ only as a sign-unanimity
certificate. Suites with $n = 5$ seeds cannot reach $p < 0.05$ (floor 0.0625)
and are reported as effect-versus-noise. Data-level before/after deltas on
grid metrics carry paired unit-level bootstrap intervals ($B = 1{,}000$,
percentile); units are spatially correlated and demographics are
district-constant, so these intervals are first-order.
% src: PAPER/argument/04_evaluation.md §3; PAPER/external-metrics/FINDINGS.md §1, §5

\textbf{Metric firewall.} We separate three rings of metrics and label every
claim accordingly: (i) \emph{optimized} --- $F_{\mathrm{spatial}}$,
$F_{\mathrm{causal}}$, $F_{\mathrm{fidelity}}$, the quantities the editor's
gradient sees; (ii) \emph{design-targeted but not optimized} --- the
supply/demand ratio family ($\mathrm{mean}(Y)$ by group), which the lift
mechanism was aimed at, so improvement there is confirmatory; and (iii)
\emph{genuinely external} --- demographic parity, disparate impact, the Theil
index, per-group service levels, and the distinct-taxi supply recount, none of
which appear in the objective. ``Improves metrics it never optimizes'' claims
ride ring (iii) only.
% src: PAPER/supply-lift/LIFT_ALGORITHM_REFERENCE.md §13

\textbf{External fairness instruments.} On the service ratio $Y$ over active
units, grouped per equity axis (housing price, compensation, migrant share; two
grouping conventions --- district extremes and median split): the two group
\emph{levels} $\mathrm{mean}(Y \mid \mathrm{disadvantaged})$ and
$\mathrm{mean}(Y \mid \mathrm{advantaged})$; \emph{demographic parity} (DP), the
signed gap between them (on a continuous outcome DP \emph{is} the
supply/demand-ratio gap --- one number, reported once); \emph{disparate impact}
(DI), their ratio (1 = parity); and the between-region \emph{Theil} index
(grouping-independent). The honest count of distinct external instruments is
therefore \{DI, DP/gap, Theil\} plus the descriptive levels.
% src: PAPER/external-metrics/FINDINGS.md §1, §4.3 (DP≡gap disclosure)

\subsection{Data-Level Fairness (Shenzhen)}\label{sec:exp-data-level}

\textbf{Optimized metrics.} Editing raises $F_{\mathrm{causal}}$ from 0.7988
to 0.8214 ($+0.0226$) and $F_{\mathrm{spatial}}$ from 0.1034 to 0.1095
($+0.0061$) --- ring (i) numbers, reported for completeness rather than as
evidence.
% src: famail_temporal/results/2026-07-10T02-06-37_alpha_sweep_s10_c80_f10_filtered/metrics.json

\textbf{External metrics (ring iii).} Every established instrument moves
toward fairness with bootstrap intervals excluding zero
(Table~\ref{tab:external-sz}, migrant axis, district extremes; the remaining
axis $\times$ grouping cells behave the same way). The gap closes from both
ends: the advantaged group's over-service falls ($21.27 \to 20.44$) while the
disadvantaged group's service \emph{rises} ($7.073 \to 7.126$).
% src: famail_temporal/baselines/external_fairness/results/shenzhen-primary-supplylift-s10/external_fairness.json

\begin{table}[t]
\caption{Shenzhen external fairness, before $\to$ after editing (migrant axis,
district extremes; paired bootstrap, $B{=}1{,}000$).}
\label{tab:external-sz}
\begin{tabular}{lrrr}
\toprule
metric & before & after & $\Delta$ [95\% CI] \\
\midrule
Disparate impact ($\uparrow$) & 0.3325 & 0.3487 & $+0.0162$ [$+0.0136, +0.0189$] \\
DP gap ($\downarrow$) & 14.199 & 13.309 & $-0.890$ [$-0.992, -0.785$] \\
Theil ($\downarrow$) & 0.1550 & 0.1463 & $-0.0087$ [$-0.0097, -0.0076$] \\
$\mathrm{mean}(Y \mid \mathrm{disadv.})$ ($\uparrow$) & 7.0734 & 7.1263 & $+0.0529$ [$+0.0086, +0.0989$] \\
$\mathrm{mean}(Y \mid \mathrm{adv.})$ & 21.272 & 20.435 & $-0.837$ \\
\bottomrule
\end{tabular}
\end{table}
% src: famail_temporal/baselines/external_fairness/results/shenzhen-primary-supplylift-s10/external_fairness.json
% (before-edit values are alpha-independent; identical cleaned base data)

\textbf{The lift-up claim rides the supply channel.} The
$\mathrm{mean}(Y \mid \mathrm{disadv.})$ rise is a ring-(ii)
\emph{design-targeted} quantity --- the lift mechanism was aimed at it --- so
we decompose it rather than celebrate it. Evaluating $Y$ at
$(S,D) \to (S,D') \to (S',D')$ splits the delta exactly into a demand move and
a supply move (Table~\ref{tab:channels}). The demand channel (pickup
relocation) carries large variance and is not significant; the supply channel
(rerouted tails adding presence) is significant under \emph{both} accounting
conventions --- tier-1 (the optimizer's fractional-presence convention) and
tier-2, which re-counts supply from raw GPS as distinct taxis per
neighborhood-hour with the edited tails substituted. The recount machinery
reproduces the production supply grid exactly (MAE 0.0) with 100\% history
matching, so tier-2 is the honest semantics, not an approximation.
% src: <s10>/channel_decomposition.json + <s10>/supply_recount.json
% src (recount validity properties): PAPER/supply-lift/FINDINGS.md §4 (G2, machinery unchanged)

\begin{table}[t]
\caption{Channel decomposition of $\Delta\,\mathrm{mean}(Y \mid \mathrm{disadv.})$,
Shenzhen migrant axis ($B{=}2{,}000$).}
\label{tab:channels}
\begin{tabular}{lrrc}
\toprule
channel & $\Delta$ & 95\% CI & sig.? \\
\midrule
Supply (tier-1) & $+0.0176$ & [$+0.0128, +0.0225$] & \textbf{yes} \\ % lint-allow: coincidental CI bound (s10 tier-1 interval, not the trim-only headline delta)
Supply (tier-2, distinct-taxi) & $+0.0411$ & [$+0.0346, +0.0479$] & \textbf{yes} \\
Demand (pickup relocation) & $+0.0352$ & [$-0.0095, +0.0814$] & no \\
Total & $+0.0529$ & [$+0.0086, +0.0989$] & yes \\
\bottomrule
\end{tabular}
\end{table}
% src: famail_temporal/results/2026-07-10T02-06-37_alpha_sweep_s10_c80_f10_filtered/channel_decomposition.json

\textbf{Realism.} Fidelity-A stability and lift's expected Fidelity-B
distributional cost (whole-tail relocation shifts trajectory-shape statistics
more than a pickup-only move) are re-measured on the adopted corpus:
Fidelity-A 0.844 vs.\ raw 0.848; corpus-level Fidelity-B 0.187, concentrated
in exactly the components a rerouted tail must move --- terminal cell
(0.635), net displacement (0.147), coverage (0.142) --- while trajectory
length, which neither edit mode alters, barely registers (0.060) --- a
by-design trade-off we disclose rather than net out.
% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json (edited row + fidelity_b_per_component)

\textbf{Provenance disclosures.} About 5\% of trim edits (118/2{,}455) had no
king-compliant repair and were reverted post hoc; the rule was adopted before
its metric effects were computed, and it moved every metric favorably --- the
infeasible edits were actively counterproductive. The pre-build oracle gate
bounded the supply-channel headroom at $+0.786$ on
$\mathrm{mean}(Y \mid \mathrm{disadv.})$ --- $2.6\times$ the pre-registered
go/no-go threshold of $+0.3$ (full accounting $+0.882$); the realized $+0.053$
sits far below the unconstrained ceiling, as a realism-bounded editor should.
% src: PAPER/supply-lift/FINDINGS.md §8 (skip-on-infeasible); §1.3 + data/oracle.json (G0 ceilings)

\subsection{Ablation: Trim-Only versus Trim+Lift}\label{sec:exp-ablation}

Because trim's optimization is frozen inside the combined editor
(\S\ref{sec:editor}), running the editor with the lift phase disabled
reproduces the demand-only editor exactly, and every difference in
Table~\ref{tab:ablation} is attributable to the lift mechanism. The ablation
certifies the design claim of \S\ref{sec:leveling}: a demand-only editor
improves fairness metrics while leaving the under-served group's absolute
service untouched --- leveling down --- and the supply channel is what changes
that.

\begin{table}[t]
\caption{Trim-only versus trim+lift at matched budgets. CI = paired-bootstrap
95\% interval; n.s. = interval includes zero.}
\label{tab:ablation}
\begin{tabular}{lrr}
\toprule
 & trim-only & trim+lift \\
\midrule
\multicolumn{3}{l}{\emph{Shenzhen (primary)}} \\
$\Delta F_{\mathrm{causal}}$ & $+0.0146$ & $\mathbf{+0.0226}$ \\ % lint-allow: ablation
$\Delta F_{\mathrm{spatial}}$ & $-0.0011$ & $\mathbf{+0.0061}$ \\ % lint-allow: ablation
$\Delta\,\mathrm{mean}(Y \mid \mathrm{disadv.})$ & $+0.000$ (flat) & $\mathbf{+0.053}$ (CI excl.\ 0) \\ % lint-allow: ablation — trim-only level 7.0734->7.0734 exactly
\multicolumn{3}{l}{\emph{San Francisco (external validity)}} \\
$\Delta F_{\mathrm{causal}}$ & $+0.0144$ & $\mathbf{+0.0316}$ \\ % lint-allow: ablation
Migrant DP gap & $-0.052$ (CI excl.\ 0) & $\mathbf{-0.073}$ (CI excl.\ 0) \\ % lint-allow: ablation — both sig at alpha*; contrast is magnitude + the supply channel
\bottomrule
\end{tabular}
\end{table}
% src (trim+lift SZ col): famail_temporal/results/2026-07-10T02-06-37_alpha_sweep_s10_c80_f10_filtered/
% metrics.json + channel_decomposition.json (mean(Y|D) total +0.0529 CI [+0.0086,+0.0989])
% NOTE(re-anchor): both columns at alpha*=(0.1,0.8,0.1); trim-only baselines re-run at alpha* (R2)
% so the ablation isolates the lift mechanism at the adopted weights, not a weight change.

Three rows carry the qualitative story, and the structural expectation from
\S\ref{sec:leveling} re-verifies exactly at the adopted weights: under
trim-only, every external instrument still improves significantly (DP
$-0.611$, DI $+0.0098$, Theil $-0.0059$, all CIs excluding zero) --- but the
disadvantaged group's service level is unchanged to four decimals
($7.0734 \to 7.0734$) while the advantaged level falls; the improvement is
pure leveling-down. Under trim+lift the same level rises with a bootstrap
interval excluding zero.
% src: famail_temporal/baselines/external_fairness/results/shenzhen-trimonly-a10/external_fairness.json
The secondary spatial metric flips from a small cost to a gain.
Per Table~\ref{tab:ablation}, the lift mechanism is not an incremental tweak
to the demand-only editor but the difference between redistributing service
and providing it.
% src: PAPER/supply-lift/FINDINGS.md §3 (the key qualitative change paragraph)

\subsection{Downstream Propagation (Shenzhen)}\label{sec:exp-downstream}

\textbf{Data quality (four sources).} Table~\ref{tab:l1} scores the raw,
edited, BC-generated, and GAN-generated corpora on fairness and both fidelity
axes, with generators trained from scratch for this comparison and Fidelity-A
gated by the real-anchored identity check
(\S\ref{sec:objective}). The claim under test: the edited corpus is the
fairest source that remains faithful on both realism axes.

\begin{table}[t]
\caption{Data-quality comparison, four sources (Shenzhen; means over five
seeds; the GAN row's Fidelity-B is seed-bimodal --- see text).}
\label{tab:l1}
\begin{tabular}{lrrr}
\toprule
source & $F_{\mathrm{causal}}$ & Fid.-A ($\uparrow$) & Fid.-B ($\downarrow$) \\
\midrule
raw & 0.7988 & 0.848 & 0.000 \\
\textbf{edited (trim+lift)} & \textbf{0.8214} & 0.844 & 0.187 \\
bc-generated & 0.7980 & 0.848 & 0.011 \\
gan-generated & 0.8089 & 0.848 & 0.171 \\
\bottomrule
\end{tabular}
\end{table}
% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json

The claim holds: the edited corpus is the fairest source and
identity-faithful (Fidelity-A 0.844 $\approx$ raw 0.848; gate passed,
matched 0.848 vs.\ mismatched 0.193), and its Fidelity-B is the bounded-edit
divergence quantified in \S\ref{sec:exp-data-level}. One instability we
surface rather than smooth: the Shenzhen GAN's Fidelity-B mean of 0.171
hides a seed-bimodal split ($\pm 0.129$; three of five seeds at
0.20--0.30, two at 0.03--0.04), and its worst seeds fail broadband ---
length-component divergence 0.247 where the edited corpus sits at 0.060.
The collapse mode Shenzhen generators have shown before returns in some
seeds here, while the SF GAN stays uniformly healthy (\S\ref{sec:exp-sf}).
% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json (gan fidelity_b values + per-component)

\textbf{Vanilla training averages the edit away.} A driver-conditioned BC
policy trained on the edited corpus at uniform weight is not fairer than one
trained on raw: paired $\Delta F_{\mathrm{causal}} = +0.0022$ ($n = 6$,
mixed signs, $p = .16$, n.s.) --- the null that motivates the upweighting
recipe of \S\ref{sec:downstream}.
% src: famail_temporal/results/weighted_bc_sweep/alpha_sweep_s10_c80_f10_filtered_6seed/paired_stats.json (edited arm, uniform weight)

\textbf{Upweighting recovers it, edit-specifically.} With the edited
demonstrations upweighted, the paired gain is monotone in the weight and
sign-unanimous: $+0.0217$ (w10), $+0.0267$ (w20), $+0.0302$ (w30), each 6/6
at the $p = .031$ floor, $t$-interval at w30 $[+0.0264, +0.0340]$. Neither
control reproduces it: the random-subset placebo is null-to-negative
($-0.0014$ w10, $-0.0023$ w30, both n.s.), and most-fair selection reaches
$+0.0033$ at w10 --- significant, but under a sixth of the edited arm's
gain --- then fades to n.s.\ as the dose rises ($+0.0017$ w20, $+0.0009$
w30) while the edited arms keep growing. $F_{\mathrm{spatial}}$ propagates
on Shenzhen: already $+0.0008$ ($p = .031$) at uniform weight, rising to
$+0.0038$/$+0.0040$/$+0.0052$ across w10/w20/w30 (each $p = .031$), while
both controls significantly \emph{degrade} it at w30 ($-0.0012$ random,
$-0.0027$ most-fair). Fidelity-A is unchanged at every weight
($|\Delta| \le +0.0007$, all n.s.); Fidelity-B rises dose-monotonically
($0.020 \to 0.027$ absolute vs.\ raw $0.013$) --- the expected signature of
deliberately shifting population statistics toward the edited slice,
disclosed rather than netted out.
% src: famail_temporal/results/weighted_bc_sweep/alpha_sweep_s10_c80_f10_filtered_6seed/paired_stats.json + dose_response.json

\textbf{Model-level variance.} A paired baseline-vs-FAMAIL comparison at
uniform weight gives $\Delta F_{\mathrm{causal}} = +0.0031 \pm 0.0022$
($n = 5$; below the Wilcoxon significance floor and read as
effect-versus-noise per \S\ref{sec:exp-setup}) --- consistent with the
vanilla-transfer null: without the weighting lever, the training procedure
alone does not reliably transmit the edit.
% src: famail_temporal/results/variance_suite/supply_lift_shz_5seed/ (paired per-seed diffs; all five positive but n=5)

\textbf{The honest boundary: allocation behavior.} Metric propagation is not
the whole story. Rolling out the trained policies and measuring the share of
pickups they allocate to disadvantaged areas, the prior trim+lift corpus
showed the share still tilting \emph{away} --- attenuated roughly $40\%$
versus the demand-only corpus, but not reversed --- and policies did not
learn to \emph{cruise} disadvantaged areas (seeking-state shares n.s.). This
suite re-run on the adopted corpus gives the same boundary, slightly less
attenuated: $-0.0033$ at w30 (0/6 seeds, $p = .031$) against the demand-only
corpus's $-0.0048$ --- roughly $31\%$ attenuation, still not a reversal ---
with seeking-state shares n.s.\ at every weight.
% src: famail_temporal/baselines/external_fairness/results/option_a_rollout_a10/summary.json (edited_w30.MigrantRatio.pickups.share_D_delta; states n.s.)
We claim the data-level lift-up and the metric propagation; we disclose the
allocation boundary, which motivates training-side allocation constraints as
future work rather than further data-side editing alone.
% src: PAPER/supply-lift/data/rollout_supplylift_summary.json + rollout_trimonly_prior_summary.json; FINDINGS.md §9

\subsection{Data-Augmentation Baselines}\label{sec:exp-baselines}

None of the baselines below optimizes a fairness objective; they are
\emph{editing-quality} baselines that test whether FAMAIL's gains come from
its objective or merely from bounded perturbation (or corpus resampling)
per se. Fairness is accordingly \emph{expected not to improve} under the
perturbation arms.
% src: PAPER/baselines/README.md (Meeting-41 framing)

\textbf{Perturbation arms.} Three arms attack the same trajectories the
headline edit selected, at the same $\varepsilon = 2$: iterative and
single-step signed-gradient attacks on the frozen identity discriminator, and
a seeded random-jitter placebo. A naming note: the gradient arms are
\emph{iFGSM/FGSM with random restart} (PGD-style random initialization within
the $\varepsilon$ ball). We had pre-registered $\delta = 0$-initialized
ablation arms expecting a no-op --- a difference-mode Siamese head has zero
subgradient at an identical pair --- but the deployed discriminator compares
embeddings by \emph{concatenation}, which is not stationary there: measured,
the $\delta = 0$ arms attack rather than stall (the iterative arm lands
within $0.0002$ of its random-start twin; the single-step arm differs with
initialization, as a one-step method must). The stationarity assumption held
only for the head we did not deploy, and the random-start arms are the
reported ones.
% src: famail_temporal/results/2026-07-13T06-26-19/…06-27-50 arm metrics (nors); deployed checkpoint combination_mode="concatenation" (fidelity/model.py); gradient-at-zero verified live 2026-07-13
Results: Table~\ref{tab:baselines}.

The arms behave as fidelity baselines, with one result stated against the
pre-registered expectation: random jitter \emph{does} raise
$F_{\mathrm{causal}}$ ($+0.0135$) --- indiscriminately diffusing the selected
trajectories diffuses their over-service too --- but it does so by breaking
the trajectories: 98.8\% of its perturbed trajectories violate king-move
adjacency and its distributional divergence reaches 0.447, against the
edited corpus's disclosed 0.187 (iFGSM: 54.4\%, 0.143; FGSM: 91.4\%,
0.339; FAMAIL edits are king-compliant by construction). Identity fidelity
survives every arm ($\approx 0.846$, gate passed) --- at $\varepsilon = 2$
the perturbations are too small to break driver identity, which is exactly
why identity fidelity alone cannot rank editing quality here and the
adjacency and distributional axes carry the comparison.
% src: famail_temporal/baselines/baseline_table/baseline_table.json (Fid-A/Fid-B(JS)/adjacency-violation columns)

\textbf{Demographic oversampling.} The naive lifting-up baseline: duplicate
real trajectories originating in demographically disadvantaged regions (all
three equity axes, phantom driver IDs, rigid $\pm 1$-cell jitter), rebuilding
demand \emph{and} supply additively, at doses matched to FAMAIL's budget. At
the matched dose $d = 10{,}000$, targeted oversampling improves
$F_{\mathrm{causal}}$ by $+0.0153$ (dose-monotone:
$+0.0059 \to +0.0097 \to +0.0153$) --- but it fabricates 10.5\% of the corpus
to get there, where FAMAIL redistributes observed behavior at zero inflation
and reaches $+0.0226$. The untargeted placebo (same fabrication, no targeting)
\emph{degrades} fairness ($-0.0172$), so the gain is targeting-specific; and
the placebo's DP gap explodes ($+2.8$) because fabricated supply lands
predominantly in already-advantaged cells --- a measured demonstration that
ratio metrics can be gamed by fabrication, the demand-endogeneity concern of
\S\ref{sec:leveling} made concrete. Two disclosures: the corpus cannot even
supply the matched dose honestly (only 8{,}241 distinct disadvantaged-origin
trajectories exist, so 1{,}759 draws are re-duplications), and fidelity is not
scored for this arm --- duplicates of real trajectories pass any realism check
by construction.
% src: PAPER/baselines/demographic-oversampling/FINDINGS.md §2-5

\begin{table}[t]
\caption{Cross-arm comparison at matched budgets (Shenzhen). Perturbation-arm
rows land with campaign stage Q1.}
\label{tab:baselines}
\begin{tabular}{lrrr}
\toprule
arm & $\Delta F_{\mathrm{causal}}$ & $\Delta F_{\mathrm{spatial}}$ & inflation \\
\midrule
raw (reference) & --- & --- & 0\% \\
\textbf{FAMAIL trim+lift} & $\mathbf{+0.0226}$ & $+0.0061$ & 0\% \\
iFGSM (rand.\ restart) & $-0.0057$ & $+0.0126$ & 0\% \\
FGSM (rand.\ restart) & $+0.0017$ & $+0.0137$ & 0\% \\
random jitter & $+0.0135$ & $+0.0132$ & 0\% \\
demog.\ oversampling (targeted) & $+0.0153$ & $+0.0052$ & 10.5\% \\
\quad placebo (untargeted) & $-0.0172$ & $+0.0138$ & 10.5\% \\
\bottomrule
\end{tabular}
\end{table}
% src: PAPER/supply-lift/FINDINGS.md §3 (FAMAIL row); famail_temporal/baselines/baseline_table/baseline_table.json (perturbation rows);
% src: oversampling cells = 3-seed means over *_d10000_s{0,1,2} metrics.json deltas (matches the committed FINDINGS aggregation; the assembler's seed-0-only d10000 row reads +0.0175)

\subsection{Robustness and Sensitivity}\label{sec:exp-robustness}

\textbf{Weight sensitivity.} Fig.~\ref{fig:alpha-pareto} reports the weight
sweep: six full editing runs across the simplex, each scored on all three
metric rings. The optimized $\Delta F_{\mathrm{causal}}$ is flat (within
$0.001$) for $\alpha_{\mathrm{sp}} \in [0, 0.55]$, while the ring-(ii)
supply-channel lift-up declines monotonically with $\alpha_{\mathrm{sp}}$ ---
significant on both tiers only for $\alpha_{\mathrm{sp}} \le 0.2$ --- and
ring-(iii) instruments improve significantly at every point. The adopted
$(0.1, 0.8, 0.1)$ is the three-ring criterion's selection; the sweep shows
the choice is robust on the primary axis and load-bearing only for the
lift-up property.
% src: PAPER/objective-motivation/weight-sensitivity/EXTENDED_FRONTIER.md (full table + criterion)

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/extended_frontier.pdf}
  \caption{The weight sweep, scored beyond the optimized metrics. (A) On the
  optimized plane, $\Delta F_{\mathrm{causal}}$ is flat across
  $\alpha_{\mathrm{sp}} \in [0, 0.55]$. (B) The design-targeted lift-up
  declines monotonically with spatial weight and is significant on both
  supply tiers only for $\alpha_{\mathrm{sp}} \le 0.2$ (filled = 95\% CI
  excludes 0). The adopted $(0.1, 0.8, 0.1)$ is the three-part criterion's
  selection.}
  % src: PAPER/objective-motivation/weight-sensitivity/extended_frontier.json via paper/figures/make_figures.py
  \label{fig:alpha-pareto}
\end{figure}

\textbf{Demographic-feature-set robustness.} $F_{\mathrm{causal}}$ is
feature-set-specific, so every layer of the argument is re-measured under two
alternate demographic sets (Table~\ref{tab:featsets}). The claim is
directional reproduction, not magnitude equality --- absolute
$F_{\mathrm{causal}}$ scales shift with the feature set by construction.

\begin{table}[t]
\caption{Feature-set robustness under trim+lift (alternate-set columns land
with campaign stages Q6--Q8).}
\label{tab:featsets}
\begin{tabular}{lrrr}
\toprule
 & \{hous,comp,migr\} & \{hous,gdp,comp\} & +logpopdens \\
\midrule
before-edit $F_{\mathrm{causal}}$ & 0.7988 & X.XXXX & X.XXXX \\ % TODO(run:Q6 -> per-set filtered metrics.json)
editor $\Delta F_{\mathrm{causal}}$ & $+0.0226$ & X.XXXX & X.XXXX \\ % TODO(run:Q6 -> per-set filtered metrics.json)
DI $\Delta$ (migrant/extremes) & $+0.0162$ & X.XXXX & X.XXXX \\ % TODO(run:Q7 -> per-set external_fairness.json)
Theil $\Delta$ & $-0.0087$ & X.XXXX & X.XXXX \\ % TODO(run:Q7 -> per-set external_fairness.json)
L1: edited fairest faithful? & see~\ref{tab:l1} & X & X \\ % TODO(run:Q8 -> per-set level1_v2_multiseed.json)
vanilla-BC null (w1) & see~\S\ref{sec:exp-downstream} & X.XXXX & X.XXXX \\ % TODO(run:R4 PRIMARY + Q8 per-set)
weighted-BC w30 & see~\S\ref{sec:exp-downstream} & X.XXXX & X.XXXX \\ % TODO(run:R4 PRIMARY + Q8 per-set)
variance (b0 vs FAMAIL) & see~\S\ref{sec:exp-downstream} & X & X \\ % TODO(run:Q8 -> per-set variance aggregate.json)
\bottomrule
\end{tabular}
\end{table}
% src (PRIMARY column): s10 corpus metrics.json + shenzhen-primary-supplylift-s10 external_fairness.json

\textbf{Filtering is not a substitute.} Removing the $K$ least-fair
trajectories instead of editing them does not reproduce the gain --- it
inverts it: $F_{\mathrm{causal}}$ falls monotonically with removal
(0.7988 $\to$ 0.7935 at the maximum $K = 2{,}455$), while editing the same
attribution-selected slice raises it to 0.8214. On the $F_{\mathrm{causal}}$
objective, neither selecting (\S\ref{sec:exp-downstream}, most-fair control)
nor removing data substitutes for editing it.
% src: famail_temporal/results/analysis/pareto_supplylift/pareto_points.csv (Q7-pareto ledger row)

\subsection{External Validity: San Francisco}\label{sec:exp-sf}

The entire protocol reruns on San Francisco with no algorithm, metric, or
fidelity change; because $F_{\mathrm{causal}}$ is city-specific and
associational (ACS proxies, not hukou), San Francisco tests whether the
\emph{conclusions} reproduce, never whether magnitudes match.

\textbf{Data level.} $F_{\mathrm{causal}}$ rises 0.8752 $\to$ 0.9067
($+0.0316$), $F_{\mathrm{spatial}}$ $+0.0139$, with Fidelity-A stability and % lint-allow: coincidental value (SF a10 F_spatial delta, not the trim-only F_causal headline)
lift's Fidelity-B cost re-measured on this corpus by the downstream suite
(pending below). Every external instrument improves: Theil $-0.0079$
(CI $[-0.0094, -0.0065]$), and the migrant axis is significant under
\emph{both} grouping conventions on the DP gap (district extremes $-0.0729$,
CI $[-0.1132, -0.0259]$; median split $-0.0370$, CI $[-0.0733, -0.0002]$),
with DI significant under district extremes ($+0.0058$,
CI $[+0.0004, +0.0103]$) and n.s.\ under median split --- a grouping
dependence we state rather than hide (the SF sample is 12 drivers).
% src: PAPER/supply-lift/data/a10/sf12_a10_metrics.json + sf12_a10_external_fairness.json

\textbf{The supply channel replicates; the ratio does not.} The supply
channel is again positive and significant ($+0.0209$,
CI $[+0.0122, +0.0300]$) --- tier-1 accounting only, a \emph{lower bound} on
Shenzhen's evidence (the distinct-taxi tier-2 recount is not plumbed for the
SF pipeline). But as on the prior corpus, lift also reroutes \emph{pickups}
into under-served cells, raising recorded demand there: the demand channel is
significantly negative ($-0.0533$, CI $[-0.0798, -0.0298]$) and the total
$\mathrm{mean}(Y \mid \mathrm{disadv.})$ moves net-negative ($-0.0324$, CI
excludes 0). Two readings coexist and we present both. Under the ratio
reading, the SF lifting-up claim does not hold and should be withheld. Under
the external-metrics reading, more rides \emph{served} in under-served areas
is a demand-side parity gain --- DP, DI, and Theil all improve --- and the
falling ratio reflects exactly the demand endogeneity of
\S\ref{sec:leveling}: recorded demand was suppressed, and serving it raises
the denominator.
% TODO(PI-framing): Zhang decision — which SF fairness story leads; both readings kept until then.
% src: PAPER/supply-lift/data/a10/sf12_a10_channel_decomposition.json

\textbf{Downstream.} The recovery reproduces on San Francisco. Vanilla BC is
null ($+0.0026$, 5/6, $p = .0625$); upweighting recovers the fairness with a
monotone, sign-unanimous dose-response --- $+0.0242$ (w10), $+0.0309$ (w20),
$+0.0332$ (w30), each 6/6 at the $p = .031$ floor with $t$-intervals
excluding zero --- and neither control reproduces it (random placebo
null-to-negative, $-0.0065$/$-0.0069$; most-fair select mixed, significantly
\emph{negative} at w10 and n.s.\ above). One city difference stated plainly:
$F_{\mathrm{spatial}}$ does \emph{not} propagate on SF ($-0.0040$ at w30,
n.s.); the primary-city result at the adopted weights is reported in
\S\ref{sec:exp-downstream}.
% src: famail_temporal/results/weighted_bc_sweep/supply_lift_a10_sf12_filtered_6seed/paired_stats.json
The SF model-level variance suite mirrors the primary city:
$\Delta F_{\mathrm{causal}} = -0.0025 \pm 0.0029$ ($n = 5$, mixed signs,
null).
% src: famail_temporal/results/variance_suite/supply_lift_sf12_5seed/ (paired per-seed diffs)
The four-source comparison also reproduces (generators retrained from
scratch; identity gate passed, matched 0.958 vs.\ mismatched 0.034): the
edited corpus is the fairest source ($F_{\mathrm{causal}}$ 0.9067 vs.\ GAN
0.8795, raw 0.8752, BC 0.8744) while identity-faithful (Fidelity-A 0.9581
$\approx$ raw 0.9578; Fidelity-B 0.098, the expected bounded-edit
divergence).
% src: famail_temporal/results/level1_table_v2/supply_lift_sf12_5seed/level1_v2_multiseed.json

\textbf{SF caveat block.} ACS foreign-born share proxies the migrant axis (not
hukou); the 12-driver density-matched subsample is small; 14.9\% of raw
Cabspotting trajectories violate king-move adjacency at source (GPS gaps), so
absolute compliance is unattainable --- the edited corpus is \emph{more}
compliant than raw (87.4\% vs.\ 85.0\%), and the cross-city gate is
edit-relative compliance (100\%); and the GAN-generated source's
distributional collapse is a city-dependent property --- the fresh SF run's
GAN stayed healthy (Fidelity-B 0.039), so any collapse-based cautionary note
applies only to the city where it is measured.
% src: famail_temporal/results/level1_table_v2/supply_lift_sf12_5seed/level1_v2_multiseed.json (gan row)
% src: PAPER/argument/07_limitations.md §5-7; PAPER/supply-lift/FINDINGS.md §10
