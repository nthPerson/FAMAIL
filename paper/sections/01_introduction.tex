\section{Introduction}\label{sec:intro}
% Drafted 2026-07-12 per docs/superpowers/specs/2026-07-12-paper-prose-sections-design.md.
% All Shenzhen numbers are committed s10-corpus values (alpha* = 0.1/0.8/0.1). The SF
% editor delta is hard-coded (not slot-marked): run r1 landed 2026-07-11 and section 4
% already carries the artifact-sourced value; the intro mirrors it.

Urban mobility policies are increasingly learned by imitation: models are
trained to reproduce the trajectories real humans actually
drove \cite{zhang2022cgail}. But trajectories carry more than behavior.
They record the fleet's service allocation --- which neighborhoods drivers
sought passengers in, and which they passed over --- and in real cities
that allocation tracks demographics: housing prices, incomes, migrant
population share. Models that imitate such data faithfully learn the
inequity as competence, and are doomed to re-enact it when deployed, sending
supply where supply already went --- exhibiting the feedback loop 
documented in other data-driven allocation systems 
\cite{ensign2018,lumisaac2016}.

Interventions typically target one end of the pipeline or the other.
In-processing methods regularize the model while leaving the
demonstrations biased \cite{zheng2023}, so the objective and the training
signal pull against each other at every step. Data-generation and
data-rebalancing methods modify the demonstration distribution instead
--- by synthesizing fair data \cite{xu2018fairgan,vanbreugel2021decaf} or
by reweighing and resampling it \cite{kamirancalders2012} --- but
replacing or extensively altering the original data can reduce realism,
introduce distribution shift, and make it difficult to identify the
source of the fairness improvement. Figure~\ref{fig:teaser} illustrates
the resulting gap: the demonstrations are an asset worth preserving, yet
a policy that imitates them unchanged learns the service gap they record.

\begin{figure}[t]
  \centering
  % Teaser (fig:teaser) — TikZ source: figures/figure-1/figure-1.tex (iterate
  % THERE; standalone harness figures/figure-1/figure-1-test.tex; design history
  % figures/figure-1/design-archive/). This figure is the WHY; fig:overview in
  % section 3 is the HOW.
  \input{figures/figure-1/figure-1}
  \caption{Why FATE edits trajectory data. (a)~A real fleet's service
  demonstrates bias: on Shenzhen the advantaged district receives
  $3.0\times$ the taxi service per unit demand.
  % src: PAPER/supply-lift/data/a10/shz_a10_external_fairness.json
  % (MigrantRatio district_extremes disparate_impact.before 0.33252 ->
  % advantaged/disadvantaged service ratio 1/0.33252 = 3.0x)
  (b)~The recorded trajectories are human-generated and therefore valuable
  --- filtering them would discard useful expertise, so FATE performs bounded edits
  on an attributed slice of the corpus containing the most-biased demonstrations.
%   (b)~The recorded trajectories are real human driving --- worth keeping
%   --- and regenerating them would discard that realism, so FATE edits
%   the corpus's most-biased slice, chosen by attribution, in place and
%   within bounded moves, generating nothing.
  % NOTE (2026-07-16): no quantified bias share here by design — k=10,000
  % is an edit budget (~10% of the corpus), not a measured extent of bias.
  (c)~The stakes: a policy imitating the raw corpus learns the service gap
  and sends supply to over-served areas; trained 
  on the upweighted edited demonstrations, the fairness survives training 
  and results in the model learning a fairer policy. The $+$ marks flag what the edit 
  changed.
  Figure~\ref{fig:overview} shows how the editor works.}
  \label{fig:teaser}
\end{figure}

FATE takes a third position. As \emph{data augmentation}, it keeps the real 
corpus, edits a small slice containing the most unfair trajectories, and 
generates nothing.  At most $k$
trajectories are edited ($k = 10{,}000$ on the primary city, about a tenth
of the corpus), each perturbed within a two-cell radius under a frozen 
ST-SiameseNet driver-identity discriminator \cite{ren2020stsiamese}, so realism is inherited from the data
rather than re-learned. The edited demonstrations are then
\emph{upweighted} during imitation --- instance reweighing
\cite{kamirancalders2012} transplanted to demonstrations --- so the
fairness of the small edited slice survives training instead of being averaged
away.

The subtlety is not measuring unfairness but repairing it honestly.
FATE's first editing mode, \emph{trim}, moves only demand: it relocates
recorded pickups within over-served areas, and on the primary city it
improves measured fairness, but without helping the under-served group at all:
every one of the 2{,}455 pickups it edits originates in an advantaged cell
and not one lands in a disadvantaged cell, so the gap closes only by reducing
over-service --- analogous to the classic leveling-down objection to equalization
\cite{parfit1997,mittelstadt2024}, though here service is relocated rather
than removed.
% src: PAPER/external-metrics/LEVELING_DOWN_MECHANISM.md (2,455/2,455 flow analysis; pre-supply-lift doc) — corroborated at alpha*: PAPER/supply-lift/data/a10/shz_trimonly_a10_external_fairness.json (mean(Y|disadv) 7.0734 -> 7.0734 exactly)
We show this behavior is structural --- the constrained optimum of any
demand-only editor on this objective (\S\ref{sec:leveling}) --- and that
diagnosis dictated the second mode. \emph{Lift} reroutes drivers' final
seeking minutes into under-served areas and makes the supply consequences
of the reroute differentiable and endogenous to the objective: the editor
is rewarded for providing service where it improves fairness, not merely
for redistributing demand. Together, trim and lift raise demand-adjusted
demographic fairness on Shenzhen by $+0.0226$
% src: PAPER/supply-lift/data/a10/shz_a10_metrics.json (deltas.f_causal)
and add statistically robust taxi presence to the under-served group, raising its mean service
ratio by $+0.0176$ under the optimizer's fractional-presence accounting
(tier-1) and by $+0.0411$ when supply is recounted as distinct taxis from
raw GPS (tier-2).
% src: PAPER/supply-lift/data/a10/shz_a10_channel_decomposition.json (channels.supply / supply_tier2)
On San Francisco the protocol reruns with no algorithm, metric, or
fidelity change, and the data-level, external-metric, and downstream
conclusions reproduce ($+0.0316$; \S\ref{sec:exp-sf}).
% src: PAPER/supply-lift/data/a10/sf12_a10_metrics.json (run r1)

% Neither edit survives training on its own. The edited slice is a tenth of
% the corpus, and a vanilla behavior-cloning objective averages it away ---
% a genuine null we verify rather than assume. Upweighting the edited
% demonstrations recovers the fairness with a monotone dose-response, and
% two controls pin the recovery to the edit itself: neither upweighting an
% equal-sized random subset nor upweighting the corpus's already-fairest
% trajectories reproduces it. Because a fairness method scored on its own
% objective proves little, evaluation rides on established measures the
% objective never optimizes --- demographic parity, disparate impact
% ($+0.0162$ on the primary city's migrant axis), and the Theil index ---
% computed before and after editing.
Neither edit survives training on its own. The edited slice is a tenth of
the corpus, and a vanilla behavior-cloning objective averages it away ---
a genuine null we verify rather than assume (\S\ref{sec:exp-downstream}). Upweighting the edited
demonstrations recovers the fairness with a monotone dose-response, and
two controls pin the recovery to the edit itself: neither upweighting an
equal-sized random subset nor upweighting the corpus's already-fairest
trajectories reproduces it. Because a fairness method scored on its own
objective proves little, evaluation rides on established measures the
objective never optimizes, computed before and after editing. All three
improve on the primary city, every interval excluding zero: on the migrant
axis, disparate impact $+0.0162$ and the demographic-parity gap $-0.890$
($14.199 \to 13.309$); between regions, the Theil index $-0.0087$
(\S\ref{sec:exp-data-level}).
% src: see §4 external-metrics table tab:external-sz (DI, DP gap, Theil rows);
% famail_temporal/baselines/external_fairness/results/shenzhen-primary-supplylift-s10/external_fairness.json
% (DI + DP gap: metrics.MigrantRatio.district_extremes.{disparate_impact,demographic_parity}.delta;
%  Theil is top-level `theil`.delta -- between-region, NOT axis-specific)
On the primary city, a targeted demographic-oversampling baseline captures
only part of the data-level gain despite fabricating 10.5\% of the corpus,
and its untargeted placebo degrades fairness outright
(\S\ref{sec:exp-baselines}).
% src: PAPER/baselines/demographic-oversampling/FINDINGS.md

\textbf{Contributions.}
\begin{itemize}
\item \textbf{A fairness-aware trajectory editor.} Two bounded editing modes,
gradient-guided under one differentiable objective, with the supply
consequences of edits made endogenous to it, and driven by two exact
attribution mechanisms: demand deficit attribution localizes existing
unfairness, and supply-gradient attribution localizes the value of added
taxi presence.
\item \textbf{A structural diagnosis of demand-only editing.} Demand-only
editing closes fairness gaps only from the top; we show why --- selection,
leverage, and frozen supply --- and show that a differentiable supply
channel is the non-perverse remedy.
\item \textbf{A transfer recipe with edit-specificity controls.}
Upweighted imitation carries the data-level fairness into trained
policies, and random-upweighting and most-fair selection controls isolate
the edit as the cause.
\item \textbf{Two-city validation on never-optimized measures.} The full
protocol runs on Shenzhen and San Francisco and is scored on external
fairness measures the objective never optimizes, against a
demographic-oversampling baseline on the primary city.
\end{itemize}
