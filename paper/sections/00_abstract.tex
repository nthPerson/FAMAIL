% Abstract body only (no \begin{abstract} wrapper) so that both main.tex and the
% standalone title+abstract PDF (kdd27-abstract-only.tex) share one source and
% cannot drift. Edit the abstract HERE, nowhere else.
% DRAFT (2026-07-11) — pending final results; numbers below are committed headline
% values. % src: PAPER/supply-lift/data/a10/ (alpha* headline bundle);
% PAPER/baselines/demographic-oversampling/FINDINGS.md
% Rewritten 2026-07-24 per the Meeting-44 mandate (problem -> challenge -> approach
% shape; self-explanatory language; "in-processing" and "distribution shift" jargon
% glossed or removed). Pre-rewrite version: paper-pre-rewrite-2026-07-23/.
% Adopted from the PI's revision draft 2026-07-24 (late), restructure T1: her abstract
% near-verbatim, with two pre-authorized claim repairs. (1) Fidelity is scored in the
% editing objective by a frozen driver-identity discriminator, not enforced as a
% per-edit constraint (ALGORITHM_FACTS "Validity", divergence D15); her
% "behavioral-fidelity constraints" is reworded accordingly. (2) "estimated" added to
% "greatest ... aggregate fairness impact" because selection is attribution- and
% screen-guided, not provably optimal. Her text states no numbers, so the numeric src
% pointers above are historical; the superseded M44 rewrite is in git history.
% 2026-07-24 (restructure T9, ⚖ Robert's ruling): the evaluation sentence names both
% cities. Her text said only "real-world HSTD"; the two-city naming is restored as the
% smallest possible edit ("... from Shenzhen and San Francisco"), nothing else touched.
% 2026-07-25 (Robert's systematic re-read, prose pass): four wording repairs, no claim
% changed. (1) "applies bounded local edits WITH the greatest ... impact" -> "TO THOSE
% with", matching \S1: selection ranks trajectories, not edits. (2) "service-provision
% process" -> "resource-provision process", the term \S1 and contribution 2 use.
% (3) the dilution sentence pair de-hedged and de-duplicated: the budget is no longer
% stated a third time, and "can be diluted" -> the mechanism we actually measure
% (uniform weighting averages the signal away; the \S4.3 vanilla null). (4) closing
% sentence: "further preserves" -> "carries ... into", matching \S6's verb, with
% "demonstrating" dropped next to two uses of "demonstrations".
Human-generated spatial-temporal data (HSTD), such as taxi trajectories,
passenger trip records, and gig-worker traces, encode not only human
decision-making strategies but also demographic disparities in how services
are distributed across groups and regions. When such data is used for mobility
modeling, simulation, and decision-making, these disparities can be learned and
propagated to downstream applications. Existing approaches either modify large
portions of the data without tracing corpus-level disparity to specific
trajectories, or constrain downstream models while leaving the biased
demonstrations unchanged. We introduce FATE, a Fairness-Aware Trajectory
Editing framework for budgeted fairness intervention in HSTD. Under a limited
editing budget, FATE attributes corpus-level disparity to influential
trajectories and applies bounded local edits to those with the greatest
estimated aggregate fairness impact. It distinguishes outcome-side edits, which
alter measured allocation statistics, from resource-aware edits, which modify
the underlying resource-provision process and direct additional resources
toward under-served areas. All edits satisfy spatial and continuity constraints, and a
frozen driver-identity discriminator scores behavioral fidelity in the editing
objective. Because the edited demonstrations are only a small fraction of the
corpus, training under uniform weights averages their fairness signal away, so
FATE upweights them during downstream training. We instantiate FATE for
taxi mobility and evaluate it on real-world HSTD from Shenzhen and San
Francisco. Results show that these edits improve multiple established fairness
measures the objective never optimizes, and increase
distinct-taxi presence in under-served areas. Edit-aware weighting then
carries these gains into learned policies, on 12 of 12 paired seeds in both
cities ($p = .00049$): sparse, targeted trajectory intervention can improve
collective service fairness with limited data modification.
% src: PAPER/supply-lift/data/a10/{shz,sf12}_a10_wbc_n12_pooled_stats.json
% (WBC-N12 + SF-WBC-N12 2026-07-18: w30 flagship, 12/12 positive in each city,
% exact Wilcoxon p = .00049 = 2/4096, the one comparison that survives
% multiple-comparison correction — the same number \S4.3, \S4.6 and \S6 carry.)
% 2026-07-25 (review finding #19, Robert approved): her abstract stated no
% quantitative result. This is the two-city-safe anchor; the 3.0x disparity is
% NOT (Shenzhen district extremes 3.01x, San Francisco 1.41x), and "about a
% tenth of the corpus" is not either (SZ k=10,000/95,297 = 10.5%, SF
% k=2,000/10,887 = 18.4%) — both would have been false as two-city claims.
