\section{Derivations}\label{app:derivations}

This appendix carries the closed-form details of $F_{\mathrm{demo}}$ deferred
from \S\ref{sec:objective}. With $X$ the z-scored demographic matrix,
$\tilde{X} = [\mathbf{1}\; X]$ its design matrix,
$H = \tilde{X}(\tilde{X}^{\top}\tilde{X})^{-1}\tilde{X}^{\top}$ the \emph{hat
matrix} of the stage-two regression (the projection onto the column space of
$\tilde{X}$ \cite{hoaglinwelsch1978}), and $M$ the centering matrix,
Eq.~\eqref{eq:fdemo} evaluates in closed form as
\begin{equation}\label{eq:fdemo-closed}
F_{\mathrm{demo}}
\;=\; \frac{R^{\top}(I-H)\,R}{R^{\top}M\,R}
\;=\; 1 - r^{2}_{\mathrm{demo}} ,
\end{equation}
the stage-two regression evaluated without ever fitting it explicitly: the
numerator is its residual sum of squares, the denominator its total.
% (H-matrix construction moved here from §3.2 on 2026-07-24, Meeting-44 mandate)
This projection form is what
makes the measure usable as an \emph{optimization objective} rather than a
one-shot audit. Demographics are fixed attributes of the cells, so $H$ is a
constant of the optimization: as editing moves $R$, Eq.~\eqref{eq:fdemo-closed}
implicitly re-fits the demographic regression exactly at every step, with no
coefficients to refresh and nothing trained inside the editing loop. Nor is the
$N \times N$ projection ever materialized: by the normal equations,
$R^{\top}(I-H)R = R^{\top}R -
(\tilde{X}^{\top}R)^{\top}(\tilde{X}^{\top}\tilde{X})^{-1}(\tilde{X}^{\top}R)$,
so each evaluation costs a few length-$N$ inner products.
% src: objective_function/causal_fairness/f_causal_term_reformulation/chosen_formulation/OPTION_B_FORMULATION.md §3.3, §5 (constant H; implicit exact re-fit; differentiability)
% src: famail_temporal/fairness/hat_matrices.py (compact evaluation identity; dense N×N infeasible at production N)
By the Frisch--Waugh--Lovell theorem \cite{frischwaugh1933,lovell1963}, the
residualize-then-project construction recovers exactly the demographic
coefficients of the full multiple regression, so $r^{2}_{\mathrm{demo}}$ is
interpretable as the share of demand-adjusted service inequity attributable to
demographics; measuring fairness through the statistical dependence between
protected attributes and outcomes follows the predictability logic of
\cite{feldman2015}, with the direction reversed: there, disparate impact is
certified by predicting the protected attribute from the data; here, the
demographics predict the adjusted service residual.

\textbf{Per-unit deficit partition.} Because $M$ and $I-H$ are idempotent,
the explained share $r^{2}_{\mathrm{demo}}$ of Eq.~\eqref{eq:fdemo-closed}
admits the exact per-unit decomposition
\begin{equation}\label{eq:unit-attr}
r^{2}_{\mathrm{demo}}
\;=\; \sum_i \frac{(MR)_i^{2} - \bigl((I-H)R\bigr)_i^{2}}{R^{\top}MR},
\end{equation}
a mathematically exact partition of the fairness deficit across active
units; a signed variant separates over- from under-served units. This is
the demand deficit attribution that drives trim (\S\ref{sec:attribution}).
% 2026-07-24 (T9): "demand-deficit" -> "demand deficit", the locked mechanism name
% (paper/README.md convention 10; \S3.2 and every other occurrence are unhyphenated).
% src: PAPER/argument/03_fairness_theory.md (per-cell attribution)

\textbf{Supply-gradient closed form.} The $F_{\mathrm{demo}}$ component of
the value-of-presence map of \S\ref{sec:attribution} has the closed form
\begin{equation}\label{eq:supply-grad}
\frac{\partial F_{\mathrm{demo}}}{\partial S_i}
\;=\; \frac{2}{R^{\top}MR}\;
\frac{\bigl((I-H)R\bigr)_i - F_{\mathrm{demo}}\,(MR)_i}{\max(D_i,\, d_0)},
\end{equation}
against which the automatic gradient is verified.
% src: PAPER/supply-lift/LIFT_ALGORITHM_REFERENCE.md §4.1 (autograd-verified closed form)

\textbf{Spatial-equity closed form.} The $F_{\mathrm{spatial}}$ term of
\S\ref{sec:objective} scores the inequality of two supply-normalized
service rates with the Gini index
\cite{su2018taxigini,horchergraham2021,karner2024}, an inequality measure
widely used in transportation equity research. The rates are
$\mathrm{DSR}_i = D_i / S_i$, the departure-service ratio, and
$\mathrm{ASR}_i = A_i / S_i$, the arrival-service ratio, with $A_i$ the
unit's recorded dropoffs and $D_i$, $S_i$ as in \S\ref{sec:problem}. The Gini enters as a pairwise
mean absolute difference, differentiable almost everywhere and hence usable
inside the gradient objective:
\begin{equation}\label{eq:fspatial}
\begin{split}
\mathrm{Gini}(x) &\;=\; \frac{\sum_{i}\sum_{j}\,\lvert x_i - x_j\rvert}
{2N^{2}\,\bar{x}},\\
F_{\mathrm{spatial}} &\;=\; 1 - \tfrac{1}{2}\bigl(\mathrm{Gini}(\mathrm{DSR})
+ \mathrm{Gini}(\mathrm{ASR})\bigr).
\end{split}
\end{equation}
% src: PAPER/argument/03_fairness_theory.md (F_spatial definition)
% src: famail_temporal/fairness/spatial.py (DSR/ASR ratios; sorted-identity evaluation, no N² materialization)
% NOTE (2026-07-24): DSR expansion now follows su2018taxigini ("departure
% service rate" — the formulation's source, Robert); code and
% RESEARCHER_HANDOFF.md §4 keep the legacy expansion "demand-service ratio"
% (same formula D_i/S_i) — mapping in paper/README.md conventions.

\section{Editor Implementation Details}\label{app:editor}

\textbf{Grid configuration.} Both cities discretize space into
$0.01^{\circ}$ grid cells and hourly time blocks (\S\ref{sec:problem}):
a $48{\times}90$ cell grid on Shenzhen and a $32{\times}30$ grid on San
Francisco. The resolution is an implementation choice; the formulation
requires only a discrete partition of space and time. Preprocessing the
Shenzhen source data also removed 106{,}677 pickups that had almost no
matching drop-offs, which are stuck GPS records rather than real demand.
% src: PAPER/argument/02_datasets.md (0.01° grid, hourly blocks, per-city dims;
% cleanup counts)
% (2026-07-25, review finding #16 page budget: the stuck-GPS cleanup sentence
% MOVED here verbatim-in-substance from \S\ref{sec:exp-setup} with its src
% pointer. The disclosure survives; only its location changed.)
% (moved here from §3.1/§4.1 on 2026-07-24 — Meeting-44 [1:29:27]/[1:37:28]:
% resolution to appendix, defined once)

\textbf{Soft cell assignment.} The discrete grid is bridged by
tem\-per\-a\-ture-annealed soft cell assignment
\cite{jang2017gumbel,maddison2017concrete,bengio2013ste} over the same
$5{\times}5$ window used to aggregate supply, moving from broad early
exploration to precise late assignment; the best iterate is kept.

\textbf{Editor constants.} In lift, the pickup moves by the full offset
$\delta$ and earlier tail states move by linearly tapered fractions
$w_j = j/\ell$ ($0.25/0.5/0.75/1.0$ at $\ell = 4$), with the anchor state
before the tail fixed. Each moved seeking state contributes $1/12$ of an hourly
presence unit to each cell of its $5{\times}5$ neighborhood in the supply
perturbation $\Delta S$, and the objective evaluates supply as
$\mathrm{clamp}(S + \Delta S,\, s_0)$ with supply floor $s_0 = 0.1$.
% src: PAPER/supply-lift/LIFT_ALGORITHM_REFERENCE.md §3, §5-6 (taper, presence mass, floors, live fidelity tail)

\textbf{King-move repair.} The source data is preprocessed --- following the
pipeline of \cite{zhang2022cgail} --- to satisfy a king-move rule: consecutive
states move at most one cell per axis, $\max(|dx|,|dy|) \le 1$. Edits should
not be held to a lower standard than the data they edit; a bounded two-cell
pickup move would silently violate the very rule the source trajectories are
filtered on. Discretized edits therefore pass through an exact
backward-reachability repair that returns the king-compliant assignment
nearest the tapered targets, or reports that none exists.
% src: LIFT_ALGORITHM_REFERENCE.md §3 (king-move rule), §6 (repair; lift skips in-loop)

\textbf{Repair provenance.} About 5\% of trim edits (118/2{,}455) had no
king-move-com\-pli\-ant repair and were reverted post hoc, before any fairness
metric was computed. Ahead of building lift we also bounded the largest
under-served service gain the supply channel could deliver; that bound, and the
far smaller realized gain, are in Appendix~\ref{app:results}.
% src: PAPER/supply-lift/FINDINGS.md §8 (skip-on-infeasible)
% (2026-07-22: Robert cut the 'moved every metric favorably' clause and compressed rule-first
% to 'before fairness metrics were computed' (matches §3.4); gate detail = appendix Headroom-ceiling block)
% 2026-07-26 (Zhang R2, D7): MOVED verbatim from §4.2's "Provenance disclosures"
% paragraph. It lands beside King-move repair because that is where the rule it
% reports on is defined. \S3.4 promises "\S... reports the fraction and when it
% was applied", so 03_methodology.tex's pointer is repaired to name this block —
% a \ref repair alone would have pointed at a subsection that no longer carries
% the fraction. The 118/2,455 disclosure appears nowhere else in the paper.

\textbf{Linearized-offset screen.} The candidate screen of
\S\ref{sec:attribution}, in detail. Each trajectory's tail --- its last
$\ell$ seeking states and pickup --- is rigidly translated by every nonzero
integer offset $\delta \in [-\varepsilon, \varepsilon]^{2}$, and each
translation is scored by its linearized gain: the value-of-presence
collected at the tail's new positions minus that at its old ones, each state
weighted by the presence mass it carries and the map summed over the same
$5{\times}5$ window through which presence spreads (\S\ref{sec:problem}). A
trajectory's score is its best offset.

\textbf{Why demand-only editing has no lever in under-served units.} The three
structural facts behind \S\ref{sec:leveling}, in detail. \emph{Selection}: the
demand deficit attribution that picks trim's edits (\S\ref{sec:attribution})
concentrates in over-served, high-residual cells. \emph{Leverage}: with supply frozen,
$\partial Y_i / \partial D_i = -S_i / D_i^{2}$ makes padding demand into
high-supply advantaged cells more consequential for $Y$ than removing it from starved
ones, and 93\% of disadvantaged units sit at the demand floor $d_0$, where
removing demand changes nothing. \emph{Supply-side inequity}: median taxi
presence is 1.8 in disadvantaged cells versus 17.6 in advantaged cells, which
no demand edit can touch, and a greedy unconstrained search, an upper bound on
any demand-only editor, leaves only the perverse option of deleting the
under-served group's recorded pickups.
% 2026-07-25 (review finding #5, the focused claim read of T10's committed diff —
% two claim repairs in this relocated block):
% (1) "so under-served cells are never edit candidates" DROPPED. The ranking is by
% SIGNED per-unit attribution (famail_temporal/algorithm/attribution.py
% rank_trajectories: ascending, "most-negative alpha_i first"), and that score is
% built from squared residual projections, so a strongly UNDER-served unit whose
% residual is demographically predictable can also score negative. Under-served
% cells being unselected is therefore an empirical fact about this corpus (the
% 2,455/2,455 finding in \S\ref{sec:leveling}), not a structural guarantee of the
% selection rule. The clause the sentence keeps -- that candidates CONCENTRATE in
% over-served, high-residual cells -- is what the mechanism supports, and Leverage
% plus Supply-side inequity carry the rest of the argument.
% (2) "an upper bound on any editor" -> "on any demand-only editor". Read
% literally, the original said no editor can help the under-served, which is the
% opposite of the paper's central result: the LIFT channel does exactly that. The
% bound is over the demand-only problem, which is also how \S\ref{sec:leveling}
% frames it ("the constrained optimum of the demand-only problem").
% (2026-07-24, T10 page-budget lever 4: moved here from \S\ref{sec:leveling},
% which now names the three facts in one sentence and points at this block.)
% src: PAPER/external-metrics/LEVELING_DOWN_MECHANISM.md; LIFT_ALGORITHM_REFERENCE.md §1 (32x; 93%; median presence 1.8 vs 17.6; oracle perversity)

\textbf{Perturbation-arm stationarity.} Detail for the
baselines of \S\ref{sec:exp-baselines}. We pre-registered
$\delta = 0$-initialized ablation arms expecting a no-op --- a
difference-mode Siamese head has zero subgradient at an identical pair ---
but the deployed discriminator compares embeddings by \emph{concatenation},
which is not stationary there: measured, the $\delta = 0$ arms attack rather
than stall (the iterative arm lands within $0.0003$ of its random-start
twin; the single-step arm differs with initialization, as a one-step method
must). The stationarity assumption held only for the head we did not deploy.
% src: famail_temporal/results/2026-07-13T06-26-19/…06-27-50 arm metrics (nors); deployed checkpoint combination_mode="concatenation" (fidelity/model.py); gradient-at-zero verified live 2026-07-13

\section{Extended Results}\label{app:results}

\textbf{Weight sensitivity.} Fig.~\ref{fig:alpha-pareto} reports the full weight
sweep summarized in \S\ref{sec:exp-robustness}: six full editing runs across the
simplex (fidelity fixed at $0.1$), each scored on all three metric classes. The
optimized $\Delta F_{\mathrm{demo}}$ is flat --- within $0.001$ --- for
$\alpha_{\mathrm{sp}} \in [0, 0.55]$; the supply-channel lift-up
declines monotonically with $\alpha_{\mathrm{sp}}$ and is significant under
both accounting conventions only for $\alpha_{\mathrm{sp}} \le 0.2$; and
the external
instruments improve significantly at every point. The adopted
$(0.1, 0.8, 0.1)$ is the criterion's selection: any weighting in the flat
range delivers
the same $\Delta F_{\mathrm{demo}}$, and the low $\alpha_{\mathrm{sp}}$ is
what keeps the supply-channel lift-up significant.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/extended_frontier.pdf}
  \caption{The weight sweep, scored beyond the optimized metrics. (A) On the
  optimized plane, $\Delta F_{\mathrm{demo}}$ is flat across
  $\alpha_{\mathrm{sp}} \in [0, 0.55]$. (B) The design-targeted lift-up
  declines monotonically with spatial weight and is significant under both
  accounting conventions only for $\alpha_{\mathrm{sp}} \le 0.2$ (filled = 95\% CI
  excludes 0). The adopted $(0.1, 0.8, 0.1)$ is the three-class criterion's
  selection.}
  % src: PAPER/objective-motivation/weight-sensitivity/extended_frontier.json via paper/figures/make_figures.py
  \Description{Two stacked panels. Panel A: scatter of the weight sweep on
  the optimized-metric plane; the demographic-fairness delta is flat across
  the
  spatial weight while the spatial delta rises. Panel B: line chart showing
  the lift-up quantity declining monotonically as the spatial weight grows,
  significant under both accounting conventions only at low spatial weight;
  the adopted
  configuration is marked.}
  \label{fig:alpha-pareto}
\end{figure}

\textbf{Feature-set robustness.} Extending the summary in
\S\ref{sec:exp-robustness}: the channel mix behind the reproduced lift-up is
not constant --- on the primary set the demand channel is n.s., while under
both alternate sets it is significant and carries most of the larger totals ---
so the added-presence mechanism replicates while the demand-side share varies
with the feature set, and we report the decomposition rather than the total
alone. On the transfer controls, the most-fair selection control reaches
$+0.0054$ and $+0.0072$ at w30 under the two alternate sets, and vanilla
transfer reaches sign-unanimity only under the housing--GDP--compensation set
($+0.0022$, a tenth of its w30 gain there).
Table~\ref{tab:featsets} gives every layer of the argument under the two
alternate sets.
% (2026-07-24, T10 page-budget lever 2: the two control readings moved here from
% \S\ref{sec:exp-robustness}, whose paragraph now states their size in words and
% points at Table~\ref{tab:featsets} for the per-set values.)


\textbf{Filtering versus editing.} Detail for the filtering rebuttal of
\S\ref{sec:exp-robustness}. Deleting the $K$ most-biased trajectories rather
than editing them inverts the gain: $F_{\mathrm{demo}}$ falls to 0.7935 at the
maximum $K = 2{,}455$, where editing the same attribution-selected slice
raises it to 0.8214; neither removing nor selecting data
(\S\ref{sec:exp-downstream}) substitutes for editing it.
% src: famail_temporal/results/analysis/pareto_supplylift/pareto_points.csv (Q7-pareto ledger row)

\textbf{Supply-channel upper bound.} Supporting the provenance disclosure of
\S\ref{sec:exp-data-level}. Before the lift phase was built, an unconstrained
greedy search bounded the largest achievable gain at $+0.786$ on
$\mathrm{mean}(Y \mid \mathrm{disadv.})$, which is $2.6\times$ the
pre-registered threshold of $+0.3$ for proceeding with the build (full
accounting $+0.882$); the realized $+0.053$ sits far below this
fidelity-unconstrained ceiling, as a fidelity-bounded editor should.
% src: PAPER/supply-lift/FINDINGS.md §1.3 + data/oracle.json (G0 ceilings)

\textbf{The two fidelity axes.} \emph{Fidelity-A} is the identity axis: the
frozen driver-identity discriminator's mean score on matched pairs (an
edited trajectory against a trace of its original driver; higher is
better), the evaluation-time reading of the \S\ref{sec:objective}
guardrail, and validated by its separation from mismatched-driver pairs.
\emph{Fidelity-B} is the distributional axis: a Jensen--Shannon divergence
over aggregate mobility statistics \cite{feng2020simulate}, computed against
the raw corpus (raw scores 0); lower is better.
% src: famail_temporal/baselines/run_level1_table_v2.py (fidelity_a = gate high_matched; fidelity_a_separation); PAPER/supply-lift/FINDINGS.md §6 (HuMID identity axis, higher better); tab:l1 raw row Fid-B 0.000 (raw-referenced)

\textbf{Edited-corpus Fidelity-B components.} Supporting the fidelity
disclosure of \S\ref{sec:exp-data-level}. The corpus-level Fidelity-B of
$0.187$ concentrates in exactly the components a rerouted tail must move
(terminal cell 0.635, net displacement 0.147, coverage 0.142), while
trajectory length, which neither editing phase alters, barely registers
(0.060).
% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json (edited row fidelity_b_per_component)

% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json

\textbf{Shenzhen GAN per-seed spread.} Supporting the four-source
disclosure of \S\ref{sec:exp-downstream}. The Shenzhen GAN's Fidelity-B mean
of 0.171 hides a bimodal split across seeds ($\pm 0.129$; three of five
seeds at 0.197--0.295, two at 0.03--0.04), and its worst seeds fail
broadband --- length-component divergence 0.247 where the edited corpus sits
at 0.060.
% src: famail_temporal/results/level1_table_v2/supply_lift_shz_5seed/level1_v2_multiseed.json (gan fidelity_b values + per-component)

\textbf{Upweighting dose saturation (Shenzhen).} Supporting
\S\ref{sec:exp-downstream}. Beyond the adopted w30, the edited arm's gain
extends to $+0.0323$ (w40) and $+0.0339$ (w50), each 6/6, with the per-step
increment shrinking monotonically ($+0.0050$, $+0.0035$, $+0.0021$,
$+0.0016$) --- w30 sits at the knee. At the adopted dose the $n = 12$ flagship
carries a paired $t$-interval of $[+0.0278,$ $+0.0315]$. % lint-allow: n=12 pooled CI bounds (computed 2026-07-18), not the old-alpha supply-lift number
% src: famail_temporal/results/weighted_bc_sweep/alpha_sweep_s10_c80_f10_filtered_6seed/dose_response.json
% src: PAPER/supply-lift/data/a10/shz_a10_wbc_n12_pooled_stats.json (t-interval)
% 2026-07-26 (final review): the flagship's 95% t-interval was dropped from the
% body when the dose paragraph became Fig.~\ref{fig:dose}. A numeric-token diff
% against fbe876f showed those bounds had left the rendered paper entirely;
% the body still reports +0.0297 +/- 0.0029, which is NOT the same statistic.
% Restored here rather than lost, per R2.

\textbf{Downstream $F_{\mathrm{spatial}}$ and fidelity dose detail (Shenzhen).}
Supporting \S\ref{sec:exp-downstream}. $F_{\mathrm{spatial}}$ propagates from
$+0.0008$ at uniform weight to $+0.0052$ at w30 (each $p = .031$), while at w30
both controls significantly degrade it. Fidelity-A is unchanged at every weight
(all n.s., $|\Delta|$ at most $+0.0007$); Fidelity-B rises dose-monotonically
from $0.020$ to $0.027$ absolute, against raw $0.013$.
% src: famail_temporal/results/weighted_bc_sweep/alpha_sweep_s10_c80_f10_filtered_6seed/paired_stats.json + dose_response.json

\textbf{Demographic oversampling in detail (Shenzhen).} Supporting
\S\ref{sec:exp-baselines}. The arm duplicates real trajectories originating in
demographically disadvantaged regions
\cite{kamirancalders2012,zietlow2022} across all three equity axes, under
phantom driver IDs and a rigid $\pm 1$-cell jitter, additively rebuilding both
demand and supply at doses matched to FATE's budget. The targeted gain is
dose-monotone over the three doses ($+0.0059$, $+0.0097$, $+0.0153$), and the
untargeted placebo's degradation is what makes the gain targeting-specific.
Two disclosures. First, only 8{,}241 distinct trajectories of disadvantaged
origin exist, so meeting the matched dose $d = 10{,}000$ requires 1{,}759
draws that repeat earlier ones. Second, fidelity is not scored for this arm,
since duplicates of real trajectories pass any fidelity check by construction.
% (2026-07-24, T10 page-budget lever 1: construction, dose ladder and the two
% disclosures moved here from \S\ref{sec:exp-baselines}; the block's original
% re-duplication sentence is the first disclosure, unchanged.)
% src: PAPER/baselines/demographic-oversampling/FINDINGS.md §2-5

\textbf{Model-level variance ($n = 5$ versus $n = 10$).} Supporting
\S\ref{sec:exp-downstream}. A paired baseline-vs-FATE comparison at uniform
weight gives $\Delta F_{\mathrm{demo}} = +0.0030 \pm 0.0022$ at $n = 10$
seeds (9/10 positive; Wilcoxon $p = .0039$): the training procedure alone
transmits a small but real fraction of the edit, an order of magnitude below
the upweighted gains, so the weighting lever remains what carries the
transfer. This $n = 10$ effect size is unchanged from the original five-seed
suite ($+0.0031$) and now clears conventional significance outright
($t$-test $p = .0019$).
% src: famail_temporal/results/variance_suite/supply_lift_shz_10seed/aggregate.json (paired_delta.f_causal; B2-VAR-SZ 2026-07-16; n=5 suite retained at variance_suite/supply_lift_shz_5seed)

\textbf{Instance reweighing in detail.} Detail for the training-side
baselines of \S\ref{sec:exp-baselines}. Reweighing uses inverse group-service
weights favoring disadvantaged-origin demonstrations, scored by the full
paired protocol. While moving demand-adjusted fairness the wrong way it pays
the same distributional cost as w30 upweighting (Fidelity-B $+0.0144$), and % lint-allow: coincidental value (fair_reweigh paired Fid-B delta, not the trim-only F_demo headline)
its rolled-out policies still tilt allocation away from the disadvantaged
group ($-0.0010$, negative on all six seeds, $p = .031$).
% (2026-07-24, T10 page-budget lever 1: moved here from \S\ref{sec:exp-baselines}
% with its % lint-allow: comment on the same source line as the number.)
% src: famail_temporal/baselines/external_fairness/results/option_a_rollout_fb/summary.json (rollout: R5 arm-prefix regression gate passed, edited_w30 -0.00329 vs committed -0.0033)

\textbf{Fairness-penalty $\lambda$ grid.} Detail for the in-processing
penalty baseline of \S\ref{sec:exp-baselines}. The penalty adds $\lambda$
times the demo\-graph\-ic-parity gap in predicted next-cell mass to the
imitation loss, and was tested in
both a signed formulation (advantaged minus disadvantaged, the direction the
data over-serves) and an absolute-value formulation. It is inert at every
dose that trains stably (six-seed suites:
$|\Delta F_{\mathrm{demo}}| \le 0.0009$ signed at
$\lambda \in \{1,$ $3.16,$ $10\}$, $+0.0008$ absolute at $\lambda = 10$, all
n.s.; single-seed probes at $\lambda = 100$ agree; the term contributes on
the order of $10^{-5}$ of the loss), and destructive only at the dose where
it finally dominates: at $\lambda = 1000$ the signed form collapses
$F_{\mathrm{demo}}$ by $-0.2053$ with Fidelity-B at 0.514, the absolute form
by $-0.1293$ (negative on all six seeds) with Fidelity-B at 0.363, against raw's
$\approx 0.013$. The absolute formulation's shallower collapse is the gap
pushing back once it crosses zero; the two formulations track each other at
every $\lambda$.
% RESOLVED(formulation) 2026-07-18: FB-PENALTY-ABS pilot+suite — absolute tracks signed at every
% dose (pilot point-by-point on the signed grid; inert <=100, catastrophic at 1000); the spec's
% pre-committed rule fired on its "drop the scoping" branch. Prior NOTE(formulation) retired.
% src (1e-5 derivation): probe-observed gap magnitudes ~1e-6 (task-4 report + fairness_penalty_probe)
% x lambda<=10 vs CE ~O(1); penalty_values not persisted by the harness — camera-ready chore to persist.
% src: famail_temporal/results/weighted_bc_sweep/fairness_penalty_6seed/paired_stats.json + fairness_penalty_probe/ (lambda 100/1000 single-seed probes)
% src: famail_temporal/results/weighted_bc_sweep/fairness_penalty_abs_{pilot,6seed}/ (FB-PENALTY-ABS 2026-07-18: l10 +0.0008 n.s.; l1000 -0.1293 6/6, Fid-B 0.363)

\textbf{Perturbation-arm constructions and per-arm figures.} Supporting
\S\ref{sec:exp-baselines}. The two gradient arms attack the frozen identity
discriminator with ST-iFGSM \cite{hu2023stifgsm}, the spatio-temporal
iterative FGSM \cite{kurakin2017ifgsm}, and with single-step FGSM
\cite{goodfellow2015fgsm}, both under PGD-style random initialization inside
the $\varepsilon$ ball \cite{madry2018pgd}; We pre-registered these arms as expected
not to improve fairness; random jitter is the one arm that did.
% 2026-07-26 (final review, R2): restored. The style pass cut the body's
% "expected not to improve" clause as a pre-emption, which D2 wants gone from the
% main text, but a pre-registration is a disclosure and R2 says MOVE not DELETE.
% Without it random jitter's +0.0135 reads as an unexplained anomaly rather than
% a logged prediction failure.
The random-start arms are the reported
ones (the pre-registered $\delta = 0$ ablation:
Appendix~\ref{app:editor}). Identity fidelity survives every arm
($\approx 0.846$): the perturbation is too small to break driver identity, so
the adjacency and distributional axes carry the comparison. Random jitter
raises $F_{\mathrm{demo}}$ because indiscriminately diffusing the selected
trajectories diffuses their over-service too, and that surprise does not recur
on San Francisco, where random is the weakest mover.
% (2026-07-24, T10 page-budget lever 1: arm constructions, the identity-fidelity
% reading, the diffusion mechanism and the San Francisco replication moved here
% from \S\ref{sec:exp-baselines}; all four perturbation cites travelled with them.)
% src: famail_temporal/baselines/baseline_table/baseline_table.json (Fid-A ~0.846 across arms)
On Shenzhen the gradient arms violate king-move adjacency (54.4\% ST-iFGSM,
91.4\% FGSM) with distributional divergence 0.143 and 0.339 respectively,
against the edited corpus's 0.187.
% src: famail_temporal/baselines/baseline_table/baseline_table.json (Shenzhen Fid-B(JS)/adjacency-violation columns)
On San Francisco all three arms land well below the editor's $+0.0316$,
violate king-move adjacency wholesale (72.0/87.0/97.5\%), and pay
$2$--$4\times$ the edited corpus's distributional divergence there,
0.198--0.418 against 0.098.
% src: famail_temporal/results/2026-07-16T16-08-{05_baseline_ifgsm,16_baseline_fgsm,23_baseline_random}_sf12/metrics.json (B1 runs 2026-07-16; SF Fid-B from level1 sf12 edited row = 0.098)

% src: PAPER/supply-lift/FINDINGS.md §3 (FATE row); famail_temporal/baselines/baseline_table/baseline_table.json (perturbation rows);
% src: oversampling cells = 3-seed means over *_d10000_s{0,1,2} metrics.json deltas (matches the committed FINDINGS aggregation; the assembler's seed-0-only d10000 row reads +0.0175)

\textbf{San Francisco channels and caveats.} Supporting \S\ref{sec:exp-sf}.
At the data level $F_{\mathrm{spatial}}$ rises by $+0.0139$ alongside the % lint-allow: coincidental value (SF a10 F_spatial delta, not the trim-only F_demo headline)
$F_{\mathrm{demo}}$ gain, and the DP gap on the migrant axis is significant
under \emph{both} grouping conventions while DI is significant under one and
n.s.\ under the other, a grouping dependence the 12-driver sample makes
unsurprising. The total decomposes as follows. The supply channel is
$+0.0209$ under fractional-presence accounting
(CI $[+0.0122,$ $+0.0300]$) and $+0.1027$ under the distinct-taxi
recount (CI $[+0.0872,$ $+0.1203]$). The demand channel is
significantly negative, $-0.0533$
(CI $[-0.0798,$ $-0.0298]$). That is large enough to make the
fractional-presence total net-negative, $-0.0324$ with a CI
excluding zero, before the recount turns it positive. Downstream,
the dose ladder is $+0.0242$ (w10) and $+0.0309$ (w20), each 6/6, before the
$n = 12$ w30 flagship reported in \S\ref{sec:exp-sf}.
% src: PAPER/supply-lift/data/a10/sf12_a10_metrics.json + sf12_a10_external_fairness.json
% src: PAPER/supply-lift/data/a10/sf12_a10_channel_decomposition.json (D1-CHAN 2026-07-18, B=2000 seed 0)
% src: PAPER/supply-lift/data/a10/sf12_a10_wbc_n12_pooled_stats.json (prior n=6: vanilla +0.0026 5/6 p=.0625; w30 +0.0332 6/6)

\emph{Caveats.} ACS foreign-born share proxies the migrant axis of Shenzhen;
the 12-driver density-matched subsample is small; and 14.9\% of raw
Cabspotting trajectories violate king-move adjacency at source (GPS gaps), so
no corpus built from this data can be fully compliant. The standard we hold the
editor to is therefore that its own edits introduce no violations (all of them
comply), and the edited corpus ends \emph{more} compliant than raw (87.6\%
vs.\ 85.2\%). The Shenzhen GAN's distributional collapse does not recur here
(Fidelity-B 0.039, cf.\ \S\ref{sec:exp-downstream}); that cautionary note is
city-specific.
% src: famail_temporal/results/level1_table_v2/supply_lift_sf12_5seed/level1_v2_multiseed.json (gan row)
% src: PAPER/supply-lift/data/a10/sf12_a10_metrics.json (provenance.compliance.filtered: 0.87647/0.85197); PAPER/argument/07_limitations.md §5-7 (era-neutral caveats; FINDINGS.md §10 is the old-alpha doc — do not copy numbers from it)
% 2026-07-26 (Zhang R2 review, BLOCKER): the §4.5 compression DELETED this
% material instead of moving it, while §4.5's own comment claimed "Nothing
% deleted; numbers moved" and its prose promised the reader these caveats were
% here. Restored verbatim from fbe876f. Values that had left the paper entirely:
% +0.0139, the fractional-presence supply +0.0209 and its CI, the demand channel % lint-allow: provenance comment listing restored SF values, not a claim
% -0.0533 and its CI, the net-negative total -0.0324, the w10/w20 SF ladder,
% 14.9%, 87.6/85.2, and the SF GAN Fidelity-B 0.039. The appendix is outside the
% 8pp body limit, so restoring costs nothing. R2's guard rail is MOVE over
% DELETE for any disclosure, limitation or negative result; this is all three.

\textbf{San Francisco downstream detail.} Supporting \S\ref{sec:exp-sf}.
At $n = 12$ the controls sharpen the contrast rather than soften it: the
random placebo turns significantly \emph{negative} ($-0.0077$, positive on
only 2 of 12, $p = .0024$), and the most-fair selection control is
significantly positive but about a sixth of the edited arm's gain
($+0.0059$, 10 of 12 positive, $p = .0020$; at $n = 6$ per dose it was
significantly negative at w10 and n.s.\ at w20).
% src: PAPER/supply-lift/data/a10/sf12_a10_wbc_n12_pooled_stats.json (SF-WBC-N12 2026-07-18)
Extended doses: $+0.0370$ (w40) and $+0.0371$ (w50), each 6/6, flat past
w40; the random control stays null-to-negative and the most-fair selection
control stays significantly \emph{positive} ($+0.0063$/$+0.0066$) --- the
dominated-control pattern of the alternate feature sets
(\S\ref{sec:exp-robustness}).
External-instrument grouping detail: the DP gap is $-0.0729$
[$-0.1132$, $-0.0259$] under district extremes and $-0.0370$ [$-0.0733$,
$-0.0002$] under median split; DI is $+0.0058$ [$+0.0004$, $+0.0103$]
under district extremes and n.s.\ under median split.
% src: PAPER/supply-lift/data/a10/sf12_a10_external_fairness.json (MigrantRatio, both conventions; moved from the San Francisco block, now §4.5, argument-triage 2026-07-23) Model-level variance: the SF suite stays null
at $n = 10$ seeds ($\Delta F_{\mathrm{demo}} = -0.0009 \pm 0.0035$, mixed
signs, closer to zero than at $n = 5$), unlike the primary city's small
significant positive effect. Four sources (generators retrained from
scratch; identity gate passed, matched 0.958 vs.\ mismatched 0.034): the
edited corpus is the fairest source ($F_{\mathrm{demo}}$ 0.9067 vs.\ GAN
0.8795, raw 0.8752, BC 0.8744) while identity-faithful (Fidelity-A 0.9581
$\approx$ raw 0.9578; Fidelity-B 0.098, the expected bounded-edit
divergence).
% src: famail_temporal/results/weighted_bc_sweep/supply_lift_a10_sf12_dose_ext_6seed/paired_stats.json (SF-WBC-DOSEEXT 2026-07-17; increments w30->w40 +0.0038, w40->w50 +0.0001)
% src: famail_temporal/results/variance_suite/supply_lift_sf12_10seed/aggregate.json (paired_delta.f_causal; B2-VAR-SF 2026-07-16; n=5 suite retained)
% src: famail_temporal/results/level1_table_v2/supply_lift_sf12_5seed/level1_v2_multiseed.json

\textbf{Rollout allocation shares (both cities).} Supporting the future-work
discussion of \S\ref{sec:conclusion}. Beyond metric propagation, we rolled
out the trained policies and measured the share of pickups they allocate to
disadvantaged areas. That share still tilts slightly negative: on Shenzhen,
$-0.0049$ at w30 on the demand-only corpus and $-0.0033$ under trim+lift
(negative on all six seed pairs in both suites, $p = .031$), attenuated by
roughly a third, not reversed; San Francisco replicates the pattern at
roughly ten times the magnitude ($-0.0432$ demand-only, $-0.0335$
trim+lift, again negative on all six seed pairs in both suites), roughly
$22\%$ attenuation. Seeking-state shares are n.s.\ at every weight in both
suites. The paper claims the data-level lift-up and the metric propagation;
this allocation gap is what motivates training-side allocation constraints
as future work.
% src: famail_temporal/baselines/external_fairness/results/option_a_rollout_a10/summary.json + option_a_rollout_trimonly_a10/summary.json (edited_w30.MigrantRatio.pickups.share_D_delta; SZ)
% src: famail_temporal/baselines/external_fairness/results/option_a_rollout_sf12_tl/summary.json + option_a_rollout_sf12_trimonly/summary.json (C3-SF-ROLLOUT 2026-07-17; attenuation 1-0.0335/0.0432=22.5%)
% src: PAPER/supply-lift/data/rollout_supplylift_summary.json + rollout_trimonly_prior_summary.json; FINDINGS.md §9
% (moved from 'The allocation boundary' + the SF passage, argument-triage 2026-07-22; restore at camera-ready if space allows. §4.6 no longer exists: the SF block merged into §4.5 in the 2026-07-26 Zhang R2 restructure.)

\section{Statistical Protocol Details}\label{app:protocol}

\textbf{Exact-Wilcoxon floor arithmetic.} With $n = 6$ paired seeds the
two-sided Wilcoxon signed-rank $p$ floors at $0.03125$, attained exactly
when all six seeds share a sign, which is why $p = 0.031$ reads only as a
sign-unanimity certificate. The flagship w30 comparison at $n = 12$ paired
seeds has floor $.00049$, low enough for an exact Wilcoxon $p$ to survive
multiple-comparison correction. Suites with $n = 5$ seeds cannot reach
$p < 0.05$ (floor $0.0625$) and are reported as effect-versus-noise.
% src: PAPER/argument/04_evaluation.md §3; PAPER/external-metrics/FINDINGS.md §1, §5

\textbf{Why the bootstrap resamples units.} A driver-level bootstrap is
unavailable rather than declined: the demand grid is a mean-hourly artifact,
not a per-driver sum, and supply is environmental, so $Y$ does not decompose
into driver contributions. Active grid units are the only available
resampling level.
% src: PAPER/external-metrics/FINDINGS.md §5

\textbf{Instrument definitions.} The service ratio $Y$ is grouped
per equity axis (housing price, compensation, migrant share) under two
conventions, district extremes and median split. Demographic parity (DP) is
the signed gap between the group levels
$\mathrm{mean}(Y \mid \mathrm{disadv.})$ and
$\mathrm{mean}(Y \mid \mathrm{adv.})$; disparate impact (DI) is their ratio
(1 = parity); the Theil index is between-region and grouping independent.
% src: PAPER/external-metrics/FINDINGS.md §1, §4.3 (DP≡gap disclosure)

\section{Extended Related Work}\label{app:related}
% Compressed 2026-07-24 to hold the 12-page TOTAL bound (Robert's directive) while the
% appendix absorbed the F_demo closed form + grid config. The original VERBATIM five-theme
% survey (Dr. Zhang's per-theme FATE-contrast cadence) is preserved at commit c2f4cb9 and in
% paper-pre-rewrite-2026-07-23/sections/02_related_work.tex — camera-ready restoration can
% draw on either. All 33 cite keys of the original are retained below.

\textbf{Fairness interventions in machine learning.} They group into
pre-, in-, and post-processing \cite{barocas2023}; the pre-processing
instruments FATE builds on are instance reweighing \cite{kamirancalders2012}
and disparate-impact repair \cite{feldman2015}, both built for labeled
classification data. Among formal definitions \cite{vermarubin2018}, FATE
targets conditional statistical parity \cite{corbettdavies2017}. As
formulated, none operate on sequential demonstrations or score fairness
over a spatial service distribution. FATE moves reweighing onto edited
demonstrations, and the parity condition onto a continuous spatial service
ratio used as a differentiable editing objective.

\textbf{Fairness in urban transportation.} Transportation equity measures
service distributions with the Gini index
\cite{su2018taxigini,horchergraham2021,karner2024}
and entropy- or welfare-based indices \cite{theil1967,atkinson1970,demaio2007}.
The closest modeling neighbor regularizes a ride-hailing demand predictor at
training time, shrinking a racial prediction-error gap from 0.361 to 0.084
\cite{zheng2023};
% verified: paper/reviews/2026-07-12-prose-citation-audit.md (zheng2023 Table 2, Conv-LSTM arm)
that correction lives inside one model's loss and does not transfer to any
other consumer of the data. FATE addresses the same bias one stage earlier,
on the demonstrations, and is scored on equity measures its objective never
optimizes.

\textbf{Imitation learning for mobility, and mobility as identity.}
Conditional GAIL variants recover taxi-driver strategies from trajectories
\cite{zhang2019cgail,zhang2022cgail}, explainable extensions expose the
recovered policies \cite{pan2020xgail}, and neural simulators generate human
mobility \cite{feng2020simulate}; this line optimizes realism and never
audits the demographic fairness of the imitated allocation. A parallel line
establishes that mobility is highly identifying: trajectory-user linking
\cite{gao2017tuler,zhou2018tulvae,miao2020deeptul} and Siamese verification
of seeking traces \cite{ren2020stsiamese}. FATE contributes no new generator
or imitation algorithm; it edits the demonstrations such models consume, and
repurposes the identity-verification line as a frozen fidelity guardrail,
avoiding the instability of a live adversarial game \cite{hoermon2016}.

\textbf{Adversarial perturbation and recourse.} The editing machinery
descends from bounded signed-gradient perturbations
\cite{goodfellow2015fgsm,kurakin2017ifgsm} instantiated spatio-temporally
\cite{hu2023stifgsm}, with discrete structure bridged by straight-through
estimation \cite{bengio2013ste} and continuous relaxation
\cite{jang2017gumbel,maddison2017concrete}. Algorithmic recourse reads the
same counterfactual tooling constructively \cite{ustun2019recourse,
wachter2018counterfactual}, but only to flip one individual's decision, with
no population-level fairness target and no data-realism obligation. FATE
extends the constructive reading from the individual to the population, and
the $\varepsilon$-bound is reinterpreted from an attack-stealth budget to an
identity-preservation budget.

\textbf{Leveling down and feedback loops.} The leveling-down objection
(equality achieved by making the better-off worse rather than the worse-off
better) originates in the ethics of equality \cite{parfit1997}, has been
applied to machine-learning fairness constraints \cite{mittelstadt2024}, and
data augmentation is the one intervention class observed to help the
disadvantaged group instead \cite{zietlow2022}. Feedback-loop analyses show
that allocation systems trained on their own historical output entrench a
self-reinforcing bias \cite{ensign2018,lumisaac2016}. Both critiques shape
this paper: trim's demand-only editing is an analogue of leveling down
(\S\ref{sec:leveling}), lift exists expressly to level up, and demand
endogeneity bounds what any demand-adjusted metric can see.
